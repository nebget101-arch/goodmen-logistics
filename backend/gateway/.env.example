# API Gateway – copy to .env and set values
# Development (defaults used if omitted)
PORT=4000
TARGET_BACKEND_URL=http://localhost:3000
CORS_ORIGIN=http://localhost:4200

# Downstream microservice URLs (gateway proxies /api/* to these)
# REPORTING_SERVICE_URL covers /api/dashboard, /api/reports, /api/audit, /api/insights
REPORTING_SERVICE_URL=http://localhost:5001

# Production: set these for your deployment
# PORT=4000
# TARGET_BACKEND_URL=https://your-backend.example.com
# CORS_ORIGIN=https://your-frontend.example.com
# NODE_ENV=production
