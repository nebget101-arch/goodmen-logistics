package core;

/**
 * Placeholder runner for the karate-core module.
 *
 * Per-service runners (e.g. {@code gateway.GatewayRunner}) live in their own
 * modules and consume shared helpers, hooks, and base configuration from this
 * module via its test-jar. No feature files run from karate-core itself.
 */
public class CoreRunner {
}
