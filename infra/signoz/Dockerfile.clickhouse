FROM clickhouse/clickhouse-server:23.11.1.2711

# Add single-node cluster config so SigNoz OTel Collector's
# ON CLUSTER cluster DDL succeeds on a standalone instance.
COPY infra/signoz/clickhouse-cluster.xml /etc/clickhouse-server/config.d/cluster.xml
