FROM signoz/query-service:0.53.0

# Create writable directory for SQLite metadata DB
RUN mkdir -p /var/lib/signoz && chmod 777 /var/lib/signoz

ENV STORAGE=clickhouse
ENV SIGNOZ_LOCAL_DB_PATH=/var/lib/signoz/signoz.db
ENV SIGNOZ_TELEMETRY_ENABLED=false

EXPOSE 8080
