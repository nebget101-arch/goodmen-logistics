# FN-1464: POST /api/vehicles/customer fails — `column "customer_id" of relation "customer_vehicles" does not exist`

## Requirement
Fix the production regression where adding a vehicle for a customer on the Work Orders page returns HTTP 500. Root cause: migration `20260317093000_rename_customers_to_shop_clients.js` renamed `customer_vehicles.customer_id` → `shop_client_id` but `backend/packages/goodmen-shared/routes/vehicles.js` and `frontend/.../basics-tab.component.ts` were not updated.

## Acceptance Criteria
- [ ] POST `/api/vehicles/customer` with either `customer_id` or `shop_client_id` in the body succeeds and persists the vehicle linked to the specified shop client
- [ ] PUT `/api/vehicles/:id` updates of `shop_client_id` on `customer_vehicles` rows succeed
- [ ] Work Orders Basics tab — adding a vehicle for a selected customer:
  - New vehicle appears in the customer-scoped vehicle list immediately
  - Re-loading the work order shows the vehicle linked to the customer
- [ ] No console errors or 500s in the e2e flow
- [ ] Existing customer-vehicle linkage continues to render correctly (no regression on stored rows)

## Dependencies
None — this is a standalone production bug fix.

## Agent
Multi-agent: backend + frontend + qa

## Integration Branch
`integration/FN-1464` (created by first subtask agent from `origin/dev`; subtasks branch off this; bug PR merges to `dev`)

## Subtasks
| Key | Summary | Agent | Branch | Files Touched | Status |
|-----|---------|-------|--------|---------------|--------|
| FN-1467 | Backend — fix customer_vehicles routes to use shop_client_id | backend | `backend/FN-1467/customer-vehicles-shop-client-id` | `backend/packages/goodmen-shared/routes/vehicles.js` | Done |
| FN-1465 | Frontend — migrate basics-tab customer_id reads/writes to shop_client_id | frontend | `frontend/FN-1465/basics-tab-shop-client-id` | `frontend/src/app/components/work-order/tabs/basics-tab/basics-tab.component.ts` | Done |
| FN-1466 | QA — validate add-customer-vehicle flow on Work Orders page | qa | _manual_ | — | Blocked by FN-1467, FN-1465 |

## API Contract (canonical, to allow parallel work)
- **Field name on the wire**: `shop_client_id` (canonical). Backend MUST also accept `customer_id` as an alias on incoming JSON during the transition.
- **POST `/api/vehicles/customer`** request body includes `shop_client_id` (or alias `customer_id`); response row exposes `shop_client_id`.
- **GET / readback** (`SELECT * FROM customer_vehicles WHERE vehicle_uuid = $1` and the `all_vehicles` view) — already returns `shop_client_id` post-rename. No backend change needed for reads.
- Frontend reads SHOULD prefer `vehicle.shop_client_id ?? vehicle.customer_id` to tolerate any cached/stale responses.

## Implementation Summary

### FN-1467 (subtask)
Backend customer-vehicles routes in `backend/packages/goodmen-shared/routes/vehicles.js` now use the post-rename column `shop_client_id`. The wire contract accepts both inbound shapes (`shop_client_id` canonical, `customer_id` as a transitional alias) so the frontend subtask (FN-1465) can land independently.

- **POST `/api/vehicles/customer`** — destructures both `shop_client_id` and `customer_id` from `req.body`, coalesces to `shopClientIdInput = shop_client_id ?? customer_id`, and the INSERT writes `shop_client_id` (replacing `customer_id` in the column list and parameter array). The OpenAPI block lists `shop_client_id` as canonical and marks `customer_id` `deprecated: true` with a note that it is mapped server-side.
- **PUT `/api/vehicles/:id`** — at handler entry, normalizes `req.body` once: if `customer_id` is present and `shop_client_id` is absent, aliases `customer_id` → `shop_client_id` and deletes the old key. The `excludedFields` list (vehicles base table path) and `customerAllowed` set (customer_vehicles fallback path) both reference `shop_client_id` instead of `customer_id`.
- **Readback** — `SELECT * FROM customer_vehicles WHERE vehicle_uuid = $1` and the `all_vehicles` view already expose `shop_client_id` post-rename. No backend read change needed.

### FN-1465 (subtask)
Migrated the Work Orders Basics tab vehicle owner field from `customer_id` to `shop_client_id`. All reads use `vehicle.shop_client_id ?? vehicle.customer_id` to tolerate cached/old responses; the create-vehicle write now sends `payload.shop_client_id` (backend accepts both names during the transition per the API contract).

Changes in `frontend/src/app/components/work-order/tabs/basics-tab/basics-tab.component.ts`:
- `onVehicleSelect()` (~line 320): customer-link branch resolves owner via `shop_client_id ?? customer_id` and uses the resolved id for both `workOrder.customerId` assignment and customer lookup.
- `createVehicleFromWorkOrder()` (~line 468): outgoing payload now sets `shop_client_id` instead of `customer_id`.
- `getVehicleSearchPool()` (~line 513): customer-scoped vehicle filter prefers `v.shop_client_id`, falls back to `v.customer_id`/`v.customerId`.
- `applyCustomerVehicleFilter()` (~line 537): mismatch comparison reads `selectedVehicle.shop_client_id ?? selectedVehicle.customer_id`.

`frontend/src/app/services/api.service.ts` was not touched — vehicle responses are typed as `any` and have no `customer_id` field declaration to update.

## Files Changed
- `backend/packages/goodmen-shared/routes/vehicles.js` (FN-1467)
- `frontend/src/app/components/work-order/tabs/basics-tab/basics-tab.component.ts` (FN-1465)

## Key Decisions
- **Canonical wire field**: `shop_client_id`. `customer_id` accepted only as a deprecated inbound alias to allow the frontend (FN-1465) to migrate without coupling to backend deploy order.
- **Alias once at handler entry** (FN-1467): PUT handler normalizes `req.body` in one place rather than scattering `?? customer_id` through the field-walk — keeps both paths (vehicles, customer_vehicles fallback) honest with a single source of truth.
- **`shop_client_id ?? customer_id` fallback on the FE** (FN-1465): tolerates any cached/stale responses still emitting the old field name during the transition window.
- Did not change `this.workOrder.customerId` (FE): the work order's own `customerId` property is a separate domain field on the work order, not the vehicle's owner — explicit "out of scope" in the FE subtask.
- **Did not touch** `routes/maintenance.js` (`maintenance_records` was not renamed) or `routes/work-orders-hub.js` (already accepts both names defensively). Did not change DB migrations.

## Deployment Handoff

### FN-1467 (subtask — backend)
- **Service to redeploy**: `customer-service` (the only backend service that mounts this routes file). See `.agent/docs/render_services.md`.
- **No DB migrations** needed — the column rename ships with `20260317093000_rename_customers_to_shop_clients.js` (already in place).
- **No env vars / feature flags** — change is transparent to callers.
- **Cache**: none.
- **Deploy order**: backend can deploy independently of frontend (FN-1465). After both ship, the `customer_id` inbound alias becomes pure dead-grace and can be removed in a follow-up cleanup once no clients are observed sending the legacy field.
- **Post-deploy verification**:
  1. POST `/api/vehicles/customer` with `{ shop_client_id: "<uuid>", ... }` → 201, response row has `shop_client_id`.
  2. POST `/api/vehicles/customer` with the legacy `{ customer_id: "<uuid>", ... }` → 201, row has `shop_client_id` set to the same uuid.
  3. PUT `/api/vehicles/<customer-vehicle-uuid>` with `{ shop_client_id: "<uuid>" }` → 200, row updated.
  4. PUT same endpoint with `{ customer_id: "<uuid>" }` → 200, row updated (alias path).
  5. No 500s; no `column "customer_id" of relation "customer_vehicles" does not exist` in logs.

### FN-1465 (subtask — frontend)
- **Service to redeploy**: `goodmen-frontend` (Render — Angular SPA build).
- **DB migrations**: none from this subtask. `20260317093000_rename_customers_to_shop_clients.js` is already applied in production (this subtask is the FE catch-up).
- **Backend coordination**: depends on FN-1467 (backend write path must accept `shop_client_id` and/or alias `customer_id`). Frontend now sends `shop_client_id`; backend must accept it before the integration PR ships.
- **Cache invalidation**: none — pure code change.
- **Feature flags**: none.
- **Deploy order**: FN-1467 backend → frontend → QA on staging.
- **Post-deploy verification** (FN-1466 covers in detail): Work Orders → New WO → select customer → "Add new vehicle" → submit returns 200 and the vehicle appears in the customer-scoped dropdown; reload preserves the customer link.

## Review Findings
_To be filled by Code Reviewer_

## QA Findings
_To be filled by QA_

## Open Items
- None
