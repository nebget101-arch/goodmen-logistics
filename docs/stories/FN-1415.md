# FN-1415: Manual FMCSA import trigger (FleetNeuron-internal only) + biweekly cron + admin UI

## Requirement

Wire the importers from FN-1413 and FN-1414 into a runtime control plane:

1. **Biweekly cron** (every 14 days, Sunday 4 AM UTC) — auto-runs all five importers
2. **Manual trigger API** — `POST /api/fmcsa/imports/run` with `{ files: [...] }`. Restricted to FleetNeuron internal tenant via `tenants.is_internal = true`. 403 for everyone else.
3. **Import history API** — `GET /api/fmcsa/imports` returns recent runs from the ledger
4. **Admin UI** — minimal Angular page for FleetNeuron internal users only: trigger panel + history table with live status polling

## Acceptance Criteria

- [ ] Cron registered for biweekly cadence; first scheduled run logs `triggered_by = 'cron'`
- [ ] `POST /api/fmcsa/imports/run` enqueues selected importers, returns 202 + run IDs
- [ ] `dryRun: true` mode parses without persisting
- [ ] `GET /api/fmcsa/imports` returns ledger ordered by `started_at DESC`
- [ ] Both endpoints return 403 for non-FleetNeuron tenants and for callers without `fmcsa.imports.manage`
- [ ] Cron disabled in `NODE_ENV=development` by default
- [x] Admin UI page: trigger panel with checkboxes per file, dry-run toggle, history table auto-refreshing every 5s while runs are in progress
- [x] Non-internal users redirected away from the UI route
- [ ] Cron expression configurable via `FMCSA_IMPORT_CRON` env var

## Dependencies

- **Blocked by FN-1412** — needs `tenants.is_internal` column and `getFmcsaKnex()`
- Backend and frontend subtasks can run in parallel via documented response contract

## Agent

backend (lead) + frontend

## Integration Branch

`integration/FN-1415` (created by first subtask agent from `origin/dev`; subtasks branch off this; story PR merges to `dev`)

## Subtasks

| Key | Summary | Agent | Branch | Files Touched | Status |
|-----|---------|-------|--------|---------------|--------|
| FN-1424 | [BE] Manual trigger API + biweekly cron + is_internal middleware | backend | `backend/FN-1424/fmcsa-import-control-plane` | `backend/packages/goodmen-shared/routes/fmcsa-imports.js`, `middleware/require-internal-tenant.js`, `services/fmcsa-import-queue.js`, `integrations-service/server.js`, `config/permissions.js` | Selected for Dev |
| FN-1425 | [FE] Admin UI page for FMCSA imports | frontend | `frontend/FN-1425/fmcsa-imports-admin-ui` | `frontend/src/app/components/admin/fmcsa-imports/**` (new), `services/fmcsa-imports.service.ts` (new), `guards/internal-tenant.guard.ts` (new), `app-routing.module.ts`, `app.module.ts`, `app.component.{html,ts}`, `services/access-control.service.ts`, `models/access-control.model.ts` | Done |
| FN-1426 | [QA] Validate manual trigger + cron + tenant gating | qa | _manual_ | — | Blocked (pending FN-1424) |

## Implementation Summary
_To be filled by implementing agent_

### FN-1425 (subtask)

Built the Angular admin page that consumes the FN-1424 control-plane endpoints. Highlights:

- New `FmcsaImportsAdminComponent` under `components/admin/fmcsa-imports/` (matching existing `components/admin/inbound-email-settings` layout — the codebase has no `admin/` feature module, so the route is registered inline in `app-routing.module.ts` rather than introducing a new module just for one page).
- New `FmcsaImportsService` (HTTP wrapper for `GET /api/fmcsa/imports` and `POST /api/fmcsa/imports/run`).
- New `InternalTenantGuard` redirects non-internal tenants to `/dashboard` (not 403) so the admin route is silent for everyone outside FleetNeuron.
- Threaded an `isInternal` flag through `UserAccess` + `AccessControlService.isInternalTenant()` so the guard prefers the backend-supplied flag once FN-1424 ships and falls back to the legacy `tenantName === 'fleetneuron default tenant'` check until then.
- Added a "FMCSA Imports" link to both the desktop and mobile user-menu nav, gated by `canSeeFmcsaImportsAdmin()`.
- Live polling: the history table re-fetches every 5s while any run is `queued` / `running` and stops once they all reach `success` / `error`. Confirmation modal blocks blind clicks on the Run button. Dry-run rows render with a `DRY RUN` tag and a tinted background so they cannot be confused with live runs.
- Unit specs cover service HTTP shape and the component's selection / submit / polling / formatting helpers.
- Styling follows the existing dark `ai-admin-shell` / `ai-panel` tokens used by the inbound-email admin page.

## Files Changed
_To be filled by implementing agent_

### FN-1425 (subtask)
- `frontend/src/app/components/admin/fmcsa-imports/fmcsa-imports.component.ts` (new)
- `frontend/src/app/components/admin/fmcsa-imports/fmcsa-imports.component.html` (new)
- `frontend/src/app/components/admin/fmcsa-imports/fmcsa-imports.component.css` (new)
- `frontend/src/app/components/admin/fmcsa-imports/fmcsa-imports.component.spec.ts` (new)
- `frontend/src/app/services/fmcsa-imports.service.ts` (new)
- `frontend/src/app/services/fmcsa-imports.service.spec.ts` (new)
- `frontend/src/app/guards/internal-tenant.guard.ts` (new)
- `frontend/src/app/services/access-control.service.ts` (added `isInternalTenant()` and `canAccessFmcsaImportsAdmin()` helpers; parses `isInternal` from `/api/auth/me`)
- `frontend/src/app/models/access-control.model.ts` (added optional `isInternal: boolean` to `UserAccess`)
- `frontend/src/app/app-routing.module.ts` (registered `/admin/fmcsa-imports` with `InternalTenantGuard`)
- `frontend/src/app/app.module.ts` (declared `FmcsaImportsAdminComponent`)
- `frontend/src/app/app.component.ts` (added `canSeeFmcsaImportsAdmin()`)
- `frontend/src/app/app.component.html` (added FMCSA Imports user-menu links — desktop + mobile)

## Key Decisions
_To be filled by implementing agent_

### FN-1425 (subtask)
- **No new admin module.** The original ticket suggested an `admin.module.ts` / `admin-routing.module.ts` pair, but the existing codebase already registers admin routes inline in `app-routing.module.ts` and houses admin components under `components/`. Adding a fresh module just for one page would diverge from the established pattern with no upside, so the FMCSA admin page follows the existing convention.
- **Two-source `isInternal` resolution.** The backend (FN-1424) is expected to add `isInternal` to `/api/auth/me`. The frontend reads that field if present and otherwise falls back to the legacy tenant-name check (`'fleetneuron default tenant'`) so the UI works regardless of subtask landing order on `integration/FN-1415`.
- **`5s` polling implemented as a self-rescheduling `setTimeout`.** Cleaner than `interval()` because the cancellation logic is purely status-driven — when no run is `queued` / `running`, no timer is scheduled.

## Deployment Handoff
_To be filled by implementing agent_

### FN-1425 (subtask)
- **Service to redeploy:** `goodmen-frontend` only. No backend or DB changes in this subtask.
- **Migrations:** none (FN-1412 already added `tenants.is_internal` and seeded the FleetNeuron default tenant).
- **Env vars:** none added or changed.
- **Cache invalidation:** none — `cache-busting.interceptor` already covers `/api/auth/me`.
- **Feature flags:** none — the admin route is naturally hidden by the tenant guard.
- **Deploy order:** Frontend can ship at any time; the route only renders for FleetNeuron-internal users (and on tenants where `isInternal=true` is exposed by `/api/auth/me`, which is the FN-1417/FN-1424 backend change).
- **Post-deploy verification:** as an internal-tenant admin, hit `/admin/fmcsa-imports` from the user menu, confirm the Refresh button loads the ledger (empty before FN-1424), confirm the Run button is disabled until at least one file is checked, and confirm a non-internal tenant gets redirected to `/dashboard` (no error toast).

## Review Findings
_To be filled by Code Reviewer_

## QA Findings
_To be filled by QA_

## Open Items
- Confirm response shape for `POST /api/fmcsa/imports/run` and `GET /api/fmcsa/imports` between backend and frontend subtasks before either starts coding (use the description in FN-1424)
- Confirm where in the existing nav the admin page should live (likely under existing internal admin nav — locate during implementation)
