# FN-787: Phase 2 — Hover Toolbar, Inline Editing & Hover Previews

## Requirement
Replace the per-row ⋮ dropdown with a fade-in hover toolbar. Inline-edit status, billing, and notes from the row. Rich hover previews for notes/attachments/driver/broker cells.

## Acceptance Criteria
See Jira FN-787. Highlights:
- [x] Hover toolbar fades in <100ms with View/Edit/Clone/Map/Dispatch/Delete
- [x] Inline status + billing + notes edits persist without modal
- [x] Hover previews render driver position/HOS, broker terms/credit, attachment thumbnails, full notes
- [ ] Row click opens detail drawer (stubbed until FN-788 lands)

## Dependencies
None (row-click wiring stubs until FN-788 ships). Blocks **FN-791** and **FN-792**. Internal order: FN-804 → FN-805, FN-806.

## Agent
frontend

## Subtasks
| Key | Summary | Agent | Branch | Status |
|-----|---------|-------|--------|--------|
| FN-804 | `hover-popover` reusable component | frontend | `frontend/FN-804/hover-popover` | Done |
| FN-805 | Hover toolbar + inline status/billing/notes edits | frontend | `frontend/FN-805/hover-toolbar-inline-edits` | Done |
| FN-806 | Hover previews (notes/attachments/driver/broker) | frontend | `frontend/FN-806/hover-previews` | Done |
| FN-807 | QA validation | qa | `qa/FN-807/hover-toolbar-e2e` | Backlog (unblocked) |

Story merge target: `frontend/FN-787/hover-toolbar`

## Implementation Summary

### FN-804 (subtask)

Added `HoverPopoverComponent` in `frontend/src/app/shared/components/hover-popover/` and declared/exported it from `SharedModule`. Projects trigger content via `<ng-content>` and renders a floating tooltip or rich template popover on hover/focus. Positioning math lives in a pure helper (`calculatePopoverPosition`) so the collision-avoidance AC can be unit-tested without a DOM. 9 position tests + 5 component tests.

### FN-805 (subtask)

**Row-hover toolbar replaces the ⋮ dropdown** in `loads-dashboard.component.html`. The `.row-toolbar` block holds 6 icon buttons — View, Edit, Clone, Map, Dispatch, Delete(if draftable) — right-aligned in the actions column. Default `opacity: 0; pointer-events: none;` flips to fully visible on `tr:hover` and `:focus-within`. 100 ms CSS transition. Copy / Create Return / Approve move off the per-row UI per spec and will live in the detail drawer (FN-788).

**Inline status dropdown** filters to valid next transitions via a `statusTransitions` map. Terminal states (DELIVERED, CANCELLED, TONU) hide the chevron entirely.

**Inline billing dropdown** mirrors the status pattern with its own `billingTransitions` map.

**Inline notes editor** adds a new `notes` column. Click opens a textarea (auto-focused + selected). Esc cancels, Enter commits (preventDefault blocks newline insertion), blur also commits; a `notesSaveInFlight` latch prevents double-save.

### FN-806 (subtask)

**Hover previews via `<app-hover-popover>`** wrap driver / broker / attachments / notes cells. All use `[showDelay]="200"` per the AC.

**Lazy fetch with caching.** `prefetchLoadPreview(load)` fires on the trigger's `mouseenter` (and `focusin` for keyboard users) and starts the `LoadDetail` GET in parallel with the 200 ms show delay — data usually lands before the card fades in. Results are cached in `Map<loadId, LoadDetail | null>`; a second `Map<brokerId, BrokerOption | null>` holds the chained `getBrokers(search, 50)` lookup that enriches the broker card with `payment_rating` + `credit_score`.

**Single scalar `previewLoadId`** identifies the currently hovered row. Three `ng-template` blocks live at the top of the component template — `#driverPreviewTpl`, `#brokerPreviewTpl`, `#attachmentsPreviewTpl` — and read from getters so they're shared across all rows with no per-row duplication.

**Graceful degradation.** Driver preview shows "Unavailable" when `driver_position_city/state` are null and an explicit "HOS data unavailable" line (no FE HOS endpoint exists today). Broker preview falls back to "Broker details unavailable" when the chained fetch misses. Attachments popover transitions through "Loading…" → list / "No attachments" without flicker.

**Notes tooltip** uses the component's simple `[text]` mode — the same `<app-hover-popover>` wraps the `.notes-inline__trigger` button and shows the full note with no extra fetch.

**Styles at SCSS root.** The `.hover-preview*` card styles are declared outside `.loads-dashboard` so they apply to templates projected through the popover's `[ngTemplateOutlet]` regardless of where the popover div ends up under Angular's emulated view encapsulation.

## Files Changed

### FN-804 (subtask)
- `frontend/src/app/shared/components/hover-popover/` (new: `.ts`, `.html`, `.scss`, `.spec.ts`, position helper)
- `frontend/src/app/shared/shared.module.ts` — declare + export `HoverPopoverComponent`
- `frontend/tsconfig.app.json` — exclude the new spec from the app build

### FN-805 (subtask)
- `frontend/src/app/components/loads-dashboard/loads-dashboard.component.{ts,html,scss}` — transition maps, inline handlers, `columnDefs` gains `notes`, CSS toolbar + textarea

### FN-806 (subtask)
- `frontend/src/app/components/loads-dashboard/loads-dashboard.component.ts` — `loadPreviewCache`, `brokerPreviewCache`, `previewLoadId`, `prefetchLoadPreview`, `prefetchBroker`, `previewLoad`/`previewBroker` getters, `getPreviewDriverPosition`, `getBrokerCreditLabel`, `getPreviewAttachments`, `getPreviewAttachmentOverflow`
- `frontend/src/app/components/loads-dashboard/loads-dashboard.component.html` — 3 preview templates at the top; driver/broker/attachments/notes cells wrapped in `<app-hover-popover>` with `[showDelay]="200"`
- `frontend/src/app/components/loads-dashboard/loads-dashboard.component.scss` — `.preview-cell`, `.preview-trigger`, and the `.hover-preview*` card styles at SCSS root

## Key Decisions

### FN-805
- Scoped removal of Copy / Return / Approve from the per-row UI per spec's 6-icon requirement. Those actions remain callable from the detail drawer (FN-788) and bulk-actions toolbar.
- Transition maps over a reusable state-machine service — two tables are clearer than an abstraction at this scope.

### FN-806
- **Lazy LoadDetail fetch instead of extending `LoadListItem`.** The list endpoint doesn't carry driver/broker IDs, so hover is the right trigger for the extra round-trip. Cached so repeat hovers cost nothing.
- **Single `previewLoadId` scalar + shared templates** — avoids template duplication inside the `*cdkVirtualFor` loop. Safe because only one popover is mounted at a time.
- **200 ms show delay matches AC.** Stacks fine with the `getLoad` 15 s timeout — even if the API hangs, the popover still renders the "Loading…" footnote.
- **Broker credit is best-effort.** `getBrokers(search, 50)` typically contains the matching broker, but a miss falls back to "Broker details unavailable" rather than failing the whole preview.
- **HOS is explicitly stubbed as "Data unavailable".** FE has no HOS endpoint yet (only a `hos.view` permission). The placeholder keeps the card layout stable so a future ticket can drop real data in without re-layout.

## Deployment Handoff

- **Services to redeploy:** `goodmen-frontend` (static Angular app). No backend/DB/env changes.
- **Migrations:** none.
- **Env/config:** none.
- **Feature flags:** none.
- **Cache invalidation:** none (client bundle rebuild only).
- **Post-deploy verification:**
  1. Hover a loads row → right-aligned toolbar fades in within 100 ms (FN-805).
  2. Click the status pill on a `NEW` load → dropdown shows only `Dispatched`, `Canceled` (FN-805).
  3. Hover the driver name → after ~200 ms, a card appears with the driver name, "Position: City, ST" (or "Unavailable"), and "HOS: Data unavailable" (FN-806).
  4. Hover the broker name → card appears with payment terms + credit score, or "Broker details unavailable" (FN-806).
  5. Hover the attachments chips → popover lists file names with "+N more" overflow (FN-806).
  6. Hover a truncated notes cell → full text appears in the tooltip (FN-806).
- **Performance note:** hovering N unique rows issues N `GET /loads/:id` + N `GET /brokers` requests (one each, cached). If this becomes a pressure point at scale, a follow-up can batch-enrich the list endpoint or add a lightweight preview endpoint.

## Review Findings
_To be filled_

## QA Findings
_To be filled_

## Open Items
- Karma/Jasmine test runner is still not wired in the frontend project (see FN-804 note).
- FE HOS endpoint does not exist yet — FN-806 stubs the HOS line as "Data unavailable". Follow-up ticket should surface real HOS data once backend lands.
- Copy Load, Create Return Load, and Approve Draft move from the per-row menu to the detail drawer — FN-788 owns that placement.
