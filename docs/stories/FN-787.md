# FN-787: Phase 2 — Hover Toolbar, Inline Editing & Hover Previews

## Requirement
Replace the per-row ⋮ dropdown with a fade-in hover toolbar. Inline-edit status, billing, and notes from the row. Rich hover previews for notes/attachments/driver/broker cells.

## Acceptance Criteria
See Jira FN-787. Highlights:
- [ ] Hover toolbar fades in <100ms with View/Edit/Clone/Map/Dispatch/Delete
- [ ] Inline status + billing + notes edits persist without modal
- [ ] Hover previews render driver position/HOS, broker terms/credit, attachment thumbnails, full notes
- [ ] Row click opens detail drawer (stubbed until FN-788 lands)

## Dependencies
None (row-click wiring stubs until FN-788 ships). Blocks **FN-791** and **FN-792**. Internal order: FN-804 → FN-805, FN-806.

## Agent
frontend

## Subtasks
| Key | Summary | Agent | Branch | Status |
|-----|---------|-------|--------|--------|
| FN-804 | `hover-popover` reusable component | frontend | `frontend/FN-804/hover-popover` | Done |
| FN-805 | Hover toolbar + inline status/billing/notes edits | frontend | `frontend/FN-805/hover-toolbar-inline-edits` | Backlog (unblocked) |
| FN-806 | Hover previews (notes/attachments/driver/broker) | frontend | `frontend/FN-806/hover-previews` | Backlog (unblocked) |
| FN-807 | QA validation | qa | `qa/FN-807/hover-toolbar-e2e` | Backlog (blocked by FN-805/806) |

Story merge target: `frontend/FN-787/hover-toolbar`

## Implementation Summary

### FN-804 (subtask)

Added `HoverPopoverComponent` in `frontend/src/app/shared/components/hover-popover/` and declared/exported it from `SharedModule`. The component projects trigger content via `<ng-content>` and renders a floating tooltip or rich template popover on hover/focus.

**Positioning (AC: renders consistently across viewport edges)** — The collision-avoidance math lives in a pure helper, `calculatePopoverPosition()`. It picks the best of four placements (bottom / top / right / left), falls back to the side with the most free space when no side fits, then clamps the final rect inside the viewport minus a configurable margin. This keeps the popover glued to an edge when the trigger is at a corner rather than clipping offscreen.

**Fade animation** — `mounted` and `visible` are tracked separately so the element renders at `opacity: 0`, we measure it and reposition on the next `requestAnimationFrame`, then flip `--visible` to fade in. Matches the spec's 100 ms fade-in/fade-out via CSS transitions.

**Dual content modes (AC: text-only and rich)** — `[text]` input renders a single-line tooltip body; `[content]` takes a `TemplateRef<unknown>` for multi-line / structured markup (used by FN-806 for broker/driver cards).

**Hover intent** — `showDelay` defaults to 100 ms; moving from trigger into the popover cancels the pending hide so users can interact with the body.

**Unit tests (AC: position calculation)** — `hover-popover.component.spec.ts` covers the position function with 9 cases (prefer auto, flip, clamp at each edge, degenerate case where popover is taller than viewport) plus component show/hide and rich-content projection via TestBed. Note: the repo does not currently have a karma/jasmine runner wired (`ng test` has no test architect in `angular.json` and `jasmine-core`/`karma` are not declared as devDependencies). The spec type-checks cleanly with `@types/jasmine` and will execute once the runner is wired — see "Deployment Handoff" for the follow-up.

## Files Changed

### FN-804 (subtask)
- `frontend/src/app/shared/components/hover-popover/hover-popover-position.ts` (new) — pure position helpers
- `frontend/src/app/shared/components/hover-popover/hover-popover.component.ts` (new)
- `frontend/src/app/shared/components/hover-popover/hover-popover.component.html` (new)
- `frontend/src/app/shared/components/hover-popover/hover-popover.component.scss` (new)
- `frontend/src/app/shared/components/hover-popover/hover-popover.component.spec.ts` (new)
- `frontend/src/app/shared/shared.module.ts` — declare + export `HoverPopoverComponent`
- `frontend/tsconfig.app.json` — exclude the new spec from the app build (matches pattern for the other three spec files)

## Key Decisions

- **Extracted `calculatePopoverPosition` as a pure function** so the AC-required "unit test covers position calculation" can run without a DOM. The component just plugs DOM rects into it.
- **Used `position: fixed` + viewport coords** rather than CDK Overlay. Overlay is heavier (portal, backdrop, attached strategies) than we need for a hover tooltip, and a custom implementation is what the AC asks to unit-test.
- **Projection API** — kept `<ng-content>` for the trigger (most common case) and `[content]` input for rich popovers. FN-806 will import templates via `[content]` for broker/driver cards.

## Deployment Handoff

- **Services to redeploy:** `goodmen-frontend` (static Angular app) after FN-787 merges. No backend or DB impact from FN-804 alone.
- **Migrations:** none.
- **Env/config:** none.
- **Feature flags:** none.
- **Cache invalidation:** none (client bundle rebuild only).
- **Post-deploy verification:** once FN-805/FN-806 consume the component, spot-check broker / driver / notes / attachments cells in loads dashboard and watch for clipping near viewport edges. FN-804 alone ships no user-visible change (component is registered but not yet wired to any cells).
- **Follow-up (not in FN-804 scope):** the repo is missing karma/jasmine wiring (`angular.json` has no `test` architect; no `karma.conf` or `tsconfig.spec.json`; `jasmine-core`/`karma` not in devDependencies). Four `*.spec.ts` files exist already. Wiring the runner is a cross-cutting concern worth its own ticket so this spec (and the existing ones) can run in CI.

## Review Findings
_To be filled_

## QA Findings
_To be filled_

## Open Items
- Karma/Jasmine test runner is not wired in the frontend project — tracked as separate follow-up (see Deployment Handoff).
