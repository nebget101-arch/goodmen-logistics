# FN-787: Phase 2 — Hover Toolbar, Inline Editing & Hover Previews

## Requirement
Replace the per-row ⋮ dropdown with a fade-in hover toolbar. Inline-edit status, billing, and notes from the row. Rich hover previews for notes/attachments/driver/broker cells.

## Acceptance Criteria
See Jira FN-787. Highlights:
- [x] Hover toolbar fades in <100ms with View/Edit/Clone/Map/Dispatch/Delete
- [x] Inline status + billing + notes edits persist without modal
- [x] Hover previews render driver position/HOS, broker terms/credit, attachment thumbnails, full notes
- [x] Row click opens detail drawer (stubbed until FN-788 lands)

## Dependencies
None (row-click wiring stubs until FN-788 ships). Blocks **FN-791** and **FN-792**. Internal order: FN-804 → FN-805, FN-806.

## Agent
frontend

## Subtasks
| Key | Summary | Agent | Branch | Status |
|-----|---------|-------|--------|--------|
| FN-804 | `hover-popover` reusable component | frontend | `frontend/FN-804/hover-popover` | Done |
| FN-805 | Hover toolbar + inline status/billing/notes edits | frontend | `frontend/FN-805/hover-toolbar-inline-edits` | Done |
| FN-806 | Hover previews (notes/attachments/driver/broker) | frontend | `frontend/FN-806/hover-previews` | Done |
| FN-807 | QA validation | qa | `qa/FN-807/hover-toolbar-e2e` | Done |

Story merge target: `frontend/FN-787/hover-toolbar`

## Implementation Summary

### FN-804 (subtask)

Added `HoverPopoverComponent` in `frontend/src/app/shared/components/hover-popover/` and declared/exported it from `SharedModule`. The component projects trigger content via `<ng-content>` and renders a floating tooltip or rich template popover on hover/focus.

**Positioning (AC: renders consistently across viewport edges)** — The collision-avoidance math lives in a pure helper, `calculatePopoverPosition()`. It picks the best of four placements (bottom / top / right / left), falls back to the side with the most free space when no side fits, then clamps the final rect inside the viewport minus a configurable margin. This keeps the popover glued to an edge when the trigger is at a corner rather than clipping offscreen.

**Fade animation** — `mounted` and `visible` are tracked separately so the element renders at `opacity: 0`, we measure it and reposition on the next `requestAnimationFrame`, then flip `--visible` to fade in. Matches the spec's 100 ms fade-in/fade-out via CSS transitions.

**Dual content modes (AC: text-only and rich)** — `[text]` input renders a single-line tooltip body; `[content]` takes a `TemplateRef<unknown>` for multi-line / structured markup (used by FN-806 for broker/driver cards).

**Hover intent** — `showDelay` defaults to 100 ms; moving from trigger into the popover cancels the pending hide so users can interact with the body.

**Unit tests (AC: position calculation)** — `hover-popover.component.spec.ts` covers the position function with 9 cases (prefer auto, flip, clamp at each edge, degenerate case where popover is taller than viewport) plus component show/hide and rich-content projection via TestBed. Note: the repo does not currently have a karma/jasmine runner wired (`ng test` has no test architect in `angular.json` and `jasmine-core`/`karma` are not declared as devDependencies). The spec type-checks cleanly with `@types/jasmine` and will execute once the runner is wired — see "Deployment Handoff" for the follow-up.

### FN-805 (subtask)

**Row-hover toolbar replaces the ⋮ dropdown** (`loads-dashboard.component.html`). The `.row-toolbar` block holds 6 icon buttons — View, Edit, Clone, Map, Dispatch, Delete(if draftable) — right-aligned in the actions column. Default `opacity: 0; pointer-events: none;` flips to fully visible on `tr:hover` and on `:focus-within` (keyboard discoverability). 100 ms CSS transition. The old `actions-dropdown` menu (Edit / Copy / Clone / Create Return / Map / Delete / Approve) is removed; Copy / Create Return / Approve move out of the per-row UI per spec and will live in the detail drawer (FN-788).

**Inline status dropdown filters to valid next transitions only** (`loads-dashboard.component.ts`). A `statusTransitions` map keyed on current status returns allowed next states. The happy path is DRAFT → NEW → DISPATCHED → IN_TRANSIT → DELIVERED; terminal states (DELIVERED, CANCELLED, TONU) return an empty array so the chevron disappears and the menu never opens. `getValidStatusTransitions(load)` is called from both the `*ngIf` chevron guard and the `*ngFor` that renders options.

**Inline billing dropdown** mirrors the status pattern: `billingMenuLoadId`, `openBillingMenu`, `changeLoadBilling`, `billingTransitions` map, `getValidBillingTransitions`. Optimistic UI update + `LoadsService.updateLoad({ billingStatus })` PUT; rollback on error with a 4 s error toast.

**Inline notes editor** adds a new `notes` column (fully integrated into `columnDefs`, both colgroups, header row, filter row — filter cell is intentionally blank). Non-edit mode renders a button showing the note (or an italic `—` when empty) with hover border to signal interactivity. Click opens a textarea, which is auto-focused + selected in the next tick (`setTimeout(0)` + `querySelector` — only one editor is ever open at a time). Key handlers: `Esc` cancels, `Enter` commits (not `Shift+Enter` — default behaviour preserved for newlines if the user wants them via drag-resize… but Enter preventDefault stops line-break insertion when committing). `blur` also commits. A `notesSaveInFlight` latch prevents Enter + blur from double-saving.

**Outside-click handling** — the existing `@HostListener('document:click')` now also clears `billingMenuLoadId`, `editingNotesLoadId`, and `editingNotesDraft`.

## Files Changed

### FN-804 (subtask)
- `frontend/src/app/shared/components/hover-popover/hover-popover-position.ts` (new) — pure position helpers
- `frontend/src/app/shared/components/hover-popover/hover-popover.component.ts` (new)
- `frontend/src/app/shared/components/hover-popover/hover-popover.component.html` (new)
- `frontend/src/app/shared/components/hover-popover/hover-popover.component.scss` (new)
- `frontend/src/app/shared/components/hover-popover/hover-popover.component.spec.ts` (new)
- `frontend/src/app/shared/shared.module.ts` — declare + export `HoverPopoverComponent`
- `frontend/tsconfig.app.json` — exclude the new spec from the app build (matches pattern for the other three spec files)

### FN-805 (subtask)
- `frontend/src/app/components/loads-dashboard/loads-dashboard.component.ts` — state fields, `statusTransitions` / `billingTransitions` maps, handlers (`openBillingMenu`, `changeLoadBilling`, `openNotesEditor`, `cancelNotesEdit`, `saveNotesEdit`), `columnDefs` gains `notes`, `onDocumentClick` clears new menus
- `frontend/src/app/components/loads-dashboard/loads-dashboard.component.html` — remove ⋮ menu, add hover toolbar, inline billing dropdown, notes column (colgroups + header + filter row + body cell with textarea/trigger)
- `frontend/src/app/components/loads-dashboard/loads-dashboard.component.scss` — `.row-toolbar`, `.row-toolbar__btn`, `.row-toolbar__btn--danger`, `.billing-cell`, `.notes-cell`, `.notes-inline__trigger`, `.notes-inline__textarea`; status chevron is now conditional so terminal states render a non-interactive pill

## Key Decisions

### FN-804
- **Extracted `calculatePopoverPosition` as a pure function** so the AC-required "unit test covers position calculation" can run without a DOM. The component just plugs DOM rects into it.
- **Used `position: fixed` + viewport coords** rather than CDK Overlay. Overlay is heavier (portal, backdrop, attached strategies) than we need for a hover tooltip, and a custom implementation is what the AC asks to unit-test.
- **Projection API** — kept `<ng-content>` for the trigger (most common case) and `[content]` input for rich popovers. FN-806 imports templates via `[content]` for broker/driver cards.

### FN-805
- **Scoped removal of Copy / Return / Approve from the per-row UI.** The spec lists exactly 6 icons (View, Edit, Clone, Map, Dispatch, Delete). Surfacing the rest would defeat the polish the hover toolbar is meant to deliver. Those actions stay accessible from the detail drawer (FN-788) and bulk actions toolbar where applicable.
- **`Enter` preventDefault on the notes textarea.** Commits on Enter match the AC. Users needing a literal newline can still add one (the save path normalizes empty strings to `null`, but multi-line content is preserved round-trip since we call `trim()` only for change detection, not for the PUT payload).
- **Transition maps over a reusable state machine.** Two tables (`statusTransitions`, `billingTransitions`) are clearer than a generic transition service for this scope. If a third status-bearing entity needs the same pattern later, extract.
- **New `notes` column defaults to visible.** Users toggle via the existing FN-767 column picker; the column's `visibleColumns` entry falls through the `loadUserPreferences` guard (only explicit `false` values hide) so existing saved preferences pick it up as visible with zero migration.
- **Native `tr:hover` + `:focus-within` instead of JS.** CSS-only fade keeps the change compact and hardware-accelerated. The 100 ms transition matches FN-804's hover-popover timing for visual consistency.

## Deployment Handoff

- **Services to redeploy:** `goodmen-frontend` (static Angular app) after FN-787 merges. No backend/DB/env changes.
- **Migrations:** none.
- **Env/config:** none.
- **Feature flags:** none.
- **Cache invalidation:** none (client bundle rebuild only).
- **Post-deploy verification:**
  1. Hover a loads-table row → right-aligned toolbar fades in within 100 ms with View/Edit/Clone/Map/Dispatch/Delete.
  2. Click the status pill on a `NEW` load → dropdown shows only `Dispatched`, `Canceled`. Click `Dispatched` → pill flips optimistically and PUT `/loads/:id` fires.
  3. Click the billing pill on a `PENDING` load → shows `BOL received`, `Canceled` only. Select one → same flow.
  4. Enable the Notes column from the column picker (if hidden) → click an empty cell → inline textarea opens, auto-focused. Type, press Enter → saves. Reopen, type, press Esc → discarded. Type, click outside → saves (blur path).
  5. Load the page under a read-only role → toolbar Clone button is hidden via `*appHasPermission="'loads.create'"`.
  6. Spot-check broker / driver / notes / attachments cells in loads dashboard and watch for clipping near viewport edges.
- **Follow-up (not in FN-787 scope):** the repo is missing karma/jasmine wiring (`angular.json` has no `test` architect; no `karma.conf` or `tsconfig.spec.json`; `jasmine-core`/`karma` not in devDependencies). Four `*.spec.ts` files exist already. Wiring the runner is a cross-cutting concern worth its own ticket so these specs can run in CI.

## Review Findings
_To be filled_

## QA Findings
_To be filled_

## Open Items
- Karma/Jasmine test runner still not wired in the frontend project — tracked as separate follow-up (see Deployment Handoff). FN-805 ships no new unit tests because the scope is a DOM-heavy integration; QA validation subtask FN-807 covers the behaviour via Cypress.
- Copy Load, Create Return Load, and Approve Draft move from the per-row menu to the detail drawer; FN-788 owns that placement.
