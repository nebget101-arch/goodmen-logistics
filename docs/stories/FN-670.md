# FN-670: Set Up Swagger on AI Service & Document Integrations Gaps (11+ endpoints)

## Requirement
Set up Swagger/OpenAPI documentation on the AI Service (which had no Swagger setup) and document the remaining undocumented endpoints in the Integrations Service's scan-bridge.js.

## Acceptance Criteria
- [x] `swagger-jsdoc` and `swagger-ui-express` installed in AI Service
- [x] Swagger setup added to `ai-service/server.js` with `/api-docs` endpoint
- [x] All 12 AI endpoints documented with request/response schemas
- [x] AI endpoints document: expected prompt format, response structure, model used, token limits
- [ ] Visit `http://localhost:4100/api-docs` → verify all AI endpoints visible
- [x] All 4 remaining `scan-bridge.js` endpoints documented
- [x] Integrations service shows 100% coverage at `/api-docs`

## Agent
backend

## Implementation Summary

### AI Service — Full Swagger Setup
- Installed `swagger-jsdoc@^6.2.8` and `swagger-ui-express@^5.0.0`
- Configured Swagger in `server.js` with `/api-docs` endpoint scanning `ai-router.js`
- Documented all **12 endpoints** (more than the 5 originally listed — service grew):

| Endpoint | Model | SDK | Description |
|---|---|---|---|
| POST /api/ai/chat | gpt-4.1-mini | OpenAI | Conversational AI assistant |
| POST /api/ai/work-order/triage | gpt-4.1-mini | OpenAI | Work order task/parts/priority suggestions |
| POST /api/ai/inventory/recommendations | gpt-4.1-mini | OpenAI | Reorder suggestions and anomaly detection |
| POST /api/ai/parts/analysis | gpt-4.1-mini | OpenAI | Parts catalog analysis |
| POST /api/ai/shop-clients/analysis | gpt-4.1-mini | OpenAI | Customer activity analysis |
| POST /api/ai/fuel/preprocess | gpt-4.1-mini | OpenAI | Fuel CSV column mapping |
| POST /api/ai/tolls/csv-normalize | gpt-4.1-mini | OpenAI | Toll CSV normalization |
| POST /api/ai/tolls/invoice-vision | claude-sonnet-4 | Anthropic | Toll invoice image extraction |
| POST /api/ai/safety/mvr-vision | gpt-4.1-mini | OpenAI | MVR/driving record extraction |
| POST /api/ai/fmcsa/match-driver | claude-sonnet-4 | Anthropic | FMCSA driver name matching |
| POST /api/ai/drivers/psp-vision | claude-sonnet-4 | Anthropic | PSP report extraction |
| POST /api/ai/settlements/insights | gpt-4.1-mini | OpenAI | Settlement insights |

### Scan-Bridge (Integrations Service) — 4 Endpoints Documented
- POST /api/scan-bridge/decode-image — Barcode photo decode
- GET /api/scan-bridge/session/{id}/events — SSE event stream
- POST /api/scan-bridge/session/{id}/scan — Push barcode to session
- GET /api/scan-bridge/mobile — Mobile scanner HTML page

## Files Changed
- `backend/microservices/ai-service/package.json` (added swagger deps)
- `backend/microservices/ai-service/server.js` (Swagger setup + /api-docs)
- `backend/microservices/ai-service/src/ai-router.js` (12 @openapi blocks)
- `backend/packages/goodmen-shared/routes/scan-bridge.js` (4 @openapi blocks)

## Key Decisions
- Documented all 12 AI endpoints (not just the 5 originally listed) since the service had grown
- Each AI endpoint documents the specific model, SDK (OpenAI vs Anthropic), token limits, and temperature
- Scan-bridge endpoints document SSE protocol and multipart upload format

## Deployment Handoff
- **Services to redeploy**: `fleetneuron-ai-service`, `fleetneuron-integrations-service`
- **DB migrations**: None
- **Environment/config changes**: None (swagger deps are new but have no env vars)
- **Post-deploy verification**:
  - AI Service: Visit `/api-docs` on ai-service URL → verify 12 AI endpoints + health
  - Integrations: Visit `/api-docs` → verify scan-bridge endpoints are now documented
- **Note**: Run `npm install` in ai-service directory after deploy to install new swagger dependencies
