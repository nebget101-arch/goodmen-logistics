# FN-383: Approve draft API fails — missing pickup_date/delivery_date/location columns

## Requirement
The `loads` table is missing 4 columns (`pickup_date`, `delivery_date`, `pickup_location`, `delivery_location`) that are referenced in 5 code paths in loads.js. This causes 500 errors on approve-draft, create, update, bulk upload, and list endpoints.

## Acceptance Criteria
- [x] Migration adds `pickup_location`, `delivery_location`, `pickup_date`, `delivery_date` to loads table
- [x] Migration is idempotent (uses hasColumn checks)
- [ ] Approve-draft endpoint returns 200 and saves form data + transitions status
- [ ] POST create load still works
- [ ] PUT update load still works
- [ ] Bulk rate con upload still works
- [ ] GET list loads with date filters still works

## Agent
database

## Implementation Summary
Created Knex migration `20260328000000_add_loads_date_location_columns.js` that adds the 4 missing columns to the `loads` table with `hasColumn` idempotency checks. Down migration drops columns individually with existence checks.

## Files Changed
- `backend/packages/goodmen-database/migrations/20260328000000_add_loads_date_location_columns.js` (NEW)

## Key Decisions
- Used individual `hasColumn` checks per column (matching existing migration patterns) for idempotency
- Columns are nullable since existing rows won't have data
- Down migration drops each column individually with existence check

## Deployment Handoff
- **Services to restart**: All backend services (migration runs via goodmen-database)
- **DB migrations**: `20260328000000_add_loads_date_location_columns.js` — must run before deploy
- **Env/config changes**: None
- **Deploy order**: Run migration first, then deploy backend services
- **Post-deploy verification**: Call `PATCH /api/loads/:id/approve-draft` on a draft load — should return 200
