# FN-383: Approve draft API fails — missing loads table columns

## Requirement
The `loads` table is missing `pickup_date`, `delivery_date`, `pickup_location`, and `delivery_location` columns that the application code references in 5 endpoints. This causes a 500 error on approve-draft and would also break manual create, edit, and bulk upload.

## Acceptance Criteria
- [x] Migration adds `pickup_location`, `delivery_location`, `pickup_date`, `delivery_date` to loads table
- [x] Migration is idempotent (uses IF NOT EXISTS pattern)
- [ ] Approve-draft endpoint returns 200 and saves form data + transitions status
- [ ] POST create load still works (manual load creation)
- [ ] PUT update load still works (editing existing loads)
- [ ] Bulk rate con upload still works
- [ ] GET list loads with date filters still works

## Agent
database

## Implementation Summary
Added Knex migration `20260328100000_add_loads_date_location_columns.js` that adds the 4 missing columns to the `loads` table using the idempotent `addIfMissing` pattern (checks `hasColumn` before adding).

## Files Changed
- `backend/packages/goodmen-database/migrations/20260328100000_add_loads_date_location_columns.js` (new)

## Key Decisions
- Used the same idempotent pattern as the initial loads migration for safety
- Column types match what the code expects: `date` for dates, `text` for locations
- All columns are nullable (no defaults) since existing rows won't have values

## Deployment Handoff
- **Services to restart**: `fleetneuron-logistics-service-dev` (runs migrations on startup)
- **Migrations**: `20260328100000_add_loads_date_location_columns.js`
- **Env/config changes**: None
- **Post-deploy verification**: Call `PATCH /api/loads/:id/approve-draft` on a draft load — should return 200

## Open Items
- None
