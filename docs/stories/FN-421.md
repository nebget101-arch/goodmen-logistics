# FN-421: Build Exception Queue Resolution UI & Backend

## Overview
Replace the placeholder Exceptions tab with a full data grid and resolution dialog.

## Subtasks

| Key | Summary | Agent | Branch | Status |
|-----|---------|-------|--------|--------|
| FN-445 | FE: Build Exception Queue data grid with resolution dialog | frontend | `agent/frontend/FN-445/exception-queue-ui` | Done |
| FN-446 | BE: Add PATCH /exceptions/:id resolution endpoint | backend | — | Selected for Dev |
| FN-447 | QA: Validate exception queue resolution | qa | — | Backlog |

### FN-445 (subtask) — Exception Queue UI

**Changes:**
- Added `TollException` interface to `tolls.model.ts`
- Added `getExceptions()` and `resolveException()` methods to `tolls.service.ts`
- Replaced placeholder with full data grid: Date, Provider, Plaza, Amount, Truck, Driver, Type, Status, Actions
- Status filter dropdown (All, Open, Resolved, Ignored) — defaults to Open
- Pagination (50/page)
- "Resolve" button on open exceptions opens resolution dialog with exception details, notes textarea, Resolve/Discard buttons
- Color-coded status badges (open=amber, resolved=green, ignored=gray)
- Success/error toast messages, grid refresh after resolution
- Added `FormsModule` to `TollsModule`

**Files Changed:**
- `frontend/src/app/tolls/tolls-exceptions/tolls-exceptions.component.ts`
- `frontend/src/app/tolls/tolls-exceptions/tolls-exceptions.component.html`
- `frontend/src/app/tolls/tolls-exceptions/tolls-exceptions.component.css`
- `frontend/src/app/tolls/tolls.model.ts` — added `TollException`
- `frontend/src/app/tolls/tolls.service.ts` — added `getExceptions()`, `resolveException()`
- `frontend/src/app/tolls/tolls.module.ts` — added `FormsModule`

## Deployment Handoff

### Services to redeploy
- `fleet-neuron-frontend`

### DB migrations
- None

### Environment/config changes
- None

### Post-deploy verification
1. Navigate to Tolls > Exceptions tab
2. Verify grid loads open exceptions (or empty state)
3. Click "Resolve" on an exception → verify dialog shows details
4. Submit resolution → verify toast and grid refresh
