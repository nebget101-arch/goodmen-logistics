# FN-1327: Role-based default layouts (Owner / Dispatcher / Compliance presets)

## Requirement
Ship role-based default layouts as presets layered on top of the existing per-user drag-to-reorder system (FN-1130). New users (or "Reset to default" users) see a layout tuned to their role rather than the single owner default.

## Acceptance Criteria
- [ ] New table `dashboard_layout_presets` stores admin-editable preset configurations.
- [ ] `GET /api/users/me/dashboard-layout` returns the role preset when no per-user override exists, sourced from `dashboard_layout_presets`.
- [ ] Three new shipped presets seeded at migration time: Owner, Dispatcher, Compliance.
- [ ] Existing four role keys (dispatcher / safety / maintenance / owner) continue to work; new presets supplement, not replace.
- [ ] Reset-to-default returns the role-correct preset.
- [ ] Settings UI lets users preview and switch between presets before drag-reordering.

## Dependencies
- Built on top of FN-1130 / FN-1171 / FN-1172 / FN-1174 (per-user layouts).

## Agent
frontend (story owner; subtasks span frontend, backend, database, qa)

## Integration Branch
`integration/FN-1327` (created by first subtask agent from `origin/dev`; subtasks branch off this; story PR merges to `dev`)

## Subtasks
| Key | Summary | Agent | Branch | Files Touched | Status |
|-----|---------|-------|--------|---------------|--------|
| FN-1341 | `dashboard_layout_presets` table + seeds | database | `database/FN-1341/layout-presets-schema` | `backend/packages/goodmen-database/migrations/20260505180000_create_dashboard_layout_presets.js`, `backend/packages/goodmen-database/seeds/dashboard_layout_presets.js` | Done |
| FN-1342 | Read presets from new table in dashboard-layout endpoints | backend | `backend/FN-1342/preset-fallback` | `services/layout-store.js`, `services/preset-store.js`, `routes/dashboard-layout.{js,test.js}` | Pending (blocked by FN-1341) |
| FN-1343 | Preset preview + switcher in Control Center settings | frontend | `frontend/FN-1343/preset-switcher` | `components/control-center/preset-switcher/**`, `services/dashboard-layout.service.ts` | Pending (blocked by FN-1342) |
| FN-1344 | Role-based preset validation | qa | `qa/FN-1344/preset-tests` | `tests/cypress/e2e/control-center/role-presets.cy.ts`, `tests/karate/auth-users/dashboard-presets.feature`, `docs/stories/evidence/FN-1327/**` | Pending (blocked by FN-1341 + FN-1342 + FN-1343) |

## Schema Contract
```
dashboard_layout_presets
  preset_key          varchar PK
  role_key            varchar (indexed)
  display_name        varchar
  layout_json         jsonb default '{}'
  is_default_for_role boolean
  created_at          timestamptz default now()
  updated_at          timestamptz default now()
```
Seeded rows: one per role marked `is_default_for_role = true`. Layouts mirror current hard-coded values in `frontend/src/app/components/control-center/role-layouts.ts`.

## API Contract
```
GET /api/users/me/dashboard-layout/presets
→ 200 [{ preset_key, role_key, display_name, layout, is_default_for_role }]

PUT /api/users/me/dashboard-layout/preset
body { preset_key }
→ 200 { layout }
```

## Implementation Summary
_To be filled by implementing agent_

### FN-1341 (subtask)
- Added `dashboard_layout_presets` table via Knex migration in `backend/packages/goodmen-database/migrations/`. Schema matches the Schema Contract above (preset_key PK, role_key indexed, display_name, jsonb layout_json default `'{}'`, is_default_for_role bool, created_at/updated_at timestamptz).
- Migration `up()` seeds the three canonical presets so they exist as soon as the schema is in place: **owner-default** (role `owner`), **dispatcher-default** (role `dispatcher`), **compliance-default** (role `safety`). Layouts mirror `auth-users-service/services/layout-store.js` ROLE_DEFAULTS so behavior is unchanged until FN-1342 swaps the read path.
- Standalone idempotent seed at `backend/packages/goodmen-database/seeds/dashboard_layout_presets.js` lets the canonical rows be re-applied on edited environments.
- Verified locally on a scratch Postgres 15 DB: forward migration, rollback, re-forward, and double seed:run all clean; row count stays at 3.

## Files Changed
_To be filled by implementing agent_

### FN-1341 (subtask)
- `backend/packages/goodmen-database/migrations/20260505180000_create_dashboard_layout_presets.js` (new)
- `backend/packages/goodmen-database/seeds/dashboard_layout_presets.js` (new)
- `docs/stories/FN-1327.md` (subtask notes + Files Touched correction)

## Key Decisions
_To be filled by implementing agent_

### FN-1341 (subtask)
- **Migration location**: ticket suggested `backend/microservices/auth-users-service/migrations/`, but the actual codebase keeps all shared-DB migrations under `backend/packages/goodmen-database/migrations/` (sibling of `knexfile.js`). `auth-users-service` has no migrations directory. Used the real location.
- **`compliance` → role_key `safety`**: parent story notes "the existing four role keys (dispatcher / safety / maintenance / owner) continue to work; new presets supplement, not replace." `safety` is the established RBAC role key for compliance/HOS work, so the Compliance preset attaches to `role_key='safety'`. UI surfaces it as "Compliance" via `display_name`. No new role key is introduced.
- **Three presets, three default rows**: AC requires "one row per role marked `is_default_for_role = true`" — interpreted as one default per role *that has a preset*, i.e. the three seeded rows are each the default for their role. `maintenance` has no preset yet (out of scope for this subtask); FN-1342 keeps falling back to layout-store's hard-coded `maintenance` default for now.
- **Unique partial index on `(role_key) WHERE is_default_for_role`**: prevents two rows from both being marked default for the same role. Belt-and-suspenders for FN-1342's read path.
- **Seed both at migration time and via a separate seed file**: AC says both. Migration uses `ON CONFLICT (preset_key) DO NOTHING` so it never overwrites edited rows; the separate seed uses `ON CONFLICT … MERGE` to actively reset the canonical three back to defaults when explicitly run.

## Deployment Handoff
_To be filled by implementing agent_

### FN-1341 (subtask)
- **Migration to run**: `backend/packages/goodmen-database/migrations/20260505180000_create_dashboard_layout_presets.js` — runs on next `knex migrate:latest` against the dev DB. No data backfill or downtime; pure additive schema.
- **Services to redeploy**: none for this subtask alone. The new table is unread until FN-1342 lands. FN-1327 PR will redeploy `auth-users-service` once FN-1342 is integrated.
- **Env / config changes**: none.
- **Cache invalidation**: none.
- **Feature flags**: none.
- **Deploy order**: run the migration before deploying FN-1342's `auth-users-service` build that reads the table.
- **Post-deploy verification**: `SELECT preset_key, role_key, display_name, is_default_for_role FROM dashboard_layout_presets ORDER BY preset_key;` should return three rows (owner-default / dispatcher-default / compliance-default), each `is_default_for_role = true`.

## Review Findings
_To be filled by Code Reviewer_

## QA Findings
_To be filled by QA_

## Open Items
- None
