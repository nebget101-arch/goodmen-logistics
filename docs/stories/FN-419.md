# FN-419: AI-Powered Toll Data Normalization for CSV Imports

## Overview
Auto-detect and normalize CSV formats from different toll providers using AI before manual column mapping.

## Subtasks

| Key | Summary | Agent | Branch | Status |
|-----|---------|-------|--------|--------|
| FN-434 | AI: Build Claude API toll CSV normalization handler | ai | `agent/ai/FN-434/csv-normalize-handler` | Done |
| FN-435 | BE: Add POST /import/ai-normalize route and mapping profile storage | backend | — | Backlog |
| FN-436 | FE: Integrate AI normalize step into Import Wizard | frontend | — | Backlog |
| FN-437 | QA: Validate AI CSV normalization with multiple provider formats | qa | — | Backlog |

### FN-434 (subtask) — AI: Toll CSV normalization handler

**Changes:**
- Created `toll-csv-normalize-handler.js` in ai-service handlers
- Accepts CSV headers + sample rows (up to 20), optional provider name
- Claude API prompt extracts: column mapping with confidence scores, detected provider, date/amount format detection, location parsing strategy, normalized sample rows, duplicate detection
- Target fields: transaction_date, amount, provider_name, plaza_name, entry/exit_location, city, state, device_number, plate_number, vehicle_class, external_transaction_id, etc.
- Registered at `POST /tolls/csv-normalize` in ai-router

**Files Changed:**
- `backend/microservices/ai-service/src/handlers/toll-csv-normalize-handler.js` (NEW)
- `backend/microservices/ai-service/src/ai-router.js` — registered new route

## Deployment Handoff

### Services to redeploy
- `ai-service`

### DB migrations
- None

### Environment/config changes
- None

### Post-deploy verification
1. POST to `/api/ai/tolls/csv-normalize` with toll CSV headers + sample rows
2. Verify column mapping response with confidence scores
3. Verify date/amount format detection
4. Verify normalized sample rows returned
