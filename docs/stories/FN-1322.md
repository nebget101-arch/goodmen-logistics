# FN-1322: Action Queue — unified, severity-ranked, grouped alerts feed

## Requirement
Merge Smart Alerts and Compliance Alerts into one severity-ranked, grouped, action-queue feed in the Control Center main column. Group near-identical alerts so 12 "preventive maintenance overdue" rows collapse into one expandable row, and expose bulk actions.

## Acceptance Criteria
- [ ] One unified Action Queue component renders in the Control Center main column; the right-rail Compliance Alerts section is removed (or replaced with narrower "upcoming deadlines" context).
- [ ] Near-identical alerts are grouped ("N vehicles overdue for preventive maintenance — view list").
- [ ] Alerts ranked by severity (Critical → High → Medium → Low) using tokens from FN-1326, then by recency.
- [ ] Each row exposes at least one primary action.
- [ ] Bulk actions: select-all-in-group + acknowledge / dismiss group; persists via existing dismissals table.
- [ ] Empty state replaces the current "No compliance alerts match your filters" string with action-oriented copy.
- [ ] No regression in alert counts vs. legacy feeds.

## Dependencies
- Blocked by FN-1326 (Story E — severity tokens + grouped-alert component).

## Agent
frontend (story owner; subtasks span frontend, backend, qa)

## Integration Branch
`integration/FN-1322` (created by first subtask agent from `origin/dev`; subtasks branch off this; story PR merges to `dev`)

## Subtasks
| Key | Summary | Agent | Branch | Files Touched | Status |
|-----|---------|-------|--------|---------------|--------|
| FN-1329 | Action Queue component + remove legacy feeds + bulk actions | frontend | `frontend/FN-1329/action-queue` | `frontend/src/app/components/dashboard/action-queue/**`, `dashboard.component.html` (right rail removed), `control-center.component.html` (smart-alerts → action-queue widget), `role-layouts.ts`, `api.service.ts`, `backend/.../layout-store.js` | Done |
| FN-1330 | Grouped alerts aggregation endpoint | backend | `backend/FN-1330/action-queue-endpoint` | `backend/gateway/routes/action-queue.js`, `backend/gateway/services/alert-grouper.js`, `backend/gateway/services/compliance-alerts-client.js`, `backend/gateway/index.js` (mount) | Done |
| FN-1331 | Action Queue validation | qa | `qa/FN-1331/action-queue-tests` | `tests/cypress/e2e/control-center/action-queue.cy.ts`, `tests/karate/dashboard/action-queue.feature`, `docs/stories/evidence/FN-1322/**` | Pending (blocked by FN-1329 + FN-1330) |

## API Contract (defined here so frontend + backend can run in parallel)
```
GET /api/dashboard/action-queue?window=today|7d|30d&severity=critical|high|medium|low|all
→ 200 { groups: [...], total, window, severity, generatedAt, upstreamErrors: [...] }

  group shape:
  {
    id: 'compliance:maintenance:pm_overdue' | 'smart:hos_imminent' | ...,
    source: 'smart' | 'compliance',
    severity: 'critical' | 'high' | 'medium' | 'low',
    category: 'driver' | 'vehicle' | 'load' | 'maintenance' | 'compliance',
    message: '3 vehicles — Preventive maintenance overdue',
    count: 3,
    latest_at: '2026-05-05T...',
    targets: [{ id, label, route, raw_alert_id }],
    primary_action: { label, action_id, payload }
  }

POST /api/dashboard/action-queue/dismiss
body { group_id?: string, target_ids?: string[] }
→ 200 { dismissed_count, group_id }
```

## Implementation Summary

### FN-1329 (subtask — frontend)
Built `ActionQueueComponent` (standalone, OnPush) and wired it into the existing **Control Center** widget grid (`ControlCenterComponent`), replacing the `app-smart-alerts` card. Consumes the FN-1330 endpoints via two new `ApiService` methods (`getActionQueue`, `dismissActionQueueGroup`) and renders each group with the `<app-grouped-alert-row>` foundation from FN-1326.

Component lives at `frontend/src/app/components/dashboard/action-queue/` rather than the `control-center/action-queue/` path the Jira description suggested — the existing `control-center/` directory is the draggable widget host (it embeds the cards), and the queue itself is conceptually a dashboard surface that the Control Center consumes. Co-locating with `dashboard/` matches the existing folder conventions; the file is exposed to `ControlCenterComponent` via standalone import.

Layout-store widget id `smart-alerts` was renamed to `action-queue` everywhere (frontend `WidgetId`, role defaults; backend `ROLE_DEFAULTS`). Persisted layouts using the legacy id are auto-migrated on read by `sanitizeLayout`, so no user loses their card order.

Right-rail Compliance Alerts feed in `dashboard.component.html` (lines 191–242 of the legacy HTML) was deleted along with the now-unused `alerts/alertFilterType/alertFilterCategory` state in `dashboard.component.ts`, the `getAlerts()` API method, and the orphan `frontend/src/app/services/smart-alerts.service.ts`. The `ApiService.getActionQueue()` is the single source of truth for alert rendering.

### FN-1330 (subtask — backend)
Implemented the grouped-alerts aggregation endpoint at the **gateway** rather than `backend/microservices/dashboard-stats-service` as originally noted in the Jira description (that service does not exist in this codebase; smart-alerts and the dismissals store already live in `backend/gateway/services/`, so co-locating the action-queue endpoint there reuses the same singletons and avoids a new microservice).

Endpoint pipeline (`backend/gateway/routes/action-queue.js`):
1. `GET /api/dashboard/action-queue` runs the existing `smart-alerts-aggregator` and the new `compliance-alerts-client` (which calls reporting-service `GET /api/dashboard/alerts`) in parallel.
2. The new `alert-grouper` service normalizes both feeds to a common shape, dedupes by `(source, category, message_template)`, applies window + severity filters, and rolls per-target dismissals.
3. `POST /api/dashboard/action-queue/dismiss` writes to the same `dismissals-store` used by `/api/alerts/smart/:id/dismiss`. Dismissing by `group_id` hides the whole rolled-up group for the dismissal TTL; passing `target_ids` hides only those rows.

Mounted on the gateway BEFORE the `/api/dashboard` proxy block so the route resolves locally instead of being forwarded to reporting-service. The shared `smartAlertsAggregator`, `dismissalsStore`, and `alertsBroadcaster` were moved up in `index.js` to be constructed before the proxy block; `/api/alerts/smart` (FN-1161) continues to consume the same singletons.

## Files Changed

### FN-1329 (subtask — frontend)
- `frontend/src/app/components/dashboard/action-queue/action-queue.component.ts` (new)
- `frontend/src/app/components/dashboard/action-queue/action-queue.component.html` (new)
- `frontend/src/app/components/dashboard/action-queue/action-queue.component.scss` (new)
- `frontend/src/app/components/dashboard/action-queue/action-queue.component.spec.ts` (new)
- `frontend/src/app/components/dashboard/dashboard.component.html` (drop right-rail Compliance Alerts; collapse to single column)
- `frontend/src/app/components/dashboard/dashboard.component.ts` (drop alert state + filter helpers)
- `frontend/src/app/components/dashboard/dashboard.component.css` (drop `.panel-alerts/.alert-*/.dashboard-column-narrow` and the 2-column responsive rules)
- `frontend/src/app/components/control-center/control-center.component.ts` (swap `SmartAlertsComponent` → `ActionQueueComponent` import)
- `frontend/src/app/components/control-center/control-center.component.html` (swap `*ngSwitchCase="'smart-alerts'"` → `'action-queue'`)
- `frontend/src/app/components/control-center/control-center.component.spec.ts` (swap stub component, update fixture data, add legacy-id migration assertion)
- `frontend/src/app/components/control-center/role-layouts.ts` (rename `smart-alerts` → `action-queue` in `WidgetId`/`ALL_WIDGET_IDS`/`ROLE_DEFAULT_LAYOUTS`; add `LEGACY_WIDGET_ID_MAP` with `smart-alerts → action-queue` migration in `sanitizeLayout`)
- `frontend/src/app/services/api.service.ts` (drop `getAlerts()`; add `getActionQueue()` and `dismissActionQueueGroup()`)
- `frontend/src/app/components/control-center/smart-alerts/**` (deleted)
- `frontend/src/app/services/smart-alerts.service.ts` + `.spec.ts` (deleted — orphan after smart-alerts component removal)
- `frontend/src/app/app.module.ts` (drop `SmartAlertsComponent` import + standalone-imports entry)
- `backend/microservices/auth-users-service/services/layout-store.js` (rename `smart-alerts` → `action-queue` in `ROLE_DEFAULTS`)
- `backend/microservices/auth-users-service/routes/dashboard-layout.test.js` (update fixture to use `action-queue`)

### FN-1330 (subtask — backend)
- `backend/gateway/services/alert-grouper.js` (new)
- `backend/gateway/services/compliance-alerts-client.js` (new)
- `backend/gateway/routes/action-queue.js` (new)
- `backend/gateway/__tests__/alert-grouper.test.js` (new)
- `backend/gateway/__tests__/action-queue.test.js` (new)
- `backend/gateway/index.js` (mount router; reorder existing aggregator/store construction above the proxy block)

## Key Decisions

### FN-1329 (subtask — frontend)
- **Component lives under `dashboard/`, not `control-center/`.** The Jira description's `control-center/action-queue/` path was aspirational; the existing `control-center/` directory is the role-aware widget grid host (`ControlCenterComponent`), and the action queue is just one widget it renders. Co-locating the new component with `dashboard/` matches existing conventions; `ControlCenterComponent` imports it as a standalone component. The directory name reflects implementation reality, not the conceptual product label "Control Center" (which is the dashboard's H1 heading).
- **No new `<app-action-queue>` rendering on the legacy `<app-dashboard>` route directly.** AC requires "the right-rail Compliance Alerts section is removed" — the action queue surfaces inside `<app-control-center>` (which the dashboard already embeds), so the dashboard column itself is just stripped down to one main column instead of being given its own duplicate queue.
- **Two endpoints are enough; no separate "acknowledge" path.** The backend (FN-1330) only exposes `POST /dismiss`. The AC mentions "acknowledge / dismiss" — implemented as a single "Dismiss selected" bulk action in the expanded body and a "Dismiss group" control on the row. If acknowledgement needs distinct semantics (e.g. snooze without permanent dismissal), that is a follow-up requiring a backend store change.
- **Optimistic local removal on dismiss.** When the dismiss POST resolves, the row (or selected targets) is removed from the local state and `total` decremented client-side without a refetch — the server response is treated as authoritative for success only. Errors restore the busy state and surface a banner; users can press Refresh to resync.
- **Layout-id migration handled client-side (and matched server-side).** Saved layouts that still reference `smart-alerts` are rewritten to `action-queue` by `sanitizeLayout` so existing users keep their card order. Backend `ROLE_DEFAULTS` are also updated so brand-new layout PUTs from the client validate cleanly.
- **`smart-alerts.service.ts` deleted as dead code.** The frontend SmartAlertsService had only one consumer (`SmartAlertsComponent`); after removing the component the service was orphaned. The smart-alerts *backend* aggregator (`backend/gateway/services/smart-alerts-aggregator.js`) is kept — `action-queue` re-uses it server-side via FN-1330's grouper.

### FN-1330 (subtask — backend)
- **Location: gateway, not a new microservice.** The Jira description references `dashboard-stats-service` which does not exist. Smart Alerts already live in the gateway and the dismissals store is per-process; building a new microservice would force cross-process state for dismissals. Co-locating in the gateway keeps the dismissal TTL behavior identical to `/api/alerts/smart`.
- **Group key strategy.** Compliance alerts are templatized by message regex (`pm_overdue`, `medical_cert_expired`, etc.) so per-target detail is stripped without losing the group identity. Smart alerts use `smart:${type}` since the aggregator already classifies them.
- **Group-level dismissals reuse alert dismissals.** The existing `MemoryDismissalsStore` keys by `(tenantId, userId, alertId)` and is content-agnostic. We pass the `group_id` as the `alertId` for group-wide dismissal and `target_ids` for individual rows; both paths share the same TTL and tenant isolation contract.
- **Lenient upstream failures.** Mirroring `smart-alerts-aggregator`, the route returns 200 with whatever upstream succeeded and surfaces errors in `upstreamErrors[]`. A reporting-service outage degrades to "smart-alerts only" rather than 5xx.
- **Severity buckets.** Smart alert numeric scores (0-100) are bucketed `>=80 critical / >=60 high / >=40 medium / <40 low`; compliance string `critical → critical, warning → high, other → medium`. Buckets aligned to the Story E severity tokens (FN-1326) so the frontend can switch on `group.severity` directly.

## Deployment Handoff

### FN-1329 (subtask — frontend)
- **Service to redeploy**: `fleetneuron-logistics-ui` (the Angular frontend). Per `.agent/docs/render_services.md`.
- **Deploy order**: deploy AFTER `fleetneuron-logistics-gateway` (FN-1330) so the new endpoints are available when the bundle is served. If deployed first, the action queue will show its error banner ("Could not load action queue") until the gateway catches up.
- **DB migrations**: none (the `user_dashboard_layouts` and `user_alert_dismissals` tables are already on `dev`).
- **Environment / config changes**: none — uses existing `apiUrl` / proxy config. No new env vars on the frontend service.
- **Cache invalidation**: none server-side. Browsers will pick up the new bundle on next reload via `CacheBustingInterceptor` headers.
- **Feature flags**: none — the new component replaces the old surfaces unconditionally.
- **Persisted-data migration**: any user with `'smart-alerts'` in `user_dashboard_layouts.layout_json` is auto-migrated client-side to `'action-queue'` by `sanitizeLayout` on next read; no DB script required, but on the first save after deploy the `cards` array will rewrite to the new id.
- **Browser smoke test (deferred to QA — FN-1331)**: dev-server smoke validation was not run end-to-end in this worktree because it requires a running gateway and a proxy.conf for it. AOT build (`ng build --configuration development`) succeeds with zero errors; backend `dashboard-layout` route tests pass (8/8). QA should validate in a deployed environment:
  1. Log into `dev`, navigate to `/dashboard`. Right-rail "Compliance alerts" panel is gone; main column shows the metrics grid full-width and the embedded `<app-control-center>` shows the new "Action queue" card in place of the old "Smart alerts" card.
  2. Create at least one critical compliance issue and one smart-alert subject; both surface in the action queue with grouped count, severity badge, and a primary CTA.
  3. Expand a multi-target group, "select all", click "Dismiss selected" → group disappears (POST `/api/dashboard/action-queue/dismiss` with `target_ids[]`).
  4. Click the row-level × → group disappears (POST with `group_id` only).
  5. Reset layout to default; the resulting layout includes `action-queue` (not `smart-alerts`).

### FN-1330 (subtask — backend)
- **Service to redeploy**: `gateway` (Render service `goodmen-api-gateway` per `.agent/docs/render_services.md`). No other services touched.
- **DB migrations to run**: none — `user_alert_dismissals` migration (`20260504191128_create_user_alert_dismissals.js`, FN-1165) is already on `dev`; the in-memory dismissals store is the current binding so no schema change is needed for this subtask.
- **Environment / config changes**: none — uses existing `REPORTING_SERVICE_URL`, `DRIVERS_COMPLIANCE_SERVICE_URL`, `VEHICLES_MAINTENANCE_SERVICE_URL`, `LOGISTICS_SERVICE_URL`, `AI_SERVICE_URL`, `JWT_SECRET`.
- **Cache invalidation**: none.
- **Feature flags**: none.
- **Deploy order**: standalone — gateway can be redeployed independently. Frontend (FN-1329) consumes the new `/api/dashboard/action-queue` endpoint and should deploy after the gateway.
- **Post-deploy verification**:
  1. `curl -H "Authorization: Bearer $TOKEN" https://api.dev.fleetneuron.ai/api/dashboard/action-queue` returns `{groups: [...], total, ...}` with HTTP 200.
  2. Existing `GET /api/dashboard/alerts` (proxied to reporting-service) still works — no path collision.
  3. Existing `GET /api/alerts/smart` (FN-1161) still works — same `dismissalsStore` instance.

## Review Findings
_To be filled by Code Reviewer_

## QA Findings
_To be filled by QA_

## Open Items
- Postgres-backed `DismissalsStore` swap is still pending (currently in-memory). Out of scope for this subtask; tracked under FN-1165 follow-up.
