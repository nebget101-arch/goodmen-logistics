# FN-1382: Vehicle Maintenance History tab — work orders + invoices on vehicle detail

## Requirement
Surface every shop work order and its generated invoice for a given vehicle directly on the vehicle detail drawer, joined via VIN through the `customer_vehicles` mirror table, so dispatch and accounting can see lifetime maintenance spend without leaving the Trucks/Trailers page.

## Acceptance Criteria
- [x] `GET /api/vehicles/:id/maintenance-history` returns paginated WO + invoice rows for fleet AND customer-owned vehicles, joined by VIN.
- [ ] Vehicle detail drawer shows a Maintenance History tab next to Overview.
- [ ] Tab lists WOs with totals and clickable invoice download links.
- [ ] Header strip shows lifetime spend, last-WO date, WO count.
- [x] Invoice column hidden for users without `invoices.view`. _(backend strips the per-row `invoice` field)_
- [ ] Empty state shown for vehicles with zero WOs.
- [ ] No regressions in existing Overview content.

## Dependencies
- **Blocked by** FN-1381 (Form redesign — to avoid simultaneous edits to `vehicles.component.html`).

## Agent
backend, frontend

## Integration Branch
`integration/FN-1382` — created by FN-1389 (backend, runs first). Subtasks branch off it; story PR merges to `dev`.

## Subtasks
| Key | Summary | Agent | Branch | Files Touched | Status |
|-----|---------|-------|--------|---------------|--------|
| FN-1389 | BE — maintenance-history endpoint | backend | `backend/FN-1389/vehicles-maintenance-history-api` | `backend/packages/goodmen-shared/routes/vehicles.js`, `backend/packages/goodmen-shared/services/vehicles-maintenance-history.service.js`, `backend/packages/goodmen-shared/services/vehicles-maintenance-history.test.js` | Done |
| FN-1390 | FE — Maintenance History tab | frontend | `frontend/FN-1390/vehicle-maintenance-history-tab` | `frontend/src/app/components/vehicles/vehicles.component.{ts,html,css}` | Pending — unblocked |
| FN-1391 | QA — manual validation | qa | _manual_ | — | Blocked by FN-1390 |

## Implementation Summary
_To be filled by implementing agent_

### FN-1389 (subtask)
- New endpoint `GET /api/vehicles/:id/maintenance-history` mounted on the vehicles router.
- Resolution flow: tenant-scoped vehicle lookup against `all_vehicles` (or `vehicles`) → VIN → all `customer_vehicles.vehicle_uuid` rows for the same tenant → `work_orders` LEFT JOIN LATERAL latest `invoices` row per WO + `locations` for shop name.
- Response matches the shape documented on FN-1382 (top-level `data[]` + `meta { page, pageSize, total, lifetime_spend }`).
- `lifetime_spend` is `SUM(work_orders.total_amount)` excluding `CANCELED` WOs (canceled spend is not real cost).
- `pageSize` clamped at 100 (default 25); `page` defaults to 1.
- Permissions: `loadUserRbac` + `requirePermission('work_orders.view')` gate access. The per-row `invoice` field is omitted when the caller lacks `invoices.view` (super_admin bypasses both checks).
- Tenant scoping enforced in every step: vehicle lookup, VIN→vehicle_uuid join, WO aggregate, and WO rows query — including the LATERAL invoice subquery. Cross-tenant `:id` returns 404.

## Files Changed
_To be filled by implementing agent_

### FN-1389 (subtask)
- `backend/packages/goodmen-shared/routes/vehicles.js` (+ ~70 lines: imports + new route handler)
- `backend/packages/goodmen-shared/services/vehicles-maintenance-history.service.js` (new)
- `backend/packages/goodmen-shared/services/vehicles-maintenance-history.test.js` (new — 6 unit tests, all passing on Node 20)

## Key Decisions
_To be filled by implementing agent_

### FN-1389 (subtask)
- **Inline RBAC, not router-level.** The vehicles router already runs a role-based `vehiclesRoleGate`; switching the whole router to `loadUserRbac` would change the access surface for every existing endpoint. The maintenance-history route mounts `loadUserRbac` + `requirePermission` as per-route middleware to keep the change scoped.
- **`includeInvoices` is a service-level flag, not a SQL gate.** The query always returns the joined invoice row (cheap LATERAL with index on `invoices.work_order_id`) and the service drops the field when the caller lacks `invoices.view`. This keeps a single SQL path and lets the test stub one shape.
- **`lifetime_spend` excludes CANCELED.** Including canceled WOs would inflate the aggregate; the meta value is intended as actual maintenance cost for accounting.
- **VIN is the join key, not `vehicle_id`.** Matches the schema established by `20260228182000_update_work_orders_vehicle_fk_to_customer_vehicles` — `work_orders.vehicle_id` references `customer_vehicles.vehicle_uuid`, and a fleet `vehicles.id` won't match directly. Resolving via VIN handles both fleet and customer-owned vehicles transparently.

## Deployment Handoff

### FN-1389 (subtask)
- **Service to redeploy**: any Render service that mounts `backend/packages/goodmen-shared/routes/vehicles.js`. Per `.agent/docs/render_services.md`, that's the gateway/api service stack — confirm with DevOps before final deploy. No new microservice.
- **DB migrations**: none. Uses existing `vehicles`, `customer_vehicles`, `work_orders`, `invoices`, `locations` schema.
- **Env / config**: none.
- **Cache invalidation**: none (no cached endpoint paths added).
- **Feature flags**: none.
- **Deploy order**: no ordering constraint with FN-1390 (frontend) — the FE consumes this endpoint, so backend must be live before the FE PR merges. The integration branch sequencing already enforces this (FE rebases on integration after BE merges in).
- **Post-deploy verification**:
  1. Hit `GET /api/vehicles/<known-vehicle-id>/maintenance-history` with a fleet vehicle id and confirm 200 + correct WO list.
  2. Repeat with a customer-owned vehicle id (passing `customer_vehicles.vehicle_uuid` as `:id`) — should also return 200.
  3. Verify a cross-tenant id returns 404 (tenant scoping).
  4. Verify a user without `work_orders.view` gets 403.
  5. Verify a user with `work_orders.view` but without `invoices.view` gets 200 with rows that have no `invoice` key.

## Review Findings
_To be filled by Code Reviewer_

## QA Findings
_To be filled by QA — evidence under `docs/stories/evidence/FN-1382/`_

## Open Items
- Decide whether to also include vehicle's `documents` (Repairs & Maintenance uploads) in the same tab or keep them separate.
- Decide invoice "download" behavior — whether the existing invoicing service supports streaming PDF for a draft invoice.
