# FN-405: Add category, source_transaction_id columns and enforce product_type enum on fuel_transactions

## Requirement
Add schema foundation for fuel product type/category tracking: `category` column (fuel/maintenance/advance), `source_transaction_id` for split rows, CHECK constraints on `product_type` and `category`, indexes, and backfill.

## Acceptance Criteria
- [x] `category` column exists with default 'fuel' and CHECK constraint
- [x] `source_transaction_id` column exists with index
- [x] `product_type` has CHECK constraint for diesel/def/reefer (NULL allowed)
- [x] Existing rows backfilled: product_type='diesel' where NULL
- [x] FUEL_NORMALIZED_FIELDS includes category
- [ ] No regression on existing import, transactions list, or exceptions

## Agent
database

## Implementation Summary
- Created migration `20260328100000_fuel_txn_category_source_columns.js` with idempotent column/constraint/index additions
- Updated `FuelTransaction` interface in `fuel.model.ts` to include `category` and `source_transaction_id`
- Added `category` to `FUEL_NORMALIZED_FIELDS` for column mapping

## Files Changed
- `backend/packages/goodmen-database/migrations/20260328100000_fuel_txn_category_source_columns.js` (new)
- `frontend/src/app/fuel/fuel.model.ts` — interface + FUEL_NORMALIZED_FIELDS update

## Key Decisions
- Used `DO $$ BEGIN ... END $$` for CHECK constraints to make them idempotent
- Constraint names prefixed with `chk_fuel_txn_` to avoid collisions
- Partial index on `source_transaction_id` (WHERE NOT NULL) for efficiency

## Deployment Handoff
- **Services**: All backend services (migration runs via goodmen-database)
- **Migrations**: `20260328100000_fuel_txn_category_source_columns.js` — run before deploy
- **Env/config**: None
- **Post-deploy verification**: Check `fuel_transactions` table has `category` and `source_transaction_id` columns
