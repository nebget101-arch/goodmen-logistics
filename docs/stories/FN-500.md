# FN-500: Settlement UI — Dual Settlement View, Balance Transfers, Approval Queue

## Requirement
The Settlement UI must surface both Driver and Equipment Owner settlements with full expense breakdowns, balance transfer approvals, and type-aware detail views.

## Acceptance Criteria
- [x] Settlement list shows both types with type badge (Driver/Equipment Owner)
- [x] Filter by settlement_type, Equipment Owner
- [x] Detail view: Header (Driver, Truck#, EO, Period, Type), Revenue (load-by-load), Fuel Expenses (with split shown), Toll Expenses (with split shown), Recurring Deductions (applies_when + split), Carried Balance (link to source), Balance Transfers (EO only), Net Pay
- [x] Separate PDF generation for Driver and EO settlements (existing PDF button works per settlement)
- [x] Balance Transfer Approval Queue: pending transfers list, Approve/Reject with notes, history
- [x] Net < 0 shown as $0 pay with rollover amount in red

## Dependencies
- FN-499 (Settlement Engine V2 — backend dual generation)

## Agent
frontend

## Implementation Summary

### `settlement-list.component.*`
- Added `settlementType`, `equipmentOwnerName`, `carriedBalance` to `SettlementRow`
- Added `settlementType` filter (Driver / Equipment Owner) — passed as `settlement_type` param to API
- New "Type" column with color-coded badge: Driver (blue), Equipment Owner (purple)
- "Additional payee" column replaced with "Equipment Owner" name
- New "Carried" column: shows carried balance in red when > 0
- "Balance transfers" button in header navigates to `/settlements/balance-transfers`

### `settlement-detail.component.*`
- Compensation summary now shows Settlement Type badge, Truck ID, and Equipment Owner name (V2 only, via `isV2Settlement()`)
- Totals section: Net pay card shows `$0.00` with red "↑ $X.XX carried forward" notice when `hasNegativeNet()`
- `buildBuckets()` now also populates V2 buckets: `fuelAdjustments`, `tollAdjustments`, `carriedBalanceAdjs`, `balanceTransferAdjs`
- New V2 sections (shown only when `isV2Settlement()`):
  - **Fuel expenses** — `source_type = 'imported_fuel'`
  - **Toll expenses** — `source_type = 'imported_toll'`
  - **Carried balance** — `source_type = 'carried_balance'`
  - **Balance transfers** — `source_type = 'balance_transfer'` (EO only via `isEoSettlement()`)

### New: `balance-transfer-queue.component.*`
- Route: `/settlements/balance-transfers`
- Lists all `settlement_balance_transfers` records from `GET /settlements/balance-transfers`
- Status filter (pending / approved / applied / rejected)
- Approve and Reject actions open a confirmation modal with optional review notes
- Calls `PATCH /settlements/balance-transfers/:id/approve` or `.../reject`

### `api.service.ts`
- `generateDualSettlements()` — POST /settlements/generate-dual
- `listBalanceTransfers()` — GET /settlements/balance-transfers
- `createBalanceTransfer()` — POST /settlements/balance-transfers
- `approveBalanceTransfer()` — PATCH /settlements/balance-transfers/:id/approve
- `rejectBalanceTransfer()` — PATCH /settlements/balance-transfers/:id/reject

## Files Changed
| File | Change |
|------|--------|
| `frontend/src/app/services/api.service.ts` | Add 5 V2 API methods |
| `frontend/src/app/settlements/settlement-list/settlement-list.component.ts` | Type filter, V2 row fields, badge helpers |
| `frontend/src/app/settlements/settlement-list/settlement-list.component.html` | Type badge column, EO column, carried column, balance-transfers button |
| `frontend/src/app/settlements/settlement-list/settlement-list.component.css` | Badge colors, carried-flag, btn-secondary |
| `frontend/src/app/settlements/settlement-detail/settlement-detail.component.ts` | V2 buckets, V2 helpers (isV2, isEo, carriedBalance, hasNegativeNet) |
| `frontend/src/app/settlements/settlement-detail/settlement-detail.component.html` | Type badge in summary, V2 sections, rollover net display |
| `frontend/src/app/settlements/settlement-detail/settlement-detail.component.css` | V2 section styles, rollover notice, badge colors |
| `frontend/src/app/settlements/balance-transfer-queue/balance-transfer-queue.component.ts` | NEW |
| `frontend/src/app/settlements/balance-transfer-queue/balance-transfer-queue.component.html` | NEW |
| `frontend/src/app/settlements/balance-transfer-queue/balance-transfer-queue.component.css` | NEW |
| `frontend/src/app/settlements/settlements.module.ts` | Register BalanceTransferQueueComponent |
| `frontend/src/app/settlements/settlements-routing.module.ts` | Add /balance-transfers route |
| `docs/stories/FN-500.md` | This doc |

## Deployment Handoff
- **Services**: Angular frontend redeploy
- **Migrations**: Requires FN-495 migration (V2 schema columns) and FN-499 backend
- **Env/config**: None
- **Deploy order**: FN-495 migration → FN-499 backend deploy → FN-500 frontend deploy
- **Post-deploy verification**:
  - Settlements list → verify Type column shows Driver/EO badges for V2 settlements
  - Open a V2 Driver settlement → Fuel/Toll/Carried sections visible; Net shows $0 + red rollover if negative
  - Open a V2 EO settlement → Balance Transfers section visible
  - Navigate to /settlements/balance-transfers → list loads; approve a pending transfer → status → Approved

## Review Findings
_To be filled by Code Reviewer_

## QA Findings
_To be filled by QA_

## Open Items
- None
