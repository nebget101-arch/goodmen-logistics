# FN-1481: Receiving activity report — who received what, when (with filters + CSV)

## Requirement
A new Activity tab/route showing posted receiving tickets with filters (date range, user, vendor, ticket#), summary aggregations, click-through detail drawer, and CSV export. Data already exists in `receiving_tickets` + `inventory_transactions`; this story exposes it.

## Acceptance Criteria
- [x] `GET /api/receiving/activity?locationId=&from=&to=&userId=&vendor=&page=&pageSize=` returns paginated POSTED tickets with lines + posted_by_name + aggregations (totalParts, totalLines, totalCost, byUser[], byVendor[]) — FN-1493
- [x] `GET /api/receiving/activity.csv?...` streams CSV (one row per line item) — FN-1493
- [x] Frontend list with filter chips, summary tiles, ticket detail drawer, CSV button — FN-1494
- [x] Drawer includes invoice file link when FN-1480 attached one — FN-1494 (rendered when `invoice_file_url` is present on the ticket payload)
- [x] Filters reflected in URL (deep-linkable) — FN-1494
- [x] Mobile: filter sheet, summary wraps, cards view — FN-1494
- [x] Keyboard + a11y: drawer focus-trap, filters tabbable — FN-1494

## Dependencies
- Blocked by FN-1478

## Agent
frontend (parent), with one backend subtask

## Integration Branch
`integration/FN-1481`

## Subtasks
| Key | Summary | Agent | Branch | Files Touched | Status |
|-----|---------|-------|--------|---------------|--------|
| FN-1493 | Backend — activity endpoint + CSV export | backend | `backend/FN-1493/activity-endpoints` | `backend/packages/goodmen-shared/routes/receiving.js`, `routes/receiving-activity.test.js` | Done |
| FN-1494 | Frontend — activity tab + drawer + CSV | frontend | `frontend/FN-1494/activity-ui` | `frontend/src/app/components/warehouse-receiving/receiving-activity*.component.*`, `frontend/src/app/services/api.service.ts`, `frontend/src/app/components/warehouse-receiving/warehouse-receiving.component.*`, `frontend/src/app/app.module.ts` | Done |
| FN-1495 | QA — Validate activity report | qa | `qa/FN-1495/activity-validation` | cypress spec, evidence | Blocked |

### FN-1493 (subtask) — Backend activity endpoints

**Branch:** `backend/FN-1493/activity-endpoints` (off `integration/FN-1481`)

**Endpoints added in `routes/receiving.js`:**
- `GET /api/receiving/activity` — paginated POSTED tickets (default 25/page, max 200) with `posted_by_name` + `location_name` resolved on each ticket and lines expanded for the current page only. Top-level aggregations (`totalParts`, `totalLines`, `totalCost`, `byUser[]`, `byVendor[]`) are computed across the **full filtered set**, not just the page, so the summary tiles stay stable across pagination. `X-Total-Count` header carries the unpaged total.
- `GET /api/receiving/activity.csv` — streams CSV via knex `.stream()`, one row per line item; columns `ticket_number, posted_at, location_name, vendor_name, reference_number, sku, part_name, qty_received, unit_cost, posted_by_name`. CSV escaping wraps any cell containing `,`, `"`, `\r`, or `\n` and doubles embedded quotes.

**Filter set** (shared by both endpoints, all optional, AND-composed via the `applyActivityFilters` helper):
- `locationId` → `receiving_tickets.location_id =`
- `from` / `to` → inclusive `posted_at` window (parsed to `Date`; invalid values ignored)
- `userId` → `receiving_tickets.posted_by =`
- `vendor` → exact `vendor_name =` (frontend dropdown drives this)

`receiving_tickets.status = 'POSTED'` is always applied. Auth required; no role gate beyond auth (read-only audit data is broadly visible).

**Tests** — `routes/receiving-activity.test.js` (15 cases): filter composition, date-range, DRAFT/other-location exclusion, pagination + `X-Total-Count`, aggregation correctness, aggregations stable across pagination, CSV streaming end-to-end (1500+ rows), CSV escaping for commas and embedded quotes, empty-result shape.

### FN-1494 (subtask) — Frontend Activity tab + drawer + CSV

**Branch:** `frontend/FN-1494/activity-ui` (off `integration/FN-1481`)

**Components added under `frontend/src/app/components/warehouse-receiving/`:**
- `receiving-activity.component.{ts,html,css,spec.ts}` — Activity report panel with date-range chips (Today / 7d / 30d / Custom), user typeahead (datalist seeded from `listUsers()`), vendor typeahead (datalist seeded from the `byVendor` aggregation returned by `/api/receiving/activity`), a current-page ticket# search, summary tiles (parts, total cost, ticket count, unique users), paginated table, and an "Export CSV" anchor that points at `/api/receiving/activity.csv` with the current filter set. Filter state round-trips through `ActivatedRoute` query params (`preset`, `from`, `to`, `userId`, `vendor`, `q`, `page`) so URLs are deep-linkable. Loading skeleton, empty state with reset shortcut, and a banner+retry path on fetch failure are all separate render branches in the template.
- `receiving-activity-drawer.component.{ts,html,css,spec.ts}` — Side drawer for a selected posted ticket. `@HostListener('document:keydown.escape')` closes the drawer; a host listener on Tab/Shift-Tab clamps focus inside the panel; `aria-modal="true"`, an explicit close button with `data-drawer-initial-focus`, and a backdrop click that only closes when the click target is the scrim. When the ticket payload includes `invoice_file_url` (or `invoice_url`), the drawer renders an "Open invoice" link in the metadata block.

**Mounted via `warehouse-receiving.component.html`:** added a Receive / Activity tab strip below the page header, controlled by an `activeTab` signal on the component. The existing receive flow renders only on the Receive tab; the Activity tab renders `<app-receiving-activity [locationId]="locationId">`.

**API helpers added to `services/api.service.ts`:**
- `getReceivingActivity(filters)` — calls `/api/receiving/activity` with the filter set above, mirroring the backend contract from FN-1493.
- `getReceivingActivityCsvUrl(filters)` — builds the absolute URL for the streaming CSV export so the template can use it as an `<a href>` with `download="receiving-activity.csv"`.

## Implementation Summary
- **FN-1493 (backend)** — Done. Two new endpoints under `/api/receiving/activity` (JSON, paginated, with aggregations) and `/api/receiving/activity.csv` (streaming CSV). Filters compose via a single `applyActivityFilters` helper so the JSON and CSV endpoints can never disagree about which rows match.
- **FN-1494 (frontend)** — Done. New `ReceivingActivityComponent` and `ReceivingActivityDrawerComponent` mounted as the Activity tab on the Warehouse Receiving page; CSV button uses the streaming export; URL query params persist the filter state for deep linking.

## Files Changed
- `backend/packages/goodmen-shared/routes/receiving.js` — added `applyActivityFilters` + `csvEscape` helpers and the two new routes between `GET /summary/today` and `GET /:id` (route ordering matters — literal paths must precede the `:id` capture).
- `backend/packages/goodmen-shared/routes/receiving-activity.test.js` — new (15 tests). Uses a self-contained knex stub that supports `whereIn`, comparison operators, multi-join projection with `table.col as alias`, raw `COALESCE(...)`, raw `<table>.<col> AS <alias>`, and `.stream()` (emits each row as a `data` event).
- `frontend/src/app/components/warehouse-receiving/receiving-activity.component.{ts,html,css,spec.ts}` — new Activity tab component with filter chips, summary tiles, paginated table, mobile cards, deep-linkable URL params.
- `frontend/src/app/components/warehouse-receiving/receiving-activity-drawer.component.{ts,html,css,spec.ts}` — new side drawer with focus-trap, Escape-to-close, conditional invoice link.
- `frontend/src/app/components/warehouse-receiving/warehouse-receiving.component.{ts,html,css}` — added `activeTab` switch (Receive | Activity) and mounted `<app-receiving-activity>` under the Activity tab.
- `frontend/src/app/services/api.service.ts` — added `getReceivingActivity` and `getReceivingActivityCsvUrl`.
- `frontend/src/app/app.module.ts` — declared `ReceivingActivityComponent` and `ReceivingActivityDrawerComponent`.

## Key Decisions
- **Aggregate in JS over the filtered ticket set, not via SQL `GROUP BY`.** Avoids a dialect-specific aggregation query and keeps the test stub simple. Trade-off: pulls all filtered ticket rows into memory once. For audit reports scoped to a location + date range this is bounded by ticket count (not lines × tickets) and fine.
- **Aggregations cover the full filtered set, not the page.** Otherwise the summary tiles would shift every time the user paged, which is useless. The page is only the `data` array; `totalParts`/`totalLines`/`totalCost`/`byUser`/`byVendor` and `X-Total-Count` all describe the unpaged result.
- **CSV streams via knex `.stream()`.** Required for the >10k-row criterion; `res.write()` per row keeps server memory bounded. The header row is written before the stream attaches so partial-failure responses still have something useful.
- **`from`/`to` are normalized to `Date` objects in the route** (not just passed through). Production-equivalent behavior across DB and tests; invalid values are silently dropped rather than returning 400 (matches the rest of the receiving routes' tolerance for missing optional filters).
- **Vendor filter is exact-match.** Frontend will populate from a vendor dropdown (the same vendor strings already on tickets); substring matching can be added later if a free-text input replaces the dropdown.
- **Aliased the CSV `parts.name` column to `part_name`** to disambiguate from `posted_by_name` and `location_name` in the row object.
- **(FN-1494) Ticket# search is client-side, on the current page only.** The backend has no `ticketNumber` query param (FN-1493 ships date/user/vendor). The intent in the spec is "quick refinement" inside an already-narrowed result set, so this avoids a backend round-trip and keeps the search responsive. If a user opens the Activity tab without any filter applied, paging may hide a search hit, which is acceptable for a refinement UX. Promote to a server filter if real users hit the limit.
- **(FN-1494) Vendor + user typeaheads are powered by `<datalist>` instead of a custom dropdown.** Vendor options come from the `byVendor` aggregation already returned by `/api/receiving/activity`, so it's always scoped to vendors that actually appear in posted tickets — no second round-trip and no orphan options. User options come from `listUsers()`. Both fall back to free-text input, so an unknown value still passes through to the backend.
- **(FN-1494) Filters reflected in `queryParams` via `replaceUrl: true`.** Avoids polluting browser history while typing; deep links keep working because the snapshot is read once on init.
- **(FN-1494) Drawer focus-trap is implemented in the component (not via CDK FocusTrap).** Keeps the dependency surface small (no new imports) and matches the project's existing drawer pattern (`load-detail-drawer`). `data-drawer-initial-focus` on the close button is the explicit landing target so screen reader users get a predictable entry point.
- **(FN-1494) Mobile breakpoint at 700px collapses the table into card list.** The table has 7 columns and overflows below ~640px; switching to cards keeps every row's totals visible at a glance. The filter bar collapses on the same breakpoint.
- **(FN-1494) Tab placement instead of a separate route.** The spec allowed either; tabbing keeps the Receive flow's location selector in scope (the Activity tab inherits the same `[locationId]`), so users don't have to re-pick a location after switching context. The route alternative is still reachable via `/receiving?activity=1`-style state — not added here to keep the URL surface minimal.

## Deployment Handoff
- **Services to redeploy:**
  - `goodmen-warehouse-microservice` — backend routes (FN-1493). All goodmen-shared microservices share the same code, but only the warehouse service exposes `/api/receiving/*`.
  - `goodmen-frontend` — Angular bundle (FN-1494). Static SPA; redeploy is just a `dist/goodmen-logistics` build + push.
- **Jobs/workers affected:** None.
- **DB migrations:** None. Endpoints query existing `receiving_tickets`, `receiving_ticket_lines`, `users`, `locations`, `parts` tables.
- **Env / config changes:** None.
- **Cache invalidation:** None — endpoints are uncached, read-only audit queries; the SPA bundle has cache-busting hashes.
- **Feature flags:** None. The Activity tab is unconditionally rendered for any user who can reach the Warehouse Receiving page (existing `AuthGuard` + `PlanGuard` on the route).
- **Deploy order:** Backend first (already deployed via FN-1493), then frontend (FN-1494), then QA validation (FN-1495). The frontend will 404 the activity calls until the backend is up; backend was deployed earlier in the integration branch so this should be a no-op in dev.
- **Post-deploy verification:**
  1. `curl -H "Authorization: Bearer <token>" "https://<warehouse-service>/api/receiving/activity?locationId=<uuid>"` returns 200 with `data`, `total`, `totalParts/totalLines/totalCost`, `byUser`, `byVendor`.
  2. `X-Total-Count` header is set.
  3. `curl -H "Authorization: Bearer <token>" "https://<warehouse-service>/api/receiving/activity.csv?locationId=<uuid>" -o out.csv` returns a CSV file with the documented columns and at least one data row in non-empty environments.
  4. OpenAPI docs (Swagger UI) show both new endpoints under the **Receiving** tag.
  5. Open `/receiving` → switch to **Activity** tab → tiles populate, table renders, click a row → drawer opens, Escape closes, Tab cycles inside the drawer.
  6. Apply each filter (date chip, user, vendor, ticket# search) → URL query params update; copy URL into a new tab → filters rehydrate.
  7. Click **Export CSV** → CSV downloads with the same rows as the active filters.
  8. Resize to <700px width → filter bar collapses, summary tiles wrap to 2-column, table becomes a card list, row Enter still opens the drawer.

## Review Findings
_To be filled by Code Reviewer_

## QA Findings
_To be filled by QA_

## Open Items
- None
