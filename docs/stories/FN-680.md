# FN-680: Schema Enhancements for Locations, Bins & Warehouse-to-Shop Supply

## Goal
Add 4 database migrations to support locations enhancements, a dedicated bins table, inventory linkage, and warehouse-to-shop supply rules.

## Agent
database

## Subtasks

| Key | Summary | Agent | Branch | Status |
|-----|---------|-------|--------|--------|
| FN-686 | Migration 1 — Enhance locations table | database | | Selected for Dev |
| FN-687 | Migration 2 — Create location_bins table | database | `database/FN-687/location-bins-table` | Done |
| FN-688 | Migration 3 — Link inventory/receiving to bins + backfill | database | | Selected for Dev |
| FN-689 | Migration 4 — Create warehouse_shop_supply_rules | database | | Selected for Dev |
| FN-690 | QA — Validate migrations run cleanly | qa | _manual_ | Backlog |

## Implementation Summary

_To be filled when all subtasks complete_

### FN-687 (subtask)
**Migration 2 — Create `location_bins` table**

Created Knex migration `20260416180000_create_location_bins.js` with:
- `location_bins` table: id (UUID PK), tenant_id, location_id (FK → locations ON DELETE CASCADE), bin_code, bin_name, bin_type (SHELF/RACK/FLOOR/CABINET/FREEZER/OUTDOOR), zone, aisle, shelf, position, capacity_notes, active, created_at, updated_at
- `uq_location_bins_code` — UNIQUE index on (location_id, bin_code)
- `idx_location_bins_tenant` — index on (tenant_id)
- `idx_location_bins_location` — index on (location_id, active)
- Down migration drops all indexes then drops table

## Files Changed

- `backend/packages/goodmen-database/migrations/20260416180000_create_location_bins.js` (FN-687)

## Key Decisions

_To be filled when all subtasks complete_

## Deployment Handoff

- **Services to redeploy**: Any service that runs migrations on startup (all services with Knex)
- **DB migrations**: Run `knex migrate:latest` — file `20260416180000_create_location_bins.js`
- **Environment/config changes**: None
- **Post-deploy verification**: Verify `location_bins` table exists with `\d location_bins` in psql
