# FN-1460: DQF document upload writes to legacy `dqf_documents` table; FK violation on requirement update

## Requirement

Re-point the DQF document upload flow at the canonical `driver_documents` table so the FK on `dqf_driver_status.evidence_document_id` is satisfied. Backfill any existing `dqf_documents` rows into `driver_documents` to preserve customer-uploaded files.

## Acceptance Criteria

- [ ] `POST /api/dqf-documents/upload` writes to `driver_documents` (not `dqf_documents`).
- [ ] Existing `dqf_documents` rows backfilled into `driver_documents` with same UUIDs (idempotent).
- [ ] All 8 references in `routes/dqf-documents.js` switched to canonical table.
- [ ] End-to-end UI flow: upload driver license → requirement status persists → file downloadable.
- [ ] No regression in `routes/dqf.js`, `routes/public-onboarding.js`, drug-alcohol, or employer-investigation features.
- [ ] `dqf_documents` left in place for one release cycle; follow-up ticket drops it.

## Dependencies

None. The parent bug is standalone; subtasks have only the QA-blocked-by-impl chain.

## Agent

backend + database (parallel) → qa

## Integration Branch

`integration/FN-1460` (created by first subtask agent from `origin/dev`; subtasks branch off this; bug PR merges to `dev`)

## Subtasks

| Key | Summary | Agent | Branch | Files Touched | Status |
|-----|---------|-------|--------|---------------|--------|
| FN-1461 | Re-point dqf-documents route to driver_documents | backend | `backend/FN-1461/dqf-upload-canonical-table` | `backend/packages/goodmen-shared/routes/dqf-documents.js` | Done |
| FN-1462 | Backfill dqf_documents → driver_documents migration | database | `database/FN-1462/dqf-document-backfill-migration` | `backend/packages/goodmen-database/migrations/<NEW>_backfill_dqf_documents_to_driver_documents.js` | Selected for Dev |
| FN-1463 | E2E validation of DQF upload + requirement status | qa | `qa/FN-1463/dqf-document-upload-e2e` | `frontend/cypress/e2e/dqf-document-upload.cy.ts`, `docs/stories/evidence/FN-1460/` | Blocked by FN-1461, FN-1462 |

## Implementation Summary

### FN-1461 (subtask)

Switched all 8 `dqf_documents` references in `routes/dqf-documents.js` to `driver_documents`. Reads alias canonical column names back to the legacy keys so the JSON response shape is unchanged for the frontend (`doc_type AS document_type`, `storage_key AS file_path`, `size_bytes AS file_size`). The `POST /upload` insert now writes `(driver_id, doc_type, file_name, storage_key, size_bytes, mime_type, storage_mode='r2')`, returning a UUID that satisfies `dqf_driver_status_evidence_document_id_foreign`.

Column-mapping decisions:
- `document_type` → `doc_type`
- `file_path` → `storage_key`
- `file_size` → `size_bytes`
- `storage_mode` set to `'r2'` (matches `routes/drug-alcohol.js` pattern for R2-backed driver_documents rows)
- `blob_id` left NULL (R2-only; no in-DB blob)
- `uploaded_by` dropped from INSERT — column does not exist on `driver_documents` and the parent AC says "prefer reusing existing columns" over adding a new column. SELECTs return `NULL AS uploaded_by` so the response shape stays identical.

## Files Changed

- `backend/packages/goodmen-shared/routes/dqf-documents.js` — 8 SQL refs flipped from `dqf_documents` → `driver_documents`; column aliases added on read; INSERT mapped to canonical columns.

## Key Decisions

- **Option 1 chosen** (re-point upload to canonical table) over (a) switching the FK to `dqf_documents` — would break four other features that already use `driver_documents` — or (b) hybrid dual-write — adds complexity without solving the underlying duplication.
- **Drop deferred** — `dqf_documents` stays as a read-only legacy shim for one release cycle; a follow-up ticket drops it after prod verification.
- **Migration is idempotent and additive** — backfill uses `ON CONFLICT (id) DO NOTHING` so it can be re-run and ordering with the backend deploy doesn't matter.

## Deployment Handoff

- **Service to redeploy**: `fleetneuron-drivers-compliance-service` (the Render service hosting `routes/dqf-documents.js`).
- **Migrations**: backfill migration delivered by FN-1462 — runs on deploy via `knex.migrate.latest()` in the service bootstrap. Ordering with this code change is irrelevant (backfill is idempotent; `ON CONFLICT (id) DO NOTHING`).
- **Env vars / config**: none.
- **Cache invalidation**: none.
- **Feature flags**: none.
- **Deploy order**: backend (this change) and migration can ship in either order — the migration is additive and the backend now writes only to `driver_documents`.
- **Post-deploy verification**:
  1. `curl -F file=@license.jpg -F driverId=<uuid> -F documentType=driver_license_front /api/dqf-documents/upload` → expect 201; new row in `driver_documents` (NOT `dqf_documents`).
  2. Chain a `POST /api/dqf/requirement/<driverId>/driver_license_front_on_file` with the returned `id` → expect 200, no FK violation on `dqf_driver_status_evidence_document_id_foreign`.
  3. Verify `GET /api/dqf-documents/driver/<driverId>` returns the new row with `document_type` / `file_path` / `file_size` keys (legacy response shape preserved).

### Out-of-scope notes for QA / follow-up

- `routes/dqf.js` (line 99) and `routes/dqf-documents.js`'s sibling features were not touched in this subtask — only the 8 references in `routes/dqf-documents.js` per the AC. `routes/dqf.js` still reads from `dqf_documents` for the legacy GET; FN-1462's backfill keeps that path working until the follow-up drop ticket lands.
- `uploaded_by` is no longer persisted on new uploads. If audit-of-uploader is required later, add a nullable `uploaded_by` column to `driver_documents` in a separate ticket.

## Review Findings

_To be filled by Code Reviewer_

## QA Findings

_To be filled by QA_

## Open Items

- Follow-up ticket (post-merge, separate release cycle): drop `dqf_documents` table once we're confident no other consumer reads it. Owner: database agent.
