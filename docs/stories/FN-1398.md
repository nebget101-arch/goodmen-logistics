# FN-1398: Barcode generator + printable label on part create

## Requirement
When a user adds a part to the parts catalog, the backend auto-generates a unique scannable barcode value and the frontend offers an immediate "Print label" action that produces a sheet-ready label containing the part's name, SKU, a Code-128 1D barcode, and a QR code — both encoding the same generated barcode value.

Closes a gap in the FN-1090 quick-add flow: today `parts.barcode` is a manual free-text field, so warehouse staff have no way to print a scannable label at part-creation time and fall back to typing SKUs by hand at receiving/picking.

## Acceptance Criteria
- [ ] On `POST /api/parts`, if `barcode` is empty, backend generates `FN-XXXXXXXX` (8 alphanumeric chars from `ABCDEFGHJKLMNPQRSTUVWXYZ23456789` — no `O/0/I/1`) and persists it in `parts.barcode`.
- [ ] If the client supplies a `barcode` value, it is preserved verbatim (no regeneration).
- [ ] Generated value is unique across `parts.barcode`; on collision, retry up to 5 times before failing the request.
- [ ] After successful part create, parts-catalog UI shows a "Print label" action in the success modal.
- [ ] Print template renders: part name (large), SKU, barcode value as text, Code-128 SVG, QR SVG — both encoding the same value.
- [ ] `window.print()` produces a clean label with no app chrome (sidebar/header/buttons hidden via `@media print`).
- [ ] A real 1D laser scanner reads the Code-128; a phone camera reads the QR — both decode to the stored value.
- [ ] No DB migration needed (uses existing `parts.barcode` column).

## Dependencies
- Parent epic FN-1090 (Quick Add Part) is In Progress — no blocking dependencies.
- QA subtask FN-1401 is blocked by FN-1399 (frontend) and FN-1400 (backend).

## Agent
frontend (Story-level lead) + backend + qa

## Integration Branch
`integration/FN-1398` (created by the first implementing agent from `origin/dev`; subtasks branch off this; final PR merges to `dev`)

## Subtasks
| Key | Summary | Agent | Branch | Files Touched | Status |
|-----|---------|-------|--------|---------------|--------|
| FN-1399 | JsBarcode + QR label component, print flow on part create | frontend | `frontend/FN-1399/barcode-label-print` | `frontend/src/app/components/parts-catalog/**`, `frontend/src/app/components/part-label-print/**` (new), `frontend/package.json`, `frontend/src/app/app.module.ts` | Done |
| FN-1400 | Auto-generate unique barcode value on part create | backend | `backend/FN-1400/auto-generate-barcode` | `backend/packages/goodmen-shared/services/parts.service.js`, `backend/packages/goodmen-shared/services/barcode-generator.js` (new), `backend/packages/goodmen-shared/services/barcode-generator.test.js` (new) | Done |
| FN-1401 | Validate generation, label print, scanner read | qa | _manual_ | `docs/stories/evidence/FN-1398/` | Blocked (waits on 1399, 1400) |

## Library Choices
- **JsBarcode** (~30 KB, MIT, SVG output, Code-128 format) — new dependency for `frontend/`.
- **qrcode@^1.5.4** — already installed, reuse from `barcode-management.component.ts`.
- No backend library needed (random ID generation is plain JS).

## Implementation Summary

### FN-1400 (subtask) — Backend barcode auto-generation

Added `barcode-generator.js` helper exposing `resolveBarcodeForCreate(supplied, checkExists)`:

- If `supplied` is a non-empty string, it is preserved verbatim (no regeneration, no validation — matches existing behavior for clients that bring their own barcode).
- Otherwise generates `FN-XXXXXXXX` from the safe alphabet `ABCDEFGHJKLMNPQRSTUVWXYZ23456789` (no `O/0/I/1`) and retries up to 5 times if `checkExists` reports a collision. After 5 collisions it throws an `Error` with `statusCode = 500`.

Wired into `partsService.createPart` via `isBarcodeTaken(value)`, which checks **both** `parts.barcode` and `part_barcodes.barcode_value` so the generated value cannot collide with anything a scanner could match. The resulting barcode is included in the `parts` insert; `POST /api/parts` already returns the row via `.returning('*')` so the frontend receives the value without a re-fetch.

Unit tests in `barcode-generator.test.js` cover: candidate format match, single-shot success, retry path with simulated collisions, exhaustion (throws after 5 attempts), `supplied → preserved`, and `empty/whitespace/null/undefined → generated`. 11/11 pass under `node --test`.

### FN-1399 (subtask)
- Added `jsbarcode@^3.12.1` (MIT) and `@types/jsbarcode@^3.11.4` to `frontend/package.json`. `qrcode` was already installed and is reused.
- New component `app-part-label-print` (`frontend/src/app/components/part-label-print/`) accepts a part as `@Input()` and renders the part name, SKU, the barcode value as text, a Code-128 SVG via JsBarcode, and a QR SVG via `QRCode.toString(value, { type: 'svg' })`. Sized for a 4"×2" label.
- Wired the print flow into `parts-catalog.component`:
  - On successful `apiService.createPart()` the response part (with backend-generated `barcode`) is captured into `printablePart` and a confirmation modal opens with **Done** and **Print label** actions.
  - **Print label** sets `partForLabel`, waits one tick for JsBarcode/QRCode to mount, then calls `window.print()` (mirrors the `drivers.component.ts:1479` DQF print pattern). The hidden `.label-print-area` is the only element kept visible by `@media print` in the parts-catalog CSS — sidebar, header, modals, and table are all hidden.
  - Existing parts in the catalog list now have a **Print label** action that runs the same `printLabelForPart()` flow (only shown for parts that have a `barcode`).
- Form barcode field is read-only with a placeholder of "auto-generated" by default; users opt in to a vendor-supplied barcode via the **I have a vendor barcode** checkbox. On Edit and on barcode-prefilled scans (FN-1107) the toggle is hidden so existing behavior is preserved.
- Declared `PartLabelPrintComponent` in `app.module.ts`. Build passes (`ng build --configuration=development`) with no errors.
- Unit specs added under `parts-catalog.component.spec.ts` covering: confirmation modal opens with backend barcode, no-op when barcode missing, `window.print()` triggered with correct part bound, `dismissPrintConfirm` clears state without printing, `vendorBarcodeOverride` reset on form open/close.

## Files Changed

### FN-1400 (subtask)
- `backend/packages/goodmen-shared/services/barcode-generator.js` (new)
- `backend/packages/goodmen-shared/services/barcode-generator.test.js` (new)
- `backend/packages/goodmen-shared/services/parts.service.js` (added `isBarcodeTaken` helper, integrated `resolveBarcodeForCreate` into `createPart`, persists `barcode` in the insert)

### FN-1399 (subtask)
- `frontend/package.json` — added `jsbarcode` + `@types/jsbarcode`
- `frontend/src/app/components/part-label-print/part-label-print.component.{ts,html,css}` — new component
- `frontend/src/app/components/parts-catalog/parts-catalog.component.ts` — print flow + vendor-override toggle state
- `frontend/src/app/components/parts-catalog/parts-catalog.component.html` — success modal, row-level Print Label action, hidden print area, override checkbox
- `frontend/src/app/components/parts-catalog/parts-catalog.component.css` — print confirm modal styles + `@media print` rules
- `frontend/src/app/components/parts-catalog/parts-catalog.component.spec.ts` — FN-1399 unit specs
- `frontend/src/app/app.module.ts` — declared `PartLabelPrintComponent`

## Key Decisions
- **Option B chosen for value strategy**: backend generates `FN-XXXXXXXX` rather than reusing SKU. Rationale: decouples printed labels from SKU edits; avoids exposing user-typed SKUs in scanned form.
- **Both Code-128 and QR on the same label**: Code-128 for cheap warehouse 1D scanners, QR as fallback for phone cameras. Same value encoded in both.
- **Browser print, not Zebra/Dymo**: scoped to `window.print()` + `@media print`. Printer-specific formats deferred.

## Deployment Handoff

### FN-1400 (backend)

- **Services to redeploy**: `fleetneuron-inventory-service`, `fleetneuron-logistics-gateway` (both consume `@goodmen/shared`; gateway proxies `POST /api/parts`).
- **DB migrations**: none — uses existing `parts.barcode` column.
- **Env / config changes**: none.
- **Cache invalidation**: none.
- **Feature flags**: none.
- **Deploy order**: any order (no cross-service contract change; response shape adds `barcode` only when previously empty).
- **Post-deploy verification**:
  1. `POST /api/parts` with no `barcode` → response `data.barcode` matches `^FN-[A-HJ-NP-Z2-9]{8}$`.
  2. `POST /api/parts` with `barcode: "CUSTOM-123"` → response `data.barcode === "CUSTOM-123"`.
  3. Spot-check `parts` table: no rows created via this endpoint should have `barcode IS NULL` after deploy.

### FN-1399 (subtask) — frontend deploy
- **Service to redeploy**: `goodmen-frontend` (Render static-site frontend; see `.agent/docs/render_services.md`). New `jsbarcode` dependency means a fresh `npm ci` is needed.
- **Jobs/workers**: none.
- **DB migrations**: none.
- **Env / config**: no new env vars.
- **Cache invalidation**: none beyond the standard frontend cache-bust.
- **Feature flags**: none.
- **Deploy order**: ship FN-1400 (backend barcode generator) **before or alongside** the frontend so newly created parts have a `barcode` for the new "Print label" CTA. The frontend is defensive — the success modal won't appear if `data.barcode` is missing — so a backend-first deploy is safe but the post-create CTA only shows up once the backend is on the new code.
- **Post-deploy verification**:
  1. Open `/parts-catalog`, create a new part — confirmation modal must appear with the generated `FN-XXXXXXXX` barcode.
  2. Click **Print label** → browser print dialog renders only the 4"×2" label (no sidebar/header/buttons).
  3. From the parts table, click **Print label** on an existing part with a barcode — same behavior.
  4. Toggle **I have a vendor barcode** in the create modal → barcode field becomes editable.

## Review Findings
_To be filled by Code Reviewer_

## QA Findings
_To be filled by QA_

## Open Items
- None
