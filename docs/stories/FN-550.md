# FN-550: Add Equipment Owner Percentage to Pay Configuration

## Overview
Add an explicit `equipment_owner_percentage` column to driver compensation profiles so settlement calculations use a stored EO% rather than deriving it as a remainder.

## Subtasks

| Key | Summary | Agent | Status |
|-----|---------|-------|--------|
| FN-554 | DB migration: add equipment_owner_percentage with backfill | database | Done |
| FN-555 | Backend: save/validate equipment_owner_percentage, update settlement EO% logic | backend | Done |
| FN-556 | Frontend: Equipment Owner % and Company % fields in Pay Configuration | frontend | In Progress |
| FN-557 | QA: Validate EO%, Company% auto-calc, validation, and settlement EO calculation | qa | Backlog |

## Implementation Notes

### FN-555 (Backend)
- **drivers.js**: POST and PUT driver endpoints now accept `equipmentOwnerPercentage` in the request body. Validates `percentage_rate + equipment_owner_percentage <= 100`, returns 400 if violated. Stores value on `driver_compensation_profiles.equipment_owner_percentage`.
- **settlement-calculation.js**: `computeLoadPay` now accepts `equipmentOwnerPercentage`. When set (non-null, > 0), uses it to compute `additionalPayeePay = gross * equipmentOwnerPercentage / 100`. Falls back to `additionalPayeeRate`, then to remainder (legacy).
- **settlement-service.js**: All three `computeLoadPay` call sites updated to pass `equipmentOwnerPercentage` from profile snapshot.

### Files Modified (FN-555)
- `backend/packages/goodmen-shared/routes/drivers.js`
- `backend/packages/goodmen-shared/services/settlement-calculation.js`
- `backend/packages/goodmen-shared/services/settlement-service.js`

### Deployment
- Render services: `goodmen-shared` is used by all microservices. Any service that uses settlement calculations (reporting-service, auth-users-service for driver CRUD) will pick up changes on next deploy.
- DB migration (FN-554) must be deployed first.
