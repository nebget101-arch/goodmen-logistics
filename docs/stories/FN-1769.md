# FN-1769: AI fault-code diagnosis on Vehicle Tracking telemetry

## Requirement
On the Vehicle Tracking page, when a selected truck has active fault codes, show an AI suggestion of what the issue likely is and whether it needs immediate attention. The telemetry panel already renders raw faults (`{ code, description, severity }`) from the FN-1750 contract (`GET /api/vehicles/:id/telemetry`); this story adds an AI interpretation layer (plain-English diagnosis + urgency call).

Telemetry is currently deterministic MOCK (`source: 'mock'`); the AI diagnosis operates on whatever fault codes the contract returns, so it works identically once a real telematics integration lands.

## AI Contract (defined up front so AI + frontend run in parallel)
```
POST /api/ai/vehicle/fault-diagnosis
Request:  { vehicleId?, unitNumber?, faultCodes: [{ code, description, severity }] }
Response: { summary, immediateAttention, urgency: 'critical'|'high'|'medium'|'low',
            recommendedAction, perFault: [{ code, likelyCause, immediateAttention }] }
```
The frontend owns `api.service.ts` (`getVehicleFaultDiagnosis`); the AI subtask owns only the ai-service handler + router (so the two subtasks don't both edit `api.service.ts`).

## Acceptance Criteria
- [ ] `POST /api/ai/vehicle/fault-diagnosis` returns the contract shape; structured JSON; empty `faultCodes` returns a benign no-fault response without a model call.
- [ ] When a selected truck has ≥1 fault code, an "AI Diagnosis" section appears under Active Fault Codes with the summary, a clear "Immediate attention required" badge when `immediateAttention` is true, and the recommended action.
- [ ] Per-fault likely-cause is surfaced (keyed by `code`).
- [ ] Loading skeleton during the AI call; graceful error if it fails (raw fault list still renders); no AI section when there are no faults.
- [ ] AI dark-theme tokens only; no new hex; build passes.

## Dependencies
- None external. QA (FN-1772) blocked by FN-1770 + FN-1771.

## Agent
ai (story) — implemented across ai + frontend + qa subtasks

## Integration Branch
`integration/FN-1769` (created by first subtask agent from `origin/dev`; subtasks branch off this; story PR merges to `dev`). Shape C (multi-agent: ai + frontend).

## Subtasks
| Key | Summary | Agent | Branch | Files Touched | Status |
|-----|---------|-------|--------|---------------|--------|
| FN-1770 | Fault-diagnosis handler + endpoint in ai-service | ai | `ai/FN-1770/fault-diagnosis` | `backend/microservices/ai-service/src/handlers/vehicle-fault-diagnosis-handler.js`, `ai-router.js` | **Done** |
| FN-1771 | AI Diagnosis UI + api.service method | frontend | `frontend/FN-1771/fault-diagnosis-ui` | `frontend/src/app/services/api.service.ts`, `components/vehicle-tracking/**` | Selected for Dev |
| FN-1772 | QA validation | qa | _manual_ | `docs/stories/evidence/FN-1769/**` | Blocked (FN-1770, FN-1771) |

## Implementation Summary
_To be filled by implementing agent_

### FN-1770 (subtask) — AI fault-diagnosis handler + endpoint
- New handler `backend/microservices/ai-service/src/handlers/vehicle-fault-diagnosis-handler.js`, modeled on `work-order-triage-handler.js` (OpenAI `gpt-4.1-mini`, structured JSON output, defensive parsing).
- Registered `POST /vehicle/fault-diagnosis` in `ai-router.js` (gateway exposes `POST /api/ai/vehicle/fault-diagnosis`) with full OpenAPI annotation.
- **No-fault short-circuit:** empty/missing `faultCodes` returns the benign response (`immediateAttention: false`, `urgency: 'low'`, empty `perFault`) **without** a model call.
- **Deterministic safety bias:** any input fault with `high`/`critical` severity whose code/description matches a safety-critical system (engine, oil pressure, brakes, coolant, overheat, temperature, ABS, steering) forces `immediateAttention: true` and `urgency` to at least `high`. The bias only ever escalates — it never downgrades a model's `critical` rating.
- **Output normalization:** model JSON is merged onto the validated input fault list; every input fault always gets a `perFault` entry keyed by `code` even if the model omits it. Raw model JSON is never returned to the client. Unparseable model output still yields a valid contract shape.
- Mirrors existing error handling (`502` + `AI_FAULT_DIAGNOSIS_ERROR`) and `logAiInteraction` logging conventions.
- Tests: `src/handlers/__tests__/vehicle-fault-diagnosis-handler.test.js` (standalone `node`, mocked `deps.openai`); added to the ai-service `npm test` chain. All pass.

## Files Changed
- `backend/microservices/ai-service/src/handlers/vehicle-fault-diagnosis-handler.js` (new)
- `backend/microservices/ai-service/src/ai-router.js` (import + route + OpenAPI)
- `backend/microservices/ai-service/src/handlers/__tests__/vehicle-fault-diagnosis-handler.test.js` (new)
- `backend/microservices/ai-service/package.json` (test script)

## Key Decisions
- Safety bias is enforced in code (post-processing `normalizeDiagnosis`), not left to the model, so a model that under-rates an engine/oil/brake fault can never downgrade urgency below `high`.
- Used `response_format: { type: 'json_object' }` + `temperature: 0.2` for deterministic structured output, consistent with other structured ai-service handlers.

## Deployment Handoff
- **Service to redeploy:** `fleetneuron-ai-service` (Render) — picks up the new handler + route.
- **Gateway:** the gateway proxies `/api/ai/*` to the ai-service; no gateway code change needed (route is additive under the existing prefix). Verify the gateway forwards `POST /api/ai/vehicle/fault-diagnosis`.
- **Migrations:** none.
- **Env/config:** none new (reuses `OPENAI_MODEL`, defaults `gpt-4.1-mini`, and the existing OpenAI client wired into ai-service `deps`).
- **Feature flags:** none.
- **Deploy order:** ai-service first; frontend (FN-1771) consumes the endpoint after.
- **Post-deploy verification:** `POST /api/ai/vehicle/fault-diagnosis` with `{ faultCodes: [] }` → benign low-urgency response; with a high-severity engine/oil fault → `immediateAttention: true`, `urgency` ≥ `high`.

## Review Findings
_To be filled by Code Reviewer_

## QA Findings
_To be filled by QA_

## Open Items
- None
