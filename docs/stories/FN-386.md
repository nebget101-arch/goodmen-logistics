# FN-386: Improve Loads Dashboard table column density to match competitor TMS spacing

## Summary
Reduce horizontal footprint of Loads Dashboard table columns by fixing layout model, replacing wide select dropdowns with text inputs, and making status pills compact.

## Subtasks

| Key | Summary | Agent | Status | Branch |
|-----|---------|-------|--------|--------|
| FN-387 | Add table-layout: fixed, colgroup, and text truncation | frontend | Selected for Dev | — |
| FN-388 | Replace filter select dropdowns with compact text inputs | frontend | Backlog | — |
| FN-389 | Make status-pill component more compact for table density | frontend | Done | `frontend/FN-389/compact-status-pill` |
| FN-390 | QA: Validate table density matches competitor TMS spacing | qa | Backlog | — |

---

### FN-389 (subtask) — Frontend: Compact status-pill variant

**Implementation Summary:**
Added a `[compact]` input to the status-pill component (Option A from the ticket). When `compact=true`, the pill uses reduced padding, smaller font size, and abbreviated labels with full label on hover tooltip.

**Changes:**
- `status-pill.component.ts`: Added `@Input() compact`, static abbreviation map, `compactLabel` and `displayLabel` getters
- `status-pill.component.html`: Uses `displayLabel` for text, conditional `status-pill--compact` CSS class, `title` attribute with full label when compact
- `status-pill.component.scss`: Added `.status-pill--compact` variant with `padding: 2px 6px` and `font-size: 11px`
- `loads-dashboard.component.html`: Added `[compact]="true"` to both status pills in the table rows (lines 472-473)

**Abbreviation Map:**
DELIVERED→Del, DISPATCHED→Disp, FUNDED→Fund, INVOICED→Inv, PENDING→Pend, CANCELLED→Canc, IN_TRANSIT→Transit, EN_ROUTE→EnRte, PICKED_UP→PU, BOL_RECEIVED→BOL, SENT_TO_FACTORING→Factor

**Files Changed:**
- `frontend/src/app/components/shared/status-pill/status-pill.component.ts`
- `frontend/src/app/components/shared/status-pill/status-pill.component.html`
- `frontend/src/app/components/shared/status-pill/status-pill.component.scss`
- `frontend/src/app/components/loads-dashboard/loads-dashboard.component.html`

## Deployment Handoff

**Services to restart/redeploy:** Frontend Angular app
**DB migrations:** None
**Environment/config changes:** None
**Deploy order:** Frontend only
**Post-deploy verification:** Check Loads Dashboard table — status/billing pills should show abbreviated labels with smaller padding. Hover should show full label. Pills elsewhere in the app (detail modal, edit header) should remain full-size.
