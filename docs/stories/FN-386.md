# FN-386: Improve Loads Dashboard table column density

## Requirement
Make all 14 columns fit on a 1920px screen without horizontal scrolling by using fixed table layout, proportional column widths, and text truncation.

## Acceptance Criteria
- [ ] Table uses `table-layout: fixed` with `<colgroup>` percentage widths
- [ ] All data cells truncate long text with `...`
- [ ] Full value visible on hover via `title` attribute
- [ ] All 14 columns visible without horizontal scroll on 1920px
- [ ] Filter select dropdowns replaced with compact text inputs
- [ ] Status/Billing pills compact mode active
- [ ] No regressions on filters, sorting, data integrity

## Subtasks

| Key | Summary | Agent | Branch | Status |
|-----|---------|-------|--------|--------|
| FN-387 | Add table-layout: fixed, colgroup, truncation | frontend | `frontend/FN-387/loads-table-fixed-layout` | Done |
| FN-388 | Replace filter selects with text inputs | frontend | — | Backlog |
| FN-389 | Compact status pills | frontend | — | Done |
| FN-390 | QA: Validate table density | qa | — | Backlog |

### FN-387 (subtask)
**Branch**: `frontend/FN-387/loads-table-fixed-layout`

**Implementation Summary**:
- Added `table-layout: fixed` to the main loads table
- Added `<colgroup>` with 14 percentage-based column widths totaling 100%
- Added truncation CSS (`white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: 0`) to all `td` elements
- Added `[title]` attributes to all data cells for hover tooltips
- Notes column exception: uses `white-space: normal` with `-webkit-line-clamp: 2` for multi-line truncation
- Also added truncation to `th` elements for header labels

**Files Changed**:
- `frontend/src/app/components/loads-dashboard/loads-dashboard.component.html` — colgroup, title attributes
- `frontend/src/app/components/loads-dashboard/loads-dashboard.component.scss` — table-layout, truncation styles

## Deployment Handoff
- **Services to restart**: Frontend (static build)
- **DB migrations**: None
- **Env/config changes**: None
- **Deploy order**: Standard frontend deploy
- **Post-deploy verification**: View Loads Dashboard — all 14 columns should be visible without horizontal scroll on 1920px
