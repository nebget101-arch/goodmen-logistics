# FN-618: OpenTelemetry Auto-Instrumentation for All Backend Services (Phase 1)

## Requirement
Add OpenTelemetry auto-instrumentation to all 9 backend services (gateway + 8 microservices) with **self-hosted SigNoz on Render** (ClickHouse + OTel Collector + Dashboard). This is the foundation — auto-instrumentation captures HTTP requests, PostgreSQL queries, Express middleware, and external HTTP calls without modifying existing code. Cost: ~$45-55/mo.

## Acceptance Criteria
- [ ] Self-hosted SigNoz deployed on Render (ClickHouse + OTel Collector + Dashboard)
- [ ] Data retention: traces 15 days, metrics 30 days
- [ ] Shared tracing initializer in `goodmen-shared/config/tracing.js`
- [ ] All 9 services have `tracing.js` + OTel deps + `require('./tracing')` as first line
- [ ] No `SIGNOZ_ACCESS_TOKEN` needed — self-hosted uses Render networking
- [ ] Gateway forwards W3C trace context headers (`traceparent`, `tracestate`)
- [ ] Docker-compose updated with OTel env vars
- [ ] All 9 services appear in self-hosted SigNoz Dashboard
- [ ] Distributed traces show gateway -> service -> DB chain

## Dependencies
- None — this is the foundation story

## Agent
devops

## Subtasks
| Key | Summary | Agent | Branch | Status |
|-----|---------|-------|--------|--------|
| FN-648 | Deploy Self-Hosted SigNoz on Render | devops | `devops/FN-648/deploy-signoz-render` | **Done** |
| FN-625 | Create shared OTel tracing initializer | devops | `devops/FN-625/shared-otel-initializer` | Pending (needs FN-648) |
| FN-626 | Add tracing.js + OTel deps to all 9 services | devops | `devops/FN-626/service-otel-wiring` | Pending (needs FN-625) |
| FN-627 | Gateway trace-context propagation + docker-compose | devops | `devops/FN-627/gateway-propagation-docker` | Pending (needs FN-625,626) |
| FN-628 | QA: Validate auto-instrumentation traces | qa | _manual_ | Pending (needs FN-625-627) |

## Implementation Summary

### FN-648 (Deploy Self-Hosted SigNoz on Render)
- Created `infra/signoz/` directory with all SigNoz infrastructure configs
- Added 3 SigNoz services to `render.yaml`: ClickHouse (private), OTel Collector (web), Dashboard (web)
- Created Dockerfiles for each service with pinned versions (ClickHouse 24.1, OTel Collector 0.102.12, Query Service 0.53.0)
- Created `otel-collector-config.yaml` with OTLP receivers, batch processors, and ClickHouse exporters for traces/metrics/logs
- Created ClickHouse config with memory limits tuned for Render Starter+ (2GB RAM)
- Added 4 local SigNoz services to `docker-compose.yml` (ClickHouse, OTel Collector, Query Service, Frontend)
- Updated `.env.example` with OTel environment variables
- Created `infra/signoz/README.md` with full deployment documentation

## Files Changed
- `infra/signoz/otel-collector-config.yaml` (new)
- `infra/signoz/clickhouse-config.xml` (new)
- `infra/signoz/clickhouse-users.xml` (new)
- `infra/signoz/Dockerfile.clickhouse` (new)
- `infra/signoz/Dockerfile.otel-collector` (new)
- `infra/signoz/Dockerfile.query-service` (new)
- `infra/signoz/README.md` (new)
- `render.yaml` (added 3 SigNoz services)
- `docker-compose.yml` (added 4 local SigNoz services + volume)
- `.env.example` (added OTel env vars)

## Key Decisions
- **Pinned Docker image versions** (not `:latest`) for reproducibility: ClickHouse 24.1, OTel Collector 0.102.12, Query Service 0.53.0
- **OTel Collector CORS** configured to allow browser-direct OTLP from fleetneuron.ai domains
- **ClickHouse memory limit** set to 80% of RAM ratio for Render's 2GB Starter+ plan
- **Local dev** uses separate Frontend container (port 3301) for full SigNoz UI access
- **Render Blueprint** uses `sync: false` for CLICKHOUSE_PASSWORD to keep it secret

## Deployment Handoff
**New Render Services to Create (in order):**
1. `signoz-clickhouse` — Private Service, Docker, 2GB RAM, 10GB persistent disk
2. `signoz-otel-collector` — Web Service, Docker, 512MB RAM
3. `signoz-dashboard` — Web Service, Docker, 1GB RAM

**Environment Variables (set in Render Dashboard):**
- `CLICKHOUSE_PASSWORD` — set same secure password on all 3 SigNoz services

**Post-Deploy Steps:**
1. Verify ClickHouse is healthy (check Render logs for startup)
2. Verify OTel Collector health at `/health` endpoint (port 13133)
3. Access SigNoz Dashboard and create admin account
4. Note the OTel Collector public URL for FleetNeuron service configuration

**No DB migrations needed.** No existing services affected until FN-625 wires them up.

## Review Findings
_To be filled by Code Reviewer_

## QA Findings
_To be filled by QA_

## Open Items
- None — self-hosted SigNoz eliminates need for external credentials
