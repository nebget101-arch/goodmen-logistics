# FN-1130: S6 — Role-based personalization for Control Center

## Requirement
Control Center layout adapts per role: Dispatcher sees load-focused cards; Safety sees compliance/HOS cards; Maintenance sees vehicle cards; Owner sees financial summary. User can drag-reorder within their role default.

## Acceptance Criteria
- [ ] Role default loaded from `/api/users/me` profile
- [ ] Drag-reorder persists to `user_dashboard_layouts` table
- [ ] Reset-to-default button restores role layout
- [ ] All 4 role defaults specified in story doc

## Dependencies
- Blocked by FN-1124, FN-1126, FN-1128, FN-1129 (consumes their components)

## Agent
frontend (story owner; subtasks span frontend, backend, database, qa)

## Integration Branch
`integration/FN-1130`

## Subtasks
| Key     | Summary                                       | Agent    | Branch                                  | Files Touched                                                                                                              | Status                   |
|---------|-----------------------------------------------|----------|-----------------------------------------|----------------------------------------------------------------------------------------------------------------------------|--------------------------|
| FN-1171 | Role-based layout + drag-reorder + reset      | frontend | `frontend/FN-1171/role-layout`          | `frontend/src/app/components/control-center/control-center.component.{ts,html,scss,spec.ts}`, `frontend/src/app/components/control-center/role-layouts.ts`, `frontend/src/app/services/dashboard-layout.service.ts`, `frontend/src/app/app.module.ts` (+1 import), `frontend/src/app/components/dashboard/dashboard.component.html` (replace 4 widget tags with `<app-control-center>`) | Done |
| FN-1172 | Layout persistence endpoints                  | backend  | `backend/FN-1172/layout-endpoints`      | `backend/microservices/auth-users-service/routes/dashboard-layout.{js,test.js}`, `backend/microservices/auth-users-service/services/layout-store.js`, `backend/microservices/auth-users-service/server.js`, `backend/microservices/auth-users-service/package.json` | Done                     |
| FN-1174 | `user_dashboard_layouts` migration            | database | `database/FN-1174/user-dashboard-layouts` | `backend/packages/goodmen-database/migrations/20260504200000_create_user_dashboard_layouts.js`                            | Done                     |
| FN-1175 | Validate role-based defaults + reorder + reset | qa       | _manual_                                | `docs/stories/evidence/FN-1130/`                                                                                           | Backlog (blocked) |

## Role Defaults

**Dispatcher**: Daily Briefing → Smart Alerts (load) → Predictive Insights (load volume + on-time %) → Quick Actions
**Safety**: Daily Briefing → Smart Alerts (HOS + fatigue) → Predictive Insights (compliance) → Quick Actions
**Maintenance**: Daily Briefing → Smart Alerts (inspection + maintenance) → Predictive Insights (maintenance windows + fuel) → Quick Actions
**Owner**: Daily Briefing → Predictive Insights (all 4) → Smart Alerts (top severity)

## Implementation Summary

### FN-1171 (subtask) — Frontend control-center grid
- New standalone `ControlCenterComponent` renders the 4 widgets (`daily-briefing`, `smart-alerts`, `predictive-insights`, `quick-actions`) in an ordered `cdkDropList`.
- On init, calls `GET /api/users/me/dashboard-layout` (FN-1172). The server always returns 200 with `{ data: { layout: { cards }, is_default, role } }`, so the FE just renders `cards` and uses `role` (already normalized server-side) for the Quick Actions defaults. When the call fails, the FE falls back to a client-side mirror of the same role-default mapping.
- Drag-reorder triggers `PUT /api/users/me/dashboard-layout` with body `{ cards: [...] }`. Failures surface a non-blocking error message; the local order is preserved so the user doesn't lose their drag.
- "Reset to default" button calls `DELETE /api/users/me/dashboard-layout` and applies the role default returned in the DELETE response (`is_default=true`).
- Top-level Quick Actions card uses the existing `QuickActionsComponent` (FN-1169) with role-specific shortcuts (e.g. dispatcher: Create load / Dispatch board / Drivers). The card-level Quick Actions embedded inside Smart Alerts and Predictive Insights rows (per FN-1169) are unchanged.
- Wired into the existing `/dashboard` route: dashboard.component.html's hand-placed `<app-daily-briefing>` / `<app-predictive-insights>` / `<app-smart-alerts>` tags replaced with a single `<app-control-center>`. The legacy fleet-metrics grid below stays.
- Unit spec covers: GET returns role default → renders default; saved layout used and sanitized (drops unknown ids, dedupes); BE role aliases (e.g. `admin`) normalized to `owner`; PUT body shape `{ cards: […] }`; no PUT when drop index unchanged; DELETE applies returned default; client fallback when GET errors; error messaging on save / reset failure.

### FN-1172 (subtask)
Added the `auth-users-service` REST surface for the Control Center layout: `GET/PUT/DELETE /api/users/me/dashboard-layout`, backed by `user_dashboard_layouts` (FN-1174). The store falls back to a role-keyed default layout when no row exists, so a freshly-onboarded user immediately sees a sensible Control Center without the frontend having to seed anything. DELETE provides the reset-to-default action consumed by FN-1171.

- `services/layout-store.js` exports `createLayoutStore({ knex })` plus pure helpers (`getRoleDefault`, `normalizeRoleKey`, `ROLE_DEFAULTS`, `DEFAULT_ROLE_KEY`) so the frontend/QA can mirror the role mapping without a network round-trip if needed.
- Role aliases collapse legacy JWT codes to four canonical keys: `dispatcher` (`dispatch`, `dispatcher`), `safety` (`safety`, `safety_manager`), `maintenance` (`maintenance`, `mechanic`, `technician`), `owner` (`owner`, `admin`, `super_admin`, `platform_admin`, `accounting`). Unknown roles fall back to `dispatcher`.
- Upsert uses `INSERT … ON CONFLICT (user_id) DO UPDATE` so PUT is idempotent and matches the FN-1174 PK.
- Payload size ceiling: 16 KB (smaller than `user-preferences`' 32 KB because the layout is just a card-order list, not a full preference graph).
- Tests use `node:test` and an in-memory store stub — no Postgres needed in CI. Mirrors the convention from `packages/goodmen-shared/routes/user-preferences.test.js`.

### FN-1174 (subtask)
Added knex migration creating `user_dashboard_layouts` to back the per-user layout persistence consumed by FN-1172 (layout endpoints) and FN-1171 (Control Center drag-reorder + reset).

- One row per user (`user_id` PK), so reads/writes are point lookups, not range scans.
- `tenant_id` is a NOT NULL FK + secondary index for tenant-scoped reads and ON DELETE CASCADE cleanup paths.
- `layout_json jsonb` defaults to `'{}'` so GETs before the user has reordered anything return an empty object instead of NULL — backend can fall back to the role default cleanly.
- FKs cascade on user/tenant delete, matching the surrounding `users` / `tenants` cleanup contract used by `user_alert_dismissals` (FN-1165) and `user_notifications` (FN-507).

## Files Changed

### FN-1171 (subtask)
- `frontend/src/app/components/control-center/control-center.component.ts` (new)
- `frontend/src/app/components/control-center/control-center.component.html` (new)
- `frontend/src/app/components/control-center/control-center.component.scss` (new)
- `frontend/src/app/components/control-center/control-center.component.spec.ts` (new)
- `frontend/src/app/components/control-center/role-layouts.ts` (new)
- `frontend/src/app/services/dashboard-layout.service.ts` (new)
- `frontend/src/app/app.module.ts` (add `ControlCenterComponent` to standalone `imports`)
- `frontend/src/app/components/dashboard/dashboard.component.html` (replace 4 widget tags with `<app-control-center>`)

### FN-1172 (subtask)
- `backend/microservices/auth-users-service/routes/dashboard-layout.js` (new)
- `backend/microservices/auth-users-service/routes/dashboard-layout.test.js` (new)
- `backend/microservices/auth-users-service/services/layout-store.js` (new)
- `backend/microservices/auth-users-service/server.js` (mounted new router before the `/api/users` shared router)
- `backend/microservices/auth-users-service/package.json` (added `test` script)

### FN-1174 (subtask)
- `backend/packages/goodmen-database/migrations/20260504200000_create_user_dashboard_layouts.js` (new)

## Key Decisions

### FN-1171
- **Standalone component, OnPush.** Matches the convention established by `DailyBriefingComponent` / `SmartAlertsComponent` / `PredictiveInsightsComponent` / `QuickActionsComponent`.
- **Server is authoritative for role + default.** The FE consumes the BE-normalized `data.role` and the BE-provided `data.layout.cards` directly, instead of fetching `/users/me` and mapping role → default on the client. The client-side `role-layouts.ts` mirror exists only for the offline / GET-failure fallback path and stays in sync with the server's role aliases (`admin`, `super_admin`, etc. → `owner`).
- **`{ cards }` payload shape, not `{ widgets }`.** Adopted FN-1172's wire shape; `cards` is also the name used by `layout-store.js`'s `ROLE_DEFAULTS`.
- **Sanitization on both ingress and egress.** Server-persisted `cards` arrays are passed through `sanitizeLayout` before render (drop unknown ids, dedupe, preserve order). The same function runs before PUT — a forward-compat hedge if the FE list of widget ids ever shrinks but the persisted blob hasn't been migrated.
- **Reset is server-driven.** `DELETE` is the canonical "use my role default" signal; the FE applies the response (which already contains the role default with `is_default=true`) rather than recomputing locally, so server-side updates to the role default propagate without a FE redeploy.
- **Top-level Quick Actions ≠ embedded Quick Actions.** The role-default `quick-actions` card surfaces *navigation* shortcuts (role-specific routes); the per-row Quick Actions inside Smart Alerts / Predictive Insights remain *contextual* actions on a specific alert/insight row (FN-1169). Both can co-exist.
- **No widget-level filtering of Smart Alerts / Predictive Insights yet.** The role defaults reference filtered views (e.g. "Smart Alerts (load)", "Predictive Insights (compliance)"). FN-1171 ships the role-aware *order* and *visibility* of cards; per-role filtering of card contents is out of scope and would be a follow-up against the alerts/insights components themselves.

### FN-1172 (subtask)
- **Service-local route, not shared router** — the ticket scopes the files to `auth-users-service/routes/dashboard-layout.js`. Mounting it directly on `auth-users-service/server.js` (rather than adding `/me/dashboard-layout` to `packages/goodmen-shared/routes/users.js`) keeps the layout feature contained to the one microservice that owns user profile state, and matches the pattern set by `routes/billing.js` for service-specific concerns.
- **Mount BEFORE `/api/users`** — Express forwards `/api/users/me/dashboard-layout` to `usersRouter` first if mounted later; the shared router doesn't define that path so it would fall through, but mounting the more-specific path first avoids any chance of regression if the shared router later gains a wildcard `/me/*` route.
- **Role key normalization in the store, not the route** — the store is the single source of truth for the role→default-layout mapping. The route just reads `req.user.role` from the JWT and forwards it. Frontend (FN-1171) re-implements the same mapping for offline / first-paint use, but the backend default is authoritative.
- **`DELETE` (not `POST /reset`) for reset-to-default** — REST-clean and matches the table semantics: removing the row is exactly what "reset" means. The endpoint returns the role default in the response so the frontend doesn't have to issue a follow-up GET.
- **16 KB payload cap** — sized for the actual data (a small `cards: []` list); rejects accidental blob uploads loudly without becoming a constraint to design around.

### FN-1174 (subtask)
- **`user_id` as PK, not a surrogate `id` UUID** — FN-1172 will upsert per user (one layout per user). A composite/unique constraint on `user_id` would force a separate PK; using `user_id` as PK is simpler and what the ticket specifies.
- **Skipped the README append** — the ticket lists `backend/migrations/README.md` as a touched file, but the actual migrations directory is `backend/packages/goodmen-database/migrations/` and there is no per-folder migrations README in this codebase (recent migrations like FN-1165, FN-507 don't update one). Following established convention.
- **`layout_json` default `'{}'`, not NULL** — keeps the FN-1172 endpoint contract trivial (always returns a JSON object).

## Deployment Handoff

### FN-1171 (frontend only)
- **Service to redeploy:** `fleetneuron-logistics-ui` (Angular frontend on Render). No other services touched by this subtask.
- **DB migrations:** None directly (the FE consumes FN-1174's `user_dashboard_layouts` table via FN-1172's endpoints, which both ship in the same `integration/FN-1130 → dev` PR).
- **BE dependency:** FN-1172 (`/api/users/me/dashboard-layout` endpoints on `fleetneuron-auth-users-service`). If the FE somehow ships ahead of the BE, GET errors fall back to the client role default and drag/reset surface a non-blocking error — acceptable graceful degradation.
- **Env / config / cache / flags:** None.
- **Deploy order at story merge:** DB (FN-1174 migration) → BE (`fleetneuron-auth-users-service`) → FE (`fleetneuron-logistics-ui`). The story-level PR bundles all three.
- **Post-deploy verification:** Log in as four different role users, navigate to `/dashboard`, confirm the role-default card order matches the table in this doc, drag a card to a new position and reload, click "Reset to default" and confirm the role default order is restored.

### FN-1172 (subtask)
- **Services to redeploy:** `goodmen-auth-users-service` (Render). Reference `.agent/docs/render_services.md` for the canonical service name.
- **Order:** Deploy *after* FN-1174's migration has run on dev (the route reads/writes `user_dashboard_layouts`). On the integration branch the migration commit (4c47e07) precedes this one, so a single `migrate:latest` + service restart on dev is sufficient.
- **Migrations:** None added by this subtask. The endpoint depends on `20260504200000_create_user_dashboard_layouts.js` (FN-1174).
- **Env / config / flags / cache:** None changed. No new env vars.
- **Post-deploy verification:**
  1. `curl -H "Authorization: Bearer <jwt>" https://<auth-users-service>/api/users/me/dashboard-layout` → 200, `data.is_default=true`, `data.role` matches the JWT role, `data.layout.cards` is the role default.
  2. `curl -X PUT -H "Authorization: Bearer <jwt>" -H "Content-Type: application/json" -d '{"cards":["daily-briefing"]}' …/dashboard-layout` → 200, `data.is_default=false`.
  3. Subsequent GET returns the persisted layout (not the role default).
  4. `curl -X DELETE …/dashboard-layout` → 200, response shape returns role default.
- **Rollback:** Roll back the service to the prior image; the new endpoint is additive — no existing route paths are altered. Existing data in `user_dashboard_layouts` is harmless once the consuming endpoint is gone (FN-1174 migration can stay).

### FN-1174 (subtask)
- **Migration to run:** `20260504200000_create_user_dashboard_layouts.js` (knex `migrate:latest` from backend, or `migrate:prod` on deploy).
- **Order:** Must run before FN-1172 (layout endpoints) deploys, because the endpoint reads/writes this table.
- **Services affected:** None at deploy time (migration only). The service that consumes the table is `auth-users-service` once FN-1172 ships.
- **Rollback:** `migrate:rollback` drops the table cleanly (`DROP TABLE IF EXISTS`).
- **Env / config / cache / flags:** No changes.

## Review Findings
_To be filled by Code Reviewer_

## QA Findings
_To be filled by QA_

## Open Items
- None
