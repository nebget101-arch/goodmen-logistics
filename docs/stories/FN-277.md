# FN-277: Ensure all date pickers across app use ai-datepicker-overlay dark theme calendar styling

## Status: In Progress

## Subtasks

| Key | Summary | Status | Branch |
|-----|---------|--------|--------|
| FN-278 | Audit all pages and restore app-ai-date-picker where native date inputs were used | Done | `agent/frontend/FN-278/restore-ai-datepicker` |

### FN-278 (subtask)

**Implementation Summary:**
Audited all templates for native `<input type="date">` usage and restored `app-ai-date-picker` in the dark-themed admin UI pages.

**Audit Results:**
- `vehicle-form.component.html` — 4 native date inputs replaced with `app-ai-date-picker` (Inspection Expiry, Registration Expiry, Insurance Expiry, Next PM Due)
- `drivers.component.html` — 1 DQF inline date input replaced with `app-ai-date-picker` (inline mode)
- `employment-application.component.html` — 6 native date inputs (public onboarding form, different UI context — left as-is)
- `onboarding-packet.component.html` — 8 native date inputs (public onboarding form — left as-is)
- `consent-form.component.html` — 1 native date input (part of onboarding — left as-is)

**Checklist verified — already using `app-ai-date-picker`:**
- Trailer form (Purchase Date, Lease Date)
- Loads Dashboard (Pickup, Delivery)
- Dispatch Drivers
- Invoices (list + detail)
- Settlements (wizard + deductions)
- Safety (reports, claims, accident detail)
- Reports (filters)
- Fuel transactions
- Customer management
- Work Orders
- Lease/Financing
- Compliance (IFTA quarterly)
- Inventory reports

**Files Changed:**
- `frontend/src/app/components/vehicles/vehicle-form/vehicle-form.component.html`
- `frontend/src/app/components/drivers/drivers.component.html`

**Key Decisions:**
- Onboarding forms (`employment-application`, `onboarding-packet`, `consent-form`) were left with native date inputs because they are public-facing driver forms with their own light-themed styling system (`ea-input`, `form-input` classes), not part of the dark admin UI.
- DQF date input uses `inline` mode to maintain compact row layout.

## Deployment Handoff
- **Services**: `fn-frontend` on Render
- **Migrations**: None
- **Env vars**: None
- **Post-deploy**: Verify calendar popup on Vehicles → Add/Edit Truck form (Compliance section) and Drivers → DQF → clearinghouse date fields
