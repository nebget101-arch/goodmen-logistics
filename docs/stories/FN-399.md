# FN-399: [Bug] Reports Center shows $0.00 across all tabs

## Overview
Reports Center shows zero revenue/expenses due to operating entity context mismatch, missing completed_date auto-population, and no zero-data UX warning.

## Subtasks

| Key | Summary | Agent | Branch | Status |
|-----|---------|-------|--------|--------|
| FN-400 | Fix operating entity context filtering in report queries | backend | — | Selected for Dev |
| FN-401 | Auto-set completed_date on DELIVERED/COMPLETED transition | backend | — | Selected for Dev |
| FN-402 | Add zero-data warning to Reports when all KPIs are $0.00 | frontend | `agent/frontend/FN-402/reports-zero-data-warning` | Done |
| FN-403 | QA: Validate Reports Center data across all 10 tabs | qa | — | Backlog |

### FN-402 (subtask) — Frontend: Add zero-data warning banner

**Changes:**
- Added `get isAllZeroData()` computed property that returns true when all KPI card values are 0/falsy AND `rows.length === 0`
- Added amber warning banner to the HTML between the loading state and entity subtotals section
- Banner message: "No revenue or expense data found for the selected filters. Check your date range and company selection."
- Styled with amber/warning colors matching the dashboard degraded banner pattern (`border: rgba(251, 146, 60, 0.6)`, radial gradient background)

**Files Changed:**
- `frontend/src/app/reports/pages/report-view/report-view.component.ts` — `isAllZeroData` getter
- `frontend/src/app/reports/pages/report-view/report-view.component.html` — warning banner
- `frontend/src/app/reports/pages/report-view/report-view.component.css` — `.reports-zero-warning` styles

## Deployment Handoff

### Services to redeploy
- `fleet-neuron-frontend` (Angular frontend)

### DB migrations
- None

### Environment/config changes
- None

### Post-deploy verification
1. Navigate to Reports with a date range that has no completed loads
2. Verify amber warning banner appears when all KPI cards show $0.00
3. Verify banner disappears when any KPI has a non-zero value
