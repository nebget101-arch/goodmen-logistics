# FN-552: Scheduled Deduction Smart Defaults

## Requirement
The New Scheduled Deduction dialog should auto-populate amounts and defaults based on the driver's expense responsibility configuration.

## Acceptance Criteria
- [ ] Selecting fixed category (Insurance, ELD, Trailer Rent) auto-fills amount from driver's expense responsibility config
- [ ] Amount auto-fills with driver's configured dollar amount when Apply To = Driver
- [ ] Amount auto-fills with EO's configured dollar amount when Apply To = Equipment Owner
- [ ] Auto-populated amount is editable (user can override)
- [ ] No auto-fill if no expense responsibility config exists (defaults to $0.00)
- [ ] Selecting variable category (Fuel, Tolls, Repairs) auto-sets Amount Type to "Percentage"
- [ ] Percentage auto-fills from expense responsibility split %
- [ ] Label shows "Split %" instead of "Amount $" when Amount Type is Percentage
- [ ] Switching Apply To updates pre-filled amount/percentage
- [ ] If user has manually edited amount, prompt "Reset to configured amount?" before overwriting

## Subtasks

| Key | Summary | Agent | Branch | Status |
|-----|---------|-------|--------|--------|
| FN-561 | Fixed expense auto-populate (Insurance, ELD, Trailer Rent) | frontend | `frontend/FN-561/deduction-fixed-autofill` | Done |
| FN-562 | Variable expense defaults to Percentage + auto-fill split % | frontend | `frontend/FN-562/deduction-variable-percentage-defaults` | Selected for Dev |
| FN-563 | Apply To change handler with dirty-check prompt | frontend | `frontend/FN-563/deduction-apply-to-handler` | Done |
| FN-564 | QA: Validate all auto-populate behaviors | qa | _manual_ | Backlog |

## Implementation Summary

### FN-561 (frontend)
- Added `expenseProfile: any = null` property to store the raw expense responsibility API response
- When loading a driver's expense responsibility (`getExpenseResponsibility`), the full response is stored in `this.expenseProfile`
- `resetForm()` clears `this.expenseProfile = null`
- Extended `onRecurringExpenseCategoryChange()`: when category is `insurance`, `eld`, or `trailerRent`, auto-fills `newRecurringDeduction.amount` from `expenseProfile.driver_fixed_amount` (target=primary) or `expenseProfile.owner_fixed_amount` (target=additional)
- If no profile or amount is 0, amount stays null (shows placeholder $0.00)

### FN-563 (frontend)
- Added `deductionAmountDirty = false` flag; set to `true` via `(input)="onDeductionAmountInput()"` on the amount field
- `resetRecurringDeductionDraft()` resets `deductionAmountDirty = false`
- `onRecurringExpenseCategoryChange()` resets `deductionAmountDirty = false` after auto-fill so fresh auto-fills don't trigger the prompt
- `onDeductionTargetChange()`: when Apply To changes, computes the configured amount for the current category (fixed: `driver_fixed_amount`/`owner_fixed_amount`; variable: `driver_percentage`/`100 - driver_percentage`). If `deductionAmountDirty`, shows `confirm('Reset to configured amount?')`; user can accept (reset) or cancel (keep override). If not dirty, silently updates.
- Target select wired: `(ngModelChange)="onDeductionTargetChange()"`

## Files Changed
| File | Change |
|------|--------|
| `frontend/src/app/components/dispatch-drivers/dispatch-drivers.component.ts` | Add `expenseProfile`, `deductionAmountDirty`, store profile on load, auto-fill + dirty tracking methods |
| `frontend/src/app/components/dispatch-drivers/dispatch-drivers.component.html` | Wire `(ngModelChange)="onDeductionTargetChange()"` on target select; `(input)="onDeductionAmountInput()"` on amount input |

## Deployment Notes
- **Services**: `fleetneuron-logistics-ui` (frontend only)
- **Migrations**: None
- **Env/config**: None
- **Post-deploy verification**: Open driver modal → Deductions tab → New Deduction → select Insurance → verify Amount auto-fills from configured driver/owner fixed amount

## Review Findings
_To be filled by Code Reviewer_

## QA Findings
_To be filled by QA_
