# FN-552: Scheduled Deduction smart defaults — auto-populate amounts from expense responsibility config

## Requirement
The New Scheduled Deduction dialog should auto-populate amounts and defaults based on the driver's expense responsibility configuration:

- **Fixed categories** (Insurance, ELD, Trailer Rent): auto-fill dollar amount from expense responsibility config for the selected "Apply to" party
- **Variable categories** (Fuel, Tolls, Repairs): auto-set Amount Type to "Percentage" and pre-fill split % from expense responsibility config
- **Apply To change**: amount/percentage updates to match the newly selected party's configured value; if user has manually overridden, prompt before resetting

## Acceptance Criteria
### Fixed expense auto-populate (Insurance, ELD, Trailer Rent)
- [x] Selecting category reads driver's expense responsibility config for that category
- [x] Amount auto-fills with driver's configured dollar amount when "Apply to" = Driver
- [x] Amount auto-fills with EO's configured dollar amount when "Apply to" = Equipment Owner
- [x] Auto-populated amount is editable (user can override)
- [x] No auto-fill if no expense responsibility config exists for that category (defaults to $0.00)

### Variable expense defaults (Fuel, Tolls, Repairs)
- [x] Selecting variable category auto-sets Amount Type to "Percentage"
- [x] Percentage auto-fills from expense responsibility split (e.g., Fuel Driver 50%)
- [x] Label shows "Split %" instead of "Amount $" when Amount Type is Percentage
- [x] Code comment added: variable deductions will be superseded by actual transaction data in Settlement V2

### Apply To change handler
- [ ] Switching "Apply to" between Driver and Equipment Owner updates pre-filled amount/percentage
- [ ] For fixed: switches between driver's dollar amount and EO's dollar amount
- [ ] For variable: switches between driver's % and EO's %
- [ ] If user has manually edited the amount, prompt "Reset to configured amount?" before overwriting

## Subtasks
| Key | Summary | Agent | Branch | Status |
|-----|---------|-------|--------|--------|
| ✅ FN-561 | Frontend: auto-populate fixed expense amounts | frontend | `frontend/FN-561/deduction-fixed-autofill` | Done |
| ✅ FN-562 | Frontend: variable expense % defaults + Split % label | frontend | `frontend/FN-562/deduction-variable-percentage-defaults` | Done |
| FN-563 | Frontend: Apply To change handler with confirm prompt | frontend | `frontend/FN-563/deduction-apply-to-handler` | In Progress |
| FN-564 | QA: Validate all smart defaults | qa | _manual_ | Backlog |

## Implementation Summary

### FN-561 (fixed auto-populate)
_Details in FN-561 branch._

### FN-562 (variable % defaults)
Extended `onRecurringExpenseCategoryChange()` in `dispatch-drivers.component.ts`:

- Checks if selected category is in `variableExpenseKeys` (fuel, tolls, repairs)
- If variable: sets `amount_type = 'percentage'`
- Auto-fills `amount` from `sharedExpensePercentages[category]` (driver share) for primary payee target, or `100 - driverShare` for additional payee target
- Falls back to `50` if no percentage is configured for the category

HTML: changed "Amount" label to `{{ newRecurringDeduction.amount_type === 'percentage' ? 'Split %' : 'Amount' }}`

Code comment added in `.ts` explaining Settlement V2 supersedes Fuel/Tolls deductions.

## Files Changed
| File | Change |
|------|--------|
| `frontend/src/app/components/dispatch-drivers/dispatch-drivers.component.ts` | `onRecurringExpenseCategoryChange()` — variable category detection, `amount_type` default, auto-fill split % |
| `frontend/src/app/components/dispatch-drivers/dispatch-drivers.component.html` | Dynamic "Split %" / "Amount" label on deduction amount field |

## Key Decisions
- `sharedExpensePercentages[category]` (defaulting to 50) is used as the driver share — this is the same source the expense config panel uses
- EO share = `100 - driverShare` (complement of driver share)
- No explicit confirmation prompt for amount override in FN-562 scope; that is handled by FN-563

## Deployment Handoff
- **Services**: Angular frontend only — redeploy after merge (auto-deploy via Render static site or CDN)
- **Migrations**: None
- **Env/config**: None
- **Post-deploy verification**:
  - Open Dispatch → edit any driver with expense responsibility configured
  - Open New Scheduled Deduction → select Fuel → confirm Amount Type = Percentage and amount pre-fills with driver's split %
  - Confirm label reads "Split %" not "Amount"

## Review Findings
_To be filled by Code Reviewer_

## QA Findings
_To be filled by QA_
