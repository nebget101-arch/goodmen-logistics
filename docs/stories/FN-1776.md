# FN-1776: Vehicle Tracking — show city & state in the position section

## Requirement
The Vehicle Tracking Position section shows only raw `lat, lng`. Show a human-readable **City, State** for the vehicle's current position.

The telemetry contract (`GET /api/vehicles/:id/telemetry`, FN-1750/FN-1752) returns coordinates only, and the mock generator scatters vehicles at random lat/lng inside a US bounding box — those points aren't real cities. Fix: seed each vehicle's position from a catalog of real US cities (deterministic per `unit_number`/`id`) and add `city`/`state` to the contract, so coordinates and city/state stay consistent. When real telematics lands, the provider/ingest populates the same fields (reverse-geocoded at ingest) — no frontend rework.

## Contract change
Telemetry payload (single + fleet) gains:
```
city:  string   // e.g. "Dallas"
state: string   // 2-letter, e.g. "TX"
```

## Acceptance Criteria
- [x] Telemetry payload includes `city` + `state` for every vehicle; deterministic and consistent with `latitude`/`longitude`. *(FN-1777)*
- [x] Position section shows "City, ST"; raw coordinates + Google Maps link (FN-1768) remain. *(FN-1778)*
- [x] Graceful fallback when city/state absent — coords only, no "undefined, ". *(FN-1778)*
- [x] AI dark-theme tokens only; no new hex; builds pass. *(FN-1778 — frontend compiles cleanly; pre-existing bundle-budget overage from maplibre-gl is unrelated.)*

## Dependencies
- Frontend subtask (FN-1778) **blocked by FN-1768** (edits the same Position block — Maps link). FN-1768 is merged to `dev`, so the integration branch (off `dev`) already carries the restructured Position `<dd>` + Maps link.
- QA (FN-1779) blocked by FN-1777 + FN-1778.

## Agent
backend (story) — implemented across backend + frontend + qa subtasks

## Integration Branch
`integration/FN-1776` (created from `origin/dev`; subtasks branch off this; story PR merges to `dev`). Shape C (multi-agent: backend + frontend).

## Subtasks
| Key | Summary | Agent | Branch | Files Touched | Status |
|-----|---------|-------|--------|---------------|--------|
| FN-1777 | Real-city catalog + `city`/`state` in mock telemetry | backend | `backend/FN-1777/telemetry-city-state` | `backend/packages/goodmen-shared/routes/vehicle-telemetry.js(.test.js)` | Done |
| FN-1778 | Render City, ST in Position section | frontend | `frontend/FN-1778/position-city-state` | `frontend/src/app/services/api.service.ts`, `components/vehicle-tracking/**` | Done |
| FN-1779 | QA validation | qa | _manual_ | `docs/stories/evidence/FN-1776/**` | Blocked (FN-1777, FN-1778) → ready (both done) |

### FN-1777 (subtask) — Real-city catalog + city/state in mock telemetry
- Added a `CITY_CATALOG` constant (42 real US cities, `{ city, state, lat, lng }`, 2-letter states) to `vehicle-telemetry.js`, all chosen to sit inside the historical continental-US box (lat 31–44 N, lng -118 to -68 W).
- `buildMockTelemetry()` now deterministically picks a catalog city via the existing seeded `rand()` (so a vehicle always lands on the same city), applies a ±0.03° (~2 mi) jitter to the city's lat/lng, and sets `latitude`/`longitude` from the jittered coords. `city` and `state` are added to the returned payload — consistent with the coordinates.
- Both endpoints (`GET /api/vehicles/:id/telemetry` single + `GET /api/fleet/telemetry`) inherit the change since both go through `buildMockTelemetry`.
- Updated the contract doc comment block and both `@openapi` descriptions to list `city`/`state`.
- Determinism preserved: same `unit_number`/`id` ⇒ same city/state/coords across reloads.
- Tests (`vehicle-telemetry.test.js`): shape test asserts `city` (non-empty) + `state` (2-letter); determinism test asserts city/state stability; new test asserts every vehicle maps to a real catalog city with coords inside the US box and within the jitter bound. All 9 tests pass under `node --test` (Node 20).

### FN-1778 (subtask) — frontend
**Branch:** `frontend/FN-1778/position-city-state` (off `origin/integration/FN-1776`)

**What changed**
- `frontend/src/app/services/api.service.ts` — `VehicleTelemetry` interface gains optional `city?: string` and `state?: string` to match the FN-1776/FN-1777 contract. Optional so older/partial payloads still typecheck.
- `vehicle-tracking.component.ts` — new `locationLabel()` helper returns `"City, ST"` only when **both** `city` and `state` are present (trimmed), else `''`. Never emits `"undefined, "` or a dangling comma.
- `vehicle-tracking.component.html` — Position `<dd>` now stacks the City, ST label (primary line) over the existing FN-1768 Maps link + raw coordinates (secondary sub-line). When `locationLabel()` is empty the city line is omitted (`*ngIf`) and coordinates render alone. The Maps link and `lat, lng` are preserved unchanged.
- `vehicle-tracking.component.scss` — added `.vt-position` (flex column), `.vt-city` (reuses the existing `rgba(248,250,252,1)` primary-text value), `.vt-coords` (smaller, lighter secondary line). No new hex / new color tokens introduced.

**Build:** `ng build --configuration production` — TypeScript compiles with zero errors. The only build failure is the pre-existing initial-bundle budget overage (3.43 MB, driven by `maplibre-gl`/`leaflet` from the tracking-map component), which predates this change and is unaffected by the ~39 lines of markup/styles.

**Graceful fallback:** verified by code — `locationLabel()` short-circuits to `''` unless both fields are truthy after trim, and the template only renders the city line when the label is non-empty.

## Implementation Summary
Backend (FN-1777) seeds each vehicle from a 42-city real-US catalog and adds deterministic, coordinate-consistent `city`/`state` to the single + fleet telemetry payloads. Frontend (FN-1778) extends the `VehicleTelemetry` type and renders "City, ST" above the preserved FN-1768 coordinates + Maps link, degrading to coords-only when the fields are absent. Both subtasks are integrated into `integration/FN-1776`; QA (FN-1779) is now unblocked.

## Files Changed
- `backend/packages/goodmen-shared/routes/vehicle-telemetry.js` (CITY_CATALOG, position logic, payload fields, contract/OpenAPI docs, export)
- `backend/packages/goodmen-shared/routes/vehicle-telemetry.test.js` (city/state assertions + new catalog test)
- `frontend/src/app/services/api.service.ts`
- `frontend/src/app/components/vehicle-tracking/vehicle-tracking.component.ts`
- `frontend/src/app/components/vehicle-tracking/vehicle-tracking.component.html`
- `frontend/src/app/components/vehicle-tracking/vehicle-tracking.component.scss`

## Key Decisions
- City catalog restricted to the existing lat/lng bounding box so the FN-1752 coordinate-range contract (and its tests) still hold after the change.
- Jitter is derived from the seeded `rand()` sequence (not `Math.random()`), so positions stay deterministic per vehicle while looking "live".
- No external reverse-geocoding — the mock owns both the coords and the label, guaranteeing they always agree.
- `city`/`state` typed **optional** on the frontend so the contract change is backward-compatible and the FE renders before FN-1777 deploys.
- City, ST is the **primary** line with coordinates demoted to a secondary sub-line — keeps the human-readable label prominent while retaining the FN-1768 Maps deep-link and exact coordinates.
- Reused existing component rgba values rather than adding tokens/hex, per the AI dark-theme constraint.

## Deployment Handoff
- **Backend service to redeploy:** `fleetneuron-vehicles-maintenance-service` (mounts `@goodmen/shared/routes/vehicle-telemetry`). Only service requiring this route.
- **Frontend:** redeploy the Angular SPA (static build) to ship the Position-section UI.
- **DB migrations:** none (no DB/schema change — mock generator only; there is intentionally no telemetry table).
- **Env/config changes:** none.
- **Feature flags / cache:** none.
- **Deploy order:** backend (FN-1777) first so the contract field exists, then frontend (FN-1778). They are independent in practice — the FE degrades gracefully when `city`/`state` are absent — but backend-first means the feature is fully visible the moment the FE lands.
- **Post-deploy verification:**
  - `GET /api/vehicles/:id/telemetry` and `GET /api/fleet/telemetry` return non-empty `city` and 2-letter `state`; re-fetching the same vehicle yields identical city/state/coords; the Maps link for the returned coords lands on the named city.
  - Open Vehicle Tracking → select a truck → confirm the Position section shows "City, ST" above the coordinates, the Google Maps link still opens the exact point, and trucks without city/state show coordinates only (no "undefined, ").

## Review Findings
_To be filled by Code Reviewer_

## QA Findings
_To be filled by QA_

## Open Items
- None
