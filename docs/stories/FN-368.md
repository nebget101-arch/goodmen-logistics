# FN-368: Approve button should save all form changes before transitioning draft to DISPATCHED

## Summary
When reviewing a draft load, clicking Approve should save all pending form changes (rate, stops, driver, broker, notes, etc.) before transitioning status to DISPATCHED, in a single atomic transaction.

## Subtasks

| Key | Summary | Agent | Status | Branch |
|-----|---------|-------|--------|--------|
| FN-372 | Update approve-draft endpoint to accept and persist form changes | backend | Done | `backend/FN-372/approve-saves-changes` |
| FN-373 | Send current form state when calling approve on draft load | frontend | Backlog | — |
| FN-374 | QA: Validate approve button saves form changes before transitioning | qa | Backlog | — |

## Acceptance Criteria
- [ ] Approve button saves all pending form changes (rate, stops, driver, broker, notes, etc.) before transitioning status
- [ ] Save + transition happens in a single database transaction
- [ ] If save fails, status does not change and user sees an error
- [ ] Existing behavior preserved when no changes were made (just approve)
- [ ] Works for both single-stop and multi-stop loads

---

### FN-372 (subtask) — Backend: Update approve-draft endpoint

**Implementation Summary:**
Modified `PATCH /api/loads/:id/approve-draft` to accept an optional request body with load fields and persist them in a single DB transaction before transitioning status to DISPATCHED.

**Changes:**
- Upgraded endpoint from simple `query()` to `getClient()` with explicit `BEGIN`/`COMMIT`/`ROLLBACK` transaction management
- Added field update logic (reuses same `fieldMap` pattern as `PUT /:id`): loadNumber, dispatcherUserId, driverId, truckId, trailerId, brokerId, brokerName, poNumber, rate, notes, completedDate
- Added stops update logic (same pattern as `PUT /:id`): deletes existing stops, inserts new ones, updates pickup_date/delivery_date on loads table
- If no body is provided, existing behavior is preserved (just transitions status)
- On any error, transaction rolls back — status stays DRAFT

**Files Changed:**
- `backend/packages/goodmen-shared/routes/loads.js` — approve-draft endpoint (~line 831)

**Key Decisions:**
- Excluded `status` and `billingStatus` from the fieldMap since approve-draft always forces `status = 'DISPATCHED'`
- Reused the same stops insert pattern from `PUT /:id` for consistency

## Deployment Handoff

**Services to restart/redeploy:** goodmen-shared backend service
**DB migrations:** None
**Environment/config changes:** None
**Cache invalidation:** None
**Feature flags:** None
**Deploy order:** Backend only (this subtask)
**Post-deploy verification:** Call `PATCH /api/loads/:id/approve-draft` with a body containing `{ "rate": 1500 }` on a DRAFT load and verify the rate is persisted along with status change to DISPATCHED
