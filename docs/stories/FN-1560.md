# FN-1560: [Warehouse Receiving] Quick-add receives at $0 cost; need editable unit cost + part default_cost reconcile

## Requirement

On the Warehouse Receiving page, adding a part via Quick add posts the receive line with `unit_cost = 0.00` whenever `parts.default_cost` is zero (which it is for many SKUs). There is no way to type the actual cost on the quick-add row, no way to edit it on the receive-lines table after the fact, and no way to push a corrected cost back into the part record so future receives default to the new figure.

Reproduction (from screenshot `Screenshot 2026-05-08 at 12.41.25 AM.png`): Allstate Garland — Main Warehouse, ticket `RCV-E8B7-1778200920738-0958`, search "tran" → click `+ Add` for `TRN-442 Transmission Sensor` qty 5 → row appears with UNIT COST `0.00`.

## Acceptance Criteria

- [ ] Quick-add panel rows show editable unit-cost input seeded from `default_cost`; `+ Add` posts the user-entered cost.
- [ ] Receive-lines UNIT COST cell is inline-editable on DRAFT tickets; blur/Enter persists via `PATCH /receiving/:ticketId/lines/:lineId`.
- [ ] Backend route `PATCH /receiving/:ticketId/lines/:lineId` validates and updates `unit_cost` (and optionally `qty_received`, `bin_location_override`); rejects edits on POSTED tickets; role-gated.
- [ ] When entered cost differs from `parts.default_cost` by > $0.01, UI shows non-blocking confirmation: "Update {sku} default cost from $A to $B?" with Update / Skip / Skip all this session.
- [ ] On Update, `PATCH /api/parts/:id` writes `default_cost` (and `default_retail_price` if user opts in via second checkbox tied to current markup); audit logged via `dtLogger`.
- [ ] Existing barcode-scan flow keeps working with no regression.

## Dependencies

None (parent epic FN-1477 is in progress; no blocking sibling story).

## Agent

frontend + backend

## Integration Branch

`integration/FN-1560` (created by first subtask agent from `origin/dev`; subtasks branch off this; story PR merges to `dev`)

## Subtasks

| Key     | Summary                                                                 | Agent    | Branch                                  | Files Touched | Status              |
|---------|-------------------------------------------------------------------------|----------|-----------------------------------------|---------------|---------------------|
| FN-1562 | Editable unit cost on quick-add + receive-lines + reconcile prompt      | frontend | `agent/frontend/FN-1562/quick-add-cost` | `frontend/src/app/components/warehouse-receiving/quick-add-panel.*`, `warehouse-receiving.*`, `services/api.service.ts` | Done |
| FN-1566 | PATCH receiving line unit_cost + PATCH part default_cost                | backend  | `backend/FN-1566/cost-patch-routes`     | `backend/packages/goodmen-shared/routes/receiving.js`, `routes/parts.js`, `services/receiving.service.js` (new), `services/parts.service.js`, `utils/cost-validators.js` (new), `utils/cost-validators.test.js` (new), `routes/__tests__/receiving-line-patch.test.js` (new) | Done |
| FN-1563 | QA — validate quick-add cost flows + receive-line edits + reconcile     | qa       | _manual_ (Cypress optional)             | —             | Blocked pending FN-1562, FN-1566 |

## Implementation Summary

### FN-1566 (subtask) — backend cost-patch routes

Added two PATCH endpoints and the supporting service + validator layer that the frontend reconcile flow (FN-1562) consumes:

- `PATCH /api/receiving/:ticketId/lines/:lineId` — updates any subset of `{ unit_cost, qty_received, bin_location_override }` on a DRAFT ticket. Returns `200` with the updated row, `400` on validation/POSTED-ticket errors, `403` for non-`parts_manager`/`admin` roles, `404` when the ticket or line is missing or the line is on a different ticket.
- `PATCH /api/parts/:id` — updates `default_cost` and/or `default_retail_price` only. Emits a `parts_cost_changed` audit log via `dtLogger` (old/new values, partId, performedBy from `req.user.id`, optional `ticketId` from request body) per the parent AC's "audit logged via dtLogger" requirement. The existing `PUT /api/parts/:id` route is untouched.

Validation rules (shared between both routes via `utils/cost-validators.js`): cost values must be finite, ≥ 0, and have ≤ 2 decimal places; numeric strings are coerced. `qty_received` must be a positive integer. `bin_location_override` is trimmed, blank → `null`, capped at 64 chars to fit the column.

Both routes use the existing in-file `requireRole(['admin', 'parts_manager'])` middleware. No DB migration was added — `parts.default_cost`, `parts.default_retail_price`, and `receiving_ticket_lines.unit_cost` already exist (`20260216_create_inventory_schema.js`).

### FN-1562 (subtask) — frontend cost editor + reconcile prompt

**Branch:** `agent/frontend/FN-1562/quick-add-cost`

Frontend implementation for FN-1560. Three pieces:

1. **Quick-add panel** — added a per-row unit-cost `<input type="number" step="0.01">` between the qty input and `+ Add` button. Cost seeds from `part.default_cost ?? 0` and is tracked in a new `costByPart: Map<string, number>` (mirroring the existing `qtyByPart`). The static `default_cost` display in the row meta was removed since the input now shows the same value editable. `QuickAddEvent` extended to `{ part, qty, unitCost }`; `+ Add` now emits the user-entered cost.

2. **Warehouse Receiving page** —
   - `onQuickAdd($event)` now passes `$event.unitCost` to `addReceivingLine` (was `part.default_cost`, which is the FN-1560 root cause).
   - Receive-lines table UNIT COST cell is an inline `<input type="number">` for `DRAFT` tickets only (POSTED tickets stay read-only). Pre-edit value is captured on `(focus)`; on `(blur)` or Enter the new cost is PATCHed via `api.updateReceivingLine`. Optimistic update + revert + error toast on failure.
   - After a quick-add or inline edit, if the entered `unit_cost` differs from the part's `default_cost` by more than 1¢, a non-blocking reconcile prompt appears above the lines table: *"Update {sku} default cost from $A to $B?"* with **Update**, **Skip**, **Skip all** buttons. Multiple parts can have prompts simultaneously (deduped by partId). "Skip all" mutes prompts for the rest of the page session (cleared on navigation).
   - On **Update**, calls `api.updatePartCost(partId, { default_cost })` and patches `partDefaultCost` on every line of that part so a follow-up edit doesn't re-trigger the prompt.

3. **API service** — added two methods:
   - `updateReceivingLine(ticketId, lineId, body)` → `PATCH /receiving/:ticketId/lines/:lineId` (body accepts `unit_cost`, `qty_received`, `bin_location_override`).
   - `updatePartCost(partId, body)` → `PATCH /parts/:id` (body accepts `default_cost`, `default_retail_price`).

`ReceivingLineView` gained `partDefaultCost: number | null` so the reconcile check has a reference point for inline edits. `toTicketView` reads it from `part_default_cost` / `partDefaultCost` / `default_cost` / `defaultCost` for forward-compat with the FN-1566 response shape.

## Files Changed

### Backend (FN-1566)

- `backend/packages/goodmen-shared/routes/receiving.js` — added PATCH `/:ticketId/lines/:lineId` after the existing DELETE handler.
- `backend/packages/goodmen-shared/routes/parts.js` — added PATCH `/:id` (cost-only) between the existing PUT `/:id` and PATCH `/:id/deactivate` routes.
- `backend/packages/goodmen-shared/services/receiving.service.js` — **new** file. `updateLine(ticketId, lineId, patch)` plus typed errors (`NotFoundError`, `ConflictError`, `ValidationError`).
- `backend/packages/goodmen-shared/services/parts.service.js` — added `updatePartCostDefaults(id, patch, audit)` and exported it.
- `backend/packages/goodmen-shared/utils/cost-validators.js` — **new** file. Pure validators (`validateCostValue`, `validateQtyReceived`, `validateBinLocationOverride`) with a `ValidationError` class.
- `backend/packages/goodmen-shared/utils/cost-validators.test.js` — **new** file. 18 unit tests covering accept/reject paths.
- `backend/packages/goodmen-shared/routes/__tests__/receiving-line-patch.test.js` — **new** file. 18 route-level tests using the in-memory knex stub pattern (modeled on `work-orders-parts-patch.test.js`).

### Frontend (FN-1562)

- `frontend/src/app/components/warehouse-receiving/quick-add-panel.component.ts` — `costByPart` map, `getRowCost`/`setRowCost`, `unitCost` on `QuickAddEvent`.
- `frontend/src/app/components/warehouse-receiving/quick-add-panel.component.html` — cost input column; removed redundant static cost display from row meta.
- `frontend/src/app/components/warehouse-receiving/quick-add-panel.component.css` — 4-column row grid + `.qap__row-cost-input` styles.
- `frontend/src/app/components/warehouse-receiving/quick-add-panel.component.spec.ts` — 4 new FN-1562 tests covering default seeding, override, invalid fallback, isolation; existing `addPart` test now asserts `unitCost`.
- `frontend/src/app/components/warehouse-receiving/warehouse-receiving.component.ts` — `partDefaultCost` field on line view, `costReconcilePrompts` queue, `skipAllReconciles` flag, `onLineCostFocus`/`onLineCostCommit`, `maybeShowCostReconcile`, `onCostReconcile{Update,Skip,SkipAll}`, `onQuickAdd` posts `event.unitCost`.
- `frontend/src/app/components/warehouse-receiving/warehouse-receiving.component.html` — reconcile-prompt region above lines table; inline cost-edit input on DRAFT lines.
- `frontend/src/app/components/warehouse-receiving/warehouse-receiving.component.css` — `.recv-reconcile*` and `.recv-table__cost-input` styles.
- `frontend/src/app/components/warehouse-receiving/warehouse-receiving.component.spec.ts` — 8 new FN-1562 tests: `onQuickAdd` posts entered cost, reconcile prompt logic, skip-all, `onCostReconcileUpdate` PATCH, inline edit happy path, inline edit revert on failure, no-op on unchanged/invalid.
- `frontend/src/app/services/api.service.ts` — `updateReceivingLine`, `updatePartCost`.

## Key Decisions

### Backend (FN-1566)

- **No new audit table.** Per the Jira description's explicit "Do NOT add a new migration here", parts-cost changes are emitted as a structured `parts_cost_changed` log line with `{ partId, sku, performedBy, ticketId, changes: { field: { from, to } } }`. The Open Items section already tracks the dedicated audit-table follow-up decision.
- **Wrote `default_cost`, not `unit_cost`.** The receiving flow already reads `default_cost` (e.g. `routes/receiving.js:911` falls back to `part.default_cost` when no override is supplied). The reconcile prompt's purpose is to keep that fallback truthful, so PATCH `/api/parts/:id` targets `default_cost` / `default_retail_price` rather than the legacy `unit_cost` / `unit_price` columns the bulk PUT route writes. A future cleanup story can converge the two column families; out of scope here.
- **Service holds DB + validation, route is thin.** Per the Jira description's "do not duplicate validation between route and service" instruction. The route handlers only translate typed-error `statusCode` into the HTTP response and never re-check rules.
- **Validators in a standalone util** so the same rules are unit-testable without an Express + knex harness, and so a future PATCH route can re-use them.
- **Audit log records `from`/`to` as numbers**, coercing the prior `decimal(10,2)` value via `Number()`. Keeps the log machine-grep-able and avoids string/number drift if/when we add the dedicated audit table later.

### Frontend (FN-1562)

- **Reconcile prompt is a banner row above the lines table, not a global snackbar.** No app-wide snackbar service exists; the page already uses `recv-banner` for inline messages, so adding an analogous `recv-reconcile` region keeps the pattern consistent and avoids introducing a new dependency. Multiple prompts stack and dedupe by partId.
- **"Skip all this session" is component-local** (a boolean field on the page component). It clears naturally when the user navigates away, matching the description's "clears on page leave". No persistence across reloads.
- **Inline-edit revert key is `lineId`.** A `Map<string, number>` captures the pre-edit value on `(focus)` and is consulted on `(blur)`/Enter. If the PATCH fails, `line.unitCost` is reverted to that value and an error banner is shown.
- **`partDefaultCost` is best-effort.** Lines added in this session via quick-add or scan know the part's current default_cost (we have the part object); lines loaded from the server only know it if the backend emits `part_default_cost`. When unknown, the reconcile prompt simply doesn't trigger on inline edits — quick-add still triggers it. No blocking dependency on the backend response shape.
- **`PATCH /parts/:id` (not a dedicated cost route).** The story doc left this open; sticking with `PATCH /parts/:id` matches what FN-1566 shipped and keeps the contract narrow. If the backend later adopts a more specific endpoint, only `api.service.ts:updatePartCost` changes.
- **Retail-price-markup checkbox deferred.** The parent AC mentions a "second checkbox tied to current markup" for `default_retail_price`. The API method already accepts the field; UI surfacing is held until product weighs in on markup defaults — adds nothing to the headline `$0 cost` fix and keeps this PR focused. Tracked under Open Items.

## Deployment Handoff

- **Render services to redeploy after merge:**
  - `fleetneuron-inventory-service-dev` (FN-1566 — owns the receiving + parts routes via `goodmen-shared`).
  - `fleetneuron-frontend-dev` (FN-1562 — UI consuming the new PATCH endpoints).
- **Deploy order:** backend → frontend. The PATCH endpoints are additive, so deploying backend first is safe; the frontend will start exercising them once it lands.
- **DB migrations:** none. (`parts.default_cost`, `parts.default_retail_price`, and `receiving_ticket_lines.unit_cost` already exist from `20260216_create_inventory_schema.js`.)
- **Env / config changes:** none.
- **Cache invalidation:** none.
- **Feature flags:** none.
- **Post-deploy verification (combined backend + frontend smoke):**
  1. Log in as a `parts_manager` user, open Warehouse Receiving on dev, create a DRAFT ticket.
  2. Quick-add a part whose `default_cost = 0`. Confirm the cost input appears, type `5.25`, click `+ Add`. The line should show `5.25` (not `0.00`). A reconcile prompt should appear: *"Update {SKU} default cost from $0.00 to $5.25?"*.
  3. Click **Update**. Confirm a `parts_cost_changed` log line in the inventory-service Render logs with `from: 0, to: 5.25`. Reload the page; the next quick-add for that part should default to `$5.25`.
  4. Edit the unit cost on an existing DRAFT line (click the cell, type a new value, blur or Enter). Cell should update via `PATCH /receiving/:id/lines/:id`; reconcile prompt should fire if drift > 1¢.
  5. Click **Skip all this session** — subsequent edits should not surface prompts until page reload.
  6. POST the ticket; the inline cost editor must disappear (POSTED is read-only). Attempting `PATCH /api/receiving/<id>/lines/<lineId>` should now return `400` with "Cannot edit lines on a posted ticket".
  7. Log in as a `technician` user → both PATCH calls should return `403`.
  8. Confirm the existing barcode-scan flow still works (scan barcode → line added with the part's default_cost as before).

## Review Findings

_To be filled by Code Reviewer_

## QA Findings

_To be filled by QA_

## Open Items

- Decide whether a dedicated `parts_cost_changes` audit table is worth a follow-up DB story (currently logged via `dtLogger` only).
- Surface a "also update retail price" checkbox in the reconcile prompt that uses the part's current cost-to-retail markup. The `api.updatePartCost` method already accepts `default_retail_price`; UI deferred pending product input on markup defaults. (Raised by FN-1562 implementation.)
