# FN-378: [Bug] Dashboard shows all zeros — silent degraded fallback activated by failing SQL query

## Overview
The dashboard stats endpoint's monolithic SQL query fails silently, causing all non-vehicle metrics to show zeros. Fix requires isolating subqueries, adding a degraded-mode banner, and removing dead code.

## Subtasks

| Key | Summary | Agent | Branch | Status |
|-----|---------|-------|--------|--------|
| FN-379 | Fix dashboard stats SQL — isolate subqueries | backend | — | In Progress |
| FN-380 | Show degraded-mode banner when stats incomplete | frontend | — | Done |
| FN-381 | Remove dead dashboard route in reporting-service | backend | `agent/backend/FN-381/remove-dead-dashboard-route` | Done |
| FN-382 | QA: Validate dashboard counts match page counts | qa | — | Backlog |

### FN-381 (subtask) — Remove dead dashboard route

**Changes:**
- Verified `reporting-service/server.js` imports from `@goodmen/shared/routes/dashboard` (the active route), NOT from `./routes/dashboard`
- Deleted `backend/microservices/reporting-service/routes/dashboard.js` — an old unscoped version lacking tenant filtering and operating entity scoping
- No dangling imports found

**Files Changed:**
- `backend/microservices/reporting-service/routes/dashboard.js` (DELETED)

## Deployment Handoff

### Services to redeploy
- `reporting-service`

### DB migrations
- None

### Environment/config changes
- None

### Post-deploy verification
1. Verify reporting-service starts without errors
2. Dashboard stats endpoint continues to work (served by goodmen-shared route)
