# FN-306: Replace all raw select dropdowns with app-ai-select across the app

## Requirement
Convert all raw `<select>` HTML elements to the `<app-ai-select>` component for consistent Material Design dropdown styling across the entire app.

## Subtasks
| Key | Summary | Agent | Branch | Status |
|-----|---------|-------|--------|--------|
| FN-307 | Convert dispatch-drivers selects (13) | frontend | `frontend/FN-307/convert-dispatch-drivers-selects` | Done |
| FN-308 | Convert settlements selects (12) | frontend | — | Pending |
| FN-309 | Convert safety selects (10) | frontend | — | Pending |
| FN-310 | Convert loads/drivers/fuel/IFTA selects (15) | frontend | — | Pending |
| FN-311 | QA: Validate all converted dropdowns | qa | _manual_ | Pending |

### FN-307 (subtask)
**Converted 13 raw selects in dispatch-drivers:**

1. Filter: driver type, status, pay basis (3 header selects)
2. Form: truck, trailer, CDL class (3 modal selects)
3. Expense responsibility row selects (1 in *ngFor)
4. Recurring deduction: expense category, target, amount type, frequency, applies when, source type (6 modal selects)

**TS additions:**
- 6 readonly option arrays (driverTypeOptions, driverStatusOptions, payBasisOptions, cdlClassOptions, deductionTargetOptions, deductionAmountTypeOptions)
- truckSelectOptions property (built when vehicles load)
- trailerSelectOptions getter (reactive to driver assignment)
- expenseCategoryOptions getter (from expenseKeys)
- sourceTypeOptions getter (from deductionExpenseTypeOptions)

**Files changed:**
- `frontend/src/app/components/dispatch-drivers/dispatch-drivers.component.html`
- `frontend/src/app/components/dispatch-drivers/dispatch-drivers.component.ts`

## Deployment Handoff
- **Services**: frontend
- **DB migrations**: None
- **Env/config**: None
- **Post-deploy verification**: Open dispatch-drivers page, verify all dropdowns use ai-select styling

## Review Findings
_To be filled by Code Reviewer_

## QA Findings
_To be filled by QA_

## Open Items
- None
