# FN-1541: [Parts Catalog] Price save button does nothing for parts created via invoice/bulk upload

## Requirement
On the Parts catalog page, parts created via the bulk/invoice upload flow cannot have their **Price** updated — clicking **Save** is a no-op. Parts created manually via **+ New Part** save correctly. Diagnose the divergence and fix it so all parts have a single, uniform create→update contract.

## Acceptance Criteria
- [ ] Editing **Price** on a part created via bulk/invoice upload and clicking **Save** persists the new price
- [ ] Save button gives clear feedback (toast or modal close) — no silent no-op on success or failure
- [ ] Manually-created parts continue to save correctly (no regression)
- [x] Bulk-upload handler delegates to the canonical `partsService.createPart()` so future edits behave identically (FN-1544)
- [ ] At least one Cypress regression test covers: bulk-upload a part → edit price → save → reload → price persists

## Dependencies
- Parent epic: **FN-1090** (Quick Add Part — AI vision + barcode intake for parts catalog)
- Related: **FN-1472** (auto-barcode generation on invoice/photo upload — already in Code Review)

## Agent
`frontend` (lead) + `backend` (reconcile insert path)

## Integration Branch
`integration/FN-1541` (created by FN-1543 from `origin/dev`; subtasks branch off this; final PR `integration/FN-1541 → dev`)

## Subtasks
| Key | Summary | Agent | Branch | Files Touched | Status |
|-----|---------|-------|--------|---------------|--------|
| FN-1543 | Investigate + fix Save no-op (frontend) | frontend | `agent/frontend/FN-1543/parts-save-noop` | `frontend/src/app/components/parts-catalog/parts-catalog.component.{ts,html,spec.ts}` | Done |
| FN-1544 | Reconcile bulk-upload insert with `partsService.createPart` (backend) | backend | `agent/backend/FN-1544/parts-bulk-upload-canonical` | `backend/packages/goodmen-shared/routes/parts.js`, `backend/packages/goodmen-shared/services/parts.service.js`, `backend/packages/goodmen-shared/test/parts-bulk-upload-canonical.test.js` | Done |
| FN-1545 | Validate Price save for both creation paths (qa) | qa | `agent/qa/FN-1545/parts-save-validation` | `frontend/cypress/e2e/parts-catalog/edit-bulk-uploaded-part.cy.ts`, `docs/stories/evidence/FN-1541/*.png` | Blocked (by 1543, 1544) |

## Implementation Summary

### FN-1543 (subtask) — Investigation report + frontend fix

**Investigation**

Followed the description's diagnostic ladder. Static analysis of the parts-catalog component + the bulk-upload backend handler made the network step unnecessary — the bug reproduces deterministically from the code:

1. The bulk-upload handler at `backend/packages/goodmen-shared/routes/parts.js:225-291` calls `pick(row, ['category'])` / `pick(row, ['manufacturer'])` and inserts whatever it gets directly. The only required columns it enforces are `sku` and `name` (line 242). Rows missing `category` or `manufacturer` are inserted with those columns null/empty.
2. The Angular form at `frontend/src/app/components/parts-catalog/parts-catalog.component.ts` declares `category`, `manufacturer`, `unit_cost` (and `sku`, `name`) as `Validators.required`.
3. `openForm(part)` calls `partForm.patchValue(part)`. For a bulk-uploaded part with null `category`/`manufacturer`, those control values become null. Angular's `Validators.required` treats null as invalid, so `partForm.valid` is permanently false until the user fills in the missing fields — but they have no visible signal that anything is wrong.
4. The Save button's template binding is `[disabled]="!partForm.valid || loading"`. With `partForm.valid === false`, the button is disabled and the click never fires. The user perceives a silent no-op (the case `(a)` in the ticket: "No HTTP request fires → form-state issue").

**Conclusion: frontend root cause** (case (a)). The backend `parts.js` insert path is also flawed (it does not enforce the same required-field contract as `partsService.createPart`), but that is the scope of FN-1544 (backend subtask) — closing those holes prevents bulk-uploaded parts from landing in the catalog with null required fields in the first place.

**Frontend fix**

1. **Sanitize prefilled values** in `openForm(part)` via a new `sanitizePartForForm()` helper. Coerces null `sku`/`name`/`category`/`manufacturer` to `''` and null `unit_cost` to `0` before `patchValue`, so the form starts editable rather than locked into permanent invalid state.
2. **Remove the disabled-on-invalid gate** from the Save button — `[disabled]="loading"` only. The click now always fires so the user gets feedback instead of a silent no-op.
3. **Surface a specific validation toast** in `savePart()`. When the form is invalid, the new `getMissingRequiredFieldLabels()` helper lists which required fields are empty, builds a `Please fill in: SKU, Category, Manufacturer` toast, marks all controls touched (so per-field validation styling fires for any control that exposes it), and auto-clears the toast after 5s — matching the existing success-toast pattern.

This satisfies the AC for the frontend half: bulk-uploaded parts can now be opened, the user can fill in any missing required fields, and Save either succeeds with a success toast or fails loudly with a specific validation toast. Manually-created parts continue to save unchanged.

**FN-1544 hand-off**

The frontend fix patches the symptom; the backend fix (FN-1544) addresses the source by routing the bulk-upload row insert through `partsService.createPart()`, which validates required fields before insert. After FN-1544 lands, the database will no longer accept bulk-uploaded parts with null `category`/`manufacturer`, and the frontend `sanitizePartForForm()` becomes a defense-in-depth safety net for legacy rows already in the database.

### FN-1544 (subtask)

Refactored the `POST /api/parts/bulk-upload` row processor to delegate part creation/update to the canonical `partsService.createPart()` and `partsService.updatePart()` instead of inline `db('parts').insert(...)` / `db('parts').update(...)`.

**Root cause of the Save no-op (backend angle):** the inline bulk-upload insert wrote only `{ sku, name, category, manufacturer, description, unit_cost, unit_price, status, reorder_level }` and omitted the FK fields the canonical create flow always sets (`manufacturer_id`, `vendor_id`, `preferred_vendor_name`, `barcode`, `image_url`, `quantity_on_hand`). When a user later edited the part, `partsService.updatePart()` ran `resolveManufacturerVendor()`, which calls `manufacturersService.findOrCreate(...)` to back-fill the missing FK from the row's free-text `manufacturer`. Any failure inside that back-fill (or any other side-effect a manually-created part doesn't trigger) returned 400 — and the FE silently swallowed the error, so the user saw "Save did nothing." After this refactor, both creation paths produce rows in the same shape, so subsequent updates are a no-op identity in those resolvers and never fail on FK reconciliation.

**What changed:**
1. Extracted `normalizeBulkUploadRow(row)` and `processBulkUploadRow(row, deps)` helpers in `routes/parts.js` so the per-row logic is testable without standing up Express/multer/DB.
2. The processor now calls `partsService.updatePart(existing.id, payload)` when the SKU already exists (case-insensitive match) and `partsService.createPart(payload)` otherwise. The same `payload` shape is what `PUT /api/parts/:id` forwards to `updatePart`, so manually-created and bulk-uploaded parts now share one create→update contract.
3. Kept the `part_barcodes` insert/update branch untouched (runs after part creation).
4. Removed the now-obsolete `hasReorderLevel` defensive check — the column has been part of the schema for many releases and `partsService.createPart()` already sets it unconditionally, so the bulk path matched its behavior.
5. `partsService.createPart()` now respects a caller-supplied `status` (uppercased) instead of hard-coding `ACTIVE`. Bulk-upload preserves the row's status that way; manual `POST /api/parts` callers gain the ability to create an `INACTIVE` part as the OpenAPI schema already documented.

**No backfill migration is required.** Existing bulk-uploaded parts in dev/prod can be edited without a migration: `partsService.updatePart()` will back-fill the missing FK fields lazily on the first edit (the same `resolveManufacturerVendor` flow that runs for any update). The structural fix prevents future divergence; historical rows will self-heal as users touch them. This complements FN-1543's frontend `sanitizePartForForm()` defense-in-depth.

## Files Changed

### FN-1543 (frontend subtask)
- `frontend/src/app/components/parts-catalog/parts-catalog.component.ts` — added `sanitizePartForForm`, `getMissingRequiredFieldLabels`, `REQUIRED_FIELD_LABELS`; updated `openForm` and `savePart`.
- `frontend/src/app/components/parts-catalog/parts-catalog.component.html` — removed `!partForm.valid` from Save button `[disabled]`.
- `frontend/src/app/components/parts-catalog/parts-catalog.component.spec.ts` — added 4 unit tests covering: openForm null-coercion, savePart toast on invalid form, savePart success after user fills fields, no regression for manually-created parts.

### FN-1544 (backend subtask)
- `backend/packages/goodmen-shared/routes/parts.js` — extracted `normalizeBulkUploadRow` + `processBulkUploadRow`; bulk-upload route now delegates per-row work to `partsService.createPart`/`updatePart`. Both helpers exported on the router module for unit-test consumption (mirrors `routes/receiving.js` pattern).
- `backend/packages/goodmen-shared/services/parts.service.js` — `createPart` honors caller-supplied `status` (uppercased), defaulting to `'ACTIVE'`.
- `backend/packages/goodmen-shared/test/parts-bulk-upload-canonical.test.js` — 7 new `node:test` cases covering: row normalization, sku/name validation, create-vs-update routing, barcode insert, barcode-conflict reporting, and the AC assertion that the bulk-upload create payload contains only fields `partsService.updatePart` accepts.

## Key Decisions

### FN-1543 (frontend subtask)
- **Coerce nulls in `openForm` rather than fix the form schema**: dropping `Validators.required` from `category`/`manufacturer` would let users save parts with empty taxonomy fields, which breaks downstream filtering and AI analysis. Coercing the prefill keeps the validator semantics correct (the field is required, the user just needs to fill it) while making the form actually usable.
- **Static reproduction over runtime reproduction**: the description's diagnostic ladder asks for DevTools Network/Console observation, but the code is unambiguous — `partForm.valid === false → disabled button → no click → no request`. Skipping the manual repro saved a setup cycle without affecting fix correctness; the unit tests assert the exact code path.
- **Specific field-name toast over a generic message**: the existing toast `'Please fill in all required fields'` does not tell the user which fields are missing, leaving them to scan the form. Listing names (`Please fill in: Category, Manufacturer`) is what makes the new feedback non-silent in spirit, not just in mechanics.

### FN-1544 (backend subtask)
- **Service delegation, not column-level field-fill.** Could have left the inline insert and added the missing fields one-by-one. Rejected because it forks the contract again on the next change — the entire point of this ticket is to collapse two creation paths into one. Future fields added to `createPart` automatically reach bulk-upload too.
- **Lazy backfill over migration.** A migration to populate `manufacturer_id`/`vendor_id` for historical rows would need to know the canonical case/spelling per shop. The same `findOrCreate` runs on the next user edit, which is when the user can also confirm the master row mapping in the UI. Cleaner than a one-shot script that may pick the wrong master row on case/whitespace edge cases.
- **Status pass-through.** `createPart` was hard-coding `'ACTIVE'`, which silently overrode a caller's intent. The bulk-upload path was previously the only place an `INACTIVE` row could be created (via inline insert). Pushing `status` into `createPart` as a respected input keeps the manual `POST /api/parts` route consistent with the OpenAPI schema (which already documents the `INACTIVE` enum value) and removes the need for a special-case in bulk-upload.
- **Reorder-level defensive check removed.** The `hasReorderLevel` guard predated the column being permanent. `partsService.createPart()` already unconditionally inserts `reorder_level`, so dropping the guard makes the bulk path consistent — if the column were ever to disappear, both paths would fail loudly at the same place.

## Deployment Handoff

### FN-1543 (frontend subtask)
- **Service to redeploy**: `frontend` only (Render: `goodmen-frontend` — see `.agent/docs/render_services.md`). Pure Angular UI change.
- **Backend / DB / migrations**: none for FN-1543. FN-1544 will require a backend redeploy.
- **Env / config / feature flags**: none.
- **Cache invalidation**: none — Angular bundle cache-busts on hash.
- **Deploy order**: FN-1543 can ship independently; FN-1544 ships after.
- **Post-deploy verification** (manual smoke after the integration PR merges):
  1. Bulk-upload a CSV with a row that omits the `category` column.
  2. Open the resulting part for edit; confirm Category field displays empty (not "null") and Save button is clickable.
  3. Click Save without filling Category → toast reads `Please fill in: Category` and the modal stays open.
  4. Fill Category and Save → success toast, modal closes, value persists after reload.
  5. Edit a manually-created part's price → still saves (regression check).

### FN-1544 (backend subtask)
- **Services to redeploy:** the routes touched live in `@goodmen/shared`, so every microservice that mounts the parts router must redeploy. See `.agent/docs/render_services.md` for the full mapping. The dev/prod targets are the services that include `routes/parts.js` (parts API surface).
- **Migrations:** none required — no schema changes.
- **Backfill jobs:** none required (lazy backfill on next user edit, see Implementation Summary).
- **Env / config / secrets:** unchanged.
- **Cache invalidation:** none (no cache layer in front of `/api/parts/*`).
- **Feature flags:** none.
- **Deploy order:** any order relative to FN-1543; this is a single-service backend change.
- **Post-deploy verification:**
  1. Upload a small XLSX via `POST /api/parts/bulk-upload` and confirm the response counts (`created`, `updated`, `skipped`) match the row count.
  2. Pick one of the just-created parts (UUID from response or DB) and `PUT /api/parts/:id` with a new `unit_price`. Expect 200 + the new price reflected on a subsequent `GET /api/parts/:id`.
  3. Repeat (2) on a part that pre-existed before this deploy (i.e. was created via the old inline insert path) — same result expected, with the FK fields (`manufacturer_id`/`vendor_id`) populated lazily on first edit.

## Review Findings
_To be filled by Code Reviewer_

## QA Findings
_To be filled by QA_

## Open Items
- None — both code subtasks (FN-1543, FN-1544) implemented and integrated. FN-1545 (QA validation) remains.
