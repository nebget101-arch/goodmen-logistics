# FN-283: Split Address field into Address, City, State, Zip with zip code auto-populate

## Requirement
Split the single Address field on the DQF/Driver page into four separate fields: Street Address, City, State, and Zip Code. When a user enters a zip code, City and State should auto-populate via an external zip code lookup API (Zippopotam.us).

## Acceptance Criteria
- [ ] Address field is split into: Street Address, City, State, Zip Code
- [ ] Entering a valid zip code auto-populates City and State via API lookup
- [ ] All four fields are saved separately in the database
- [ ] Existing single-address data is migrated (best-effort parse or left in street address)
- [ ] Edit Driver modal and DQF page both use the new split fields
- [ ] Form validation: zip code format (5-digit or ZIP+4), state as 2-letter code
- [ ] Works on both Add Driver and Edit Driver flows

## Dependencies
- None (standalone enhancement)

## Agent
database -> backend -> frontend (sequential)

## Subtasks
| Key | Summary | Agent | Branch | Status |
|-----|---------|-------|--------|--------|
| FN-284 | DB migration: split address columns | database | `database/FN-284/split-address-columns` | Done |
| FN-285 | Backend: update CRUD + zip lookup | backend | `backend/FN-285/split-address-endpoints` | Done |
| FN-286 | Frontend: split form + zip auto-populate | frontend | `frontend/FN-286/split-address-form` | Done |
| FN-287 | QA: validate split address fields | qa | _manual_ | Done |

### FN-284 (subtask)
**Migration**: `20260327100000_split_driver_address_columns.js`

Changes to `drivers` table:
- Added `street_address TEXT` (nullable)
- Added `city VARCHAR(100)` (nullable)
- Added `state VARCHAR(2)` (nullable)
- Added `zip_code VARCHAR(10)` (nullable)
- Copied existing `address` data into `street_address`
- Dropped old `address` column

Rollback recreates `address` and copies `street_address` back.

### FN-285 (subtask)
**Backend endpoint changes:**

1. **POST `/api/drivers`**: Accepts `streetAddress`, `city`, `state`, `zipCode` (backward compat: also accepts legacy `address` field, stored as `street_address`)
2. **PUT `/api/drivers/:id`**: `allowedDriverFields` updated to accept `streetAddress`, `city`, `state`, `zipCode` instead of `address`
3. **GET `/api/drivers?view=dqf`**: Returns `street_address`, `city`, `state`, `zip_code` (auto-mapped to camelCase by `transformRows`)
4. **GET `/api/dqf/drivers/:driverId`**: Returns `street_address`, `city`, `state`, `zip_code` (mapped to camelCase by `transformRow`)
5. **NEW GET `/api/drivers/zip-lookup/:zipCode`**: Calls Zippopotam.us API, returns `{ zipCode, city, state }`

**Files changed:**
- `backend/packages/goodmen-shared/routes/drivers.js`
- `backend/packages/goodmen-shared/routes/dqf.js`

### FN-286 (subtask)
**Frontend changes:**

1. **Form model**: Replaced `address: ''` with `streetAddress`, `city`, `state`, `zipCode` in both `newDriver` init and `resetForm()`
2. **Add Driver form**: Replaced single address input with 4 fields (Street Address, City/State/Zip in a row)
3. **Edit Driver form**: Same split — 4 fields with inline layout
4. **Zip lookup**: `onZipCodeBlur()` method calls `apiService.lookupZipCode()` on blur, auto-fills city and state
5. **API service**: Added `lookupZipCode(zipCode)` method calling `GET /api/drivers/zip-lookup/:zipCode`

**Files changed:**
- `frontend/src/app/components/drivers/drivers.component.ts`
- `frontend/src/app/components/drivers/drivers.component.html`
- `frontend/src/app/services/api.service.ts`

## Implementation Summary
Full-stack split of the single address field into 4 separate fields (street_address, city, state, zip_code) with zip code auto-populate via Zippopotam.us API.

## Files Changed
- `backend/packages/goodmen-database/migrations/20260327100000_split_driver_address_columns.js` (new migration)
- `backend/packages/goodmen-shared/routes/drivers.js` (CRUD + zip lookup endpoint)
- `backend/packages/goodmen-shared/routes/dqf.js` (DQF read endpoint)
- `frontend/src/app/components/drivers/drivers.component.ts` (form logic)
- `frontend/src/app/components/drivers/drivers.component.html` (form UI)
- `frontend/src/app/services/api.service.ts` (zip lookup service)

## Key Decisions
- Used Zippopotam.us (free, no API key) for zip code lookup
- Backward compatibility: POST still accepts `address` field, stored as `street_address`
- PUT uses `toSnakeCase()` on `streetAddress` -> `street_address` automatically

## Deployment Handoff
- **Deploy order**: DB migration (FN-284) -> Backend (FN-285) -> Frontend (FN-286)
- **Services**: goodmen-database, goodmen-shared, frontend
- **DB migrations to run**: `20260327100000_split_driver_address_columns.js`
- **No new env vars needed** (Zippopotam.us is free, no API key)
- **Post-deploy verification**:
  - DB: `SELECT street_address, city, state, zip_code FROM drivers LIMIT 5;` — verify old address data is in street_address
  - API: `curl /api/drivers/zip-lookup/90210` should return `{ zipCode: "90210", city: "Beverly Hills", state: "CA" }`

## Review Findings
**Reviewer**: Claude Code (2026-03-26)
**Verdict**: APPROVE

**Issues (low severity):**
1. `state` destructured as `driverState` in POST handler to avoid JS keyword collision — correct but worth noting for maintainability (`drivers.js:448`)
2. Frontend uses inline `style` attributes for flex layout — follows existing patterns, acceptable
3. No rate limiting on `/zip-lookup/:zipCode` — low risk for internal app

**Risks:**
- Zippopotam.us downtime would disable auto-populate (graceful fallback — user types manually)
- Data migration is best-effort (existing address → street_address, no parsing)

**Positives:**
- Clean migration with rollback and idempotent column checks
- Backward compatibility on POST (legacy `address` field still accepted)
- INSERT parameter count verified correct (27 params + 2 literals = 29 columns)

## QA Findings
_To be filled by QA_

## Open Items
- None
