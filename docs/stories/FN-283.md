# FN-283: Split Address field into Address, City, State, Zip with zip code auto-populate

## Requirement
Split the single Address field on the DQF/Driver page into four separate fields: Street Address, City, State, and Zip Code. When a user enters a zip code, City and State should auto-populate via an external zip code lookup API (Zippopotam.us).

## Acceptance Criteria
- [ ] Address field is split into: Street Address, City, State, Zip Code
- [ ] Entering a valid zip code auto-populates City and State via API lookup
- [ ] All four fields are saved separately in the database
- [ ] Existing single-address data is migrated (best-effort parse or left in street address)
- [ ] Edit Driver modal and DQF page both use the new split fields
- [ ] Form validation: zip code format (5-digit or ZIP+4), state as 2-letter code
- [ ] Works on both Add Driver and Edit Driver flows

## Dependencies
- None (standalone enhancement)

## Agent
database -> backend -> frontend (sequential)

## Subtasks
| Key | Summary | Agent | Branch | Status |
|-----|---------|-------|--------|--------|
| FN-284 | DB migration: split address columns | database | `database/FN-284/split-address-columns` | Done |
| FN-285 | Backend: update CRUD + zip lookup | backend | `backend/FN-285/split-address-endpoints` | Done |
| FN-286 | Frontend: split form + zip auto-populate | frontend | `frontend/FN-286/split-address-form` | Blocked by FN-285 |
| FN-287 | QA: validate split address fields | qa | _manual_ | Blocked by FN-286 |

### FN-284 (subtask)
**Migration**: `20260327100000_split_driver_address_columns.js`

Changes to `drivers` table:
- Added `street_address TEXT`, `city VARCHAR(100)`, `state VARCHAR(2)`, `zip_code VARCHAR(10)`
- Copied existing `address` data into `street_address`
- Dropped old `address` column

### FN-285 (subtask)
**Backend endpoint changes:**

1. **POST `/api/drivers`**: Accepts `streetAddress`, `city`, `state`, `zipCode` (backward compat: also accepts legacy `address` field, stored as `street_address`)
2. **PUT `/api/drivers/:id`**: `allowedDriverFields` updated to accept `streetAddress`, `city`, `state`, `zipCode` instead of `address`
3. **GET `/api/drivers?view=dqf`**: Returns `street_address`, `city`, `state`, `zip_code` (auto-mapped to camelCase by `transformRows`)
4. **GET `/api/dqf/drivers/:driverId`**: Returns `street_address`, `city`, `state`, `zip_code` (mapped to camelCase by `transformRow`)
5. **NEW GET `/api/drivers/zip-lookup/:zipCode`**: Calls Zippopotam.us API, returns `{ zipCode, city, state }`

**Files changed:**
- `backend/packages/goodmen-shared/routes/drivers.js`
- `backend/packages/goodmen-shared/routes/dqf.js`

## Implementation Summary
_To be filled by implementing agent_

## Files Changed
_To be filled by implementing agent_

## Key Decisions
- Used Zippopotam.us (free, no API key) for zip code lookup
- Backward compatibility: POST still accepts `address` field, stored as `street_address`
- PUT uses `toSnakeCase()` on `streetAddress` → `street_address` automatically

## Deployment Handoff
- **Services**: goodmen-shared (backend)
- **DB migrations to run**: `20260327100000_split_driver_address_columns.js` (from FN-284)
- **Deploy order**: Run DB migration FIRST, then deploy backend
- **No new env vars needed** (Zippopotam.us is free, no API key)
- **Post-deploy verification**: `curl /api/drivers/zip-lookup/90210` should return `{ zipCode: "90210", city: "Beverly Hills", state: "CA" }`

## Review Findings
_To be filled by Code Reviewer_

## QA Findings
_To be filled by QA_

## Open Items
- None
