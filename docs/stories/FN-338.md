# FN-338: Convert separate filter bars to inline table header filters

## Requirement
Move filter controls from separate `.filters-bar` divs into `<tr class="header-filters">` rows inside `<thead>` across all data pages, matching the pattern already used in dispatch-drivers and drivers components.

## Acceptance Criteria
- [ ] All data pages use inline header filters instead of separate filter bars
- [ ] Filters function identically after migration
- [ ] Consistent styling across all pages

## Dependencies
- None

## Agent
frontend

## Subtasks
| Key | Summary | Agent | Branch | Status |
|-----|---------|-------|--------|--------|
| FN-343 | Convert settlement-list and scheduled-deductions filter bars | frontend | `frontend/FN-343/settlements-inline-filters` | Done |

### FN-343 (subtask)
Converted settlement-list (3 filters: week, driver, status) and scheduled-deductions (2 filters: driver, status) from `.filters-bar` divs to inline `<tr class="header-filters">` rows in `<thead>`.

**Files changed:**
- `frontend/src/app/settlements/settlement-list/settlement-list.component.html` — removed `.filters-bar`, added header-filters row
- `frontend/src/app/settlements/settlement-list/settlement-list.component.css` — replaced `.filters-bar` CSS with `.header-filters` styles
- `frontend/src/app/settlements/scheduled-deductions/scheduled-deductions.component.html` — removed `.filters-bar`, added header-filters row
- `frontend/src/app/settlements/scheduled-deductions/scheduled-deductions.component.css` — replaced `.filters-bar` CSS with `.header-filters` styles, updated dark theme override

## Implementation Summary
_To be filled when all subtasks complete_

## Deployment Handoff
- **Services to restart/redeploy**: Frontend static build (auto-deploys on merge)
- **Jobs/workers affected**: None
- **DB migrations**: None
- **Env/config changes**: None
- **Post-deploy verification**: Navigate to Settlements list and Scheduled Deductions — verify filters appear in table header row and function correctly

## Review Findings
_To be filled by Code Reviewer_

## QA Findings
_To be filled by QA_

## Open Items
- None
