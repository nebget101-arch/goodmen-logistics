# FN-391: Loads page UI enhancements

## Subtasks

| Key | Summary | Agent | Status | Branch |
|-----|---------|-------|--------|--------|
| FN-392 | Replace top filter section with color-coded status summary bar | frontend | Selected for Dev | — |
| FN-393 | Fix date picker z-index | frontend | Done | `frontend/FN-393/datepicker-zindex` |
| FN-394 | Reduce column widths and remove Notes column | frontend | Selected for Dev | — |
| FN-395 | Add Completed date filter in column filter row | frontend | Done | `frontend/FN-395/completed-date-filter` |
| FN-396 | Make attachment badges downloadable links | frontend | Selected for Dev | — |
| FN-397 | Show full status/billing names | frontend | Selected for Dev | — |
| FN-398 | QA: Validate all enhancements | qa | Backlog | — |

---

### FN-395 (subtask) — Add Completed date filter

**Implementation Summary:**
Added an `app-inline-date-filter` component under the Completed column header in the filter row, matching the existing Pickup date filter pattern. Filters loads by `completed_date` using date string matching.

**Changes:**
- `loads-dashboard.component.html`: Replaced empty `<th></th>` with `app-inline-date-filter` bound to `headerFilters.completed`
- `loads-dashboard.component.ts`: Added `completed` to `headerFilters` object, `headerFilterLabels`, `clearAllFilters()`, and `filteredLoads` getter

**Files Changed:**
- `frontend/src/app/components/loads-dashboard/loads-dashboard.component.html`
- `frontend/src/app/components/loads-dashboard/loads-dashboard.component.ts`

## Deployment Handoff
**Services to restart/redeploy:** Frontend Angular app
**DB migrations:** None
**Environment/config changes:** None
**Deploy order:** Frontend only
**Post-deploy verification:** Verify Completed column in loads table has a date picker filter. Selecting a date should filter loads by completed_date.
