# FN-391: Loads page UI enhancements — status bar, column sizing, filters, attachments, full status names

## Overview
Multiple UI improvements for the Loads dashboard page to improve usability and match industry TMS standards.

## Subtasks

| Key | Summary | Agent | Branch | Status |
|-----|---------|-------|--------|--------|
| FN-392 | Replace top filter section with color-coded status summary bar | frontend | — | Selected for Dev |
| FN-393 | Fix date picker z-index | frontend | — | Done |
| FN-394 | Reduce column widths and remove Notes column | frontend | `agent/frontend/FN-394/column-sizing` | Done |
| FN-395 | Add Completed date filter | frontend | — | Selected for Dev |
| FN-396 | Make attachment badges downloadable links | frontend | — | Selected for Dev |
| FN-397 | Show full status/billing names | frontend | — | Selected for Dev |
| FN-398 | QA: Validate all Loads page UI enhancements | qa | — | Backlog |

### FN-394 (subtask) — Reduce column widths and remove Notes column

**Changes:**
- Removed Notes column entirely: `<col>`, `<th>` header, filter `<th>` with input, data `<td>`
- Removed `notes` from `headerFilters` type definition, default values, labels map, filter logic, and reset
- Removed `.notes-cell` CSS class from SCSS
- Redistributed column widths across remaining 13 columns (was 14), reducing Driver (9%→8%), Rate (6%→5%), and giving freed space to Broker, Pickup, Delivery, Completed, Attachments

**Files Changed:**
- `frontend/src/app/components/loads-dashboard/loads-dashboard.component.html`
- `frontend/src/app/components/loads-dashboard/loads-dashboard.component.ts`
- `frontend/src/app/components/loads-dashboard/loads-dashboard.component.scss`

## Deployment Handoff

### Services to redeploy
- `fleet-neuron-frontend` (Angular frontend)

### DB migrations
- None

### Environment/config changes
- None

### Post-deploy verification
1. Verify Notes column no longer appears in loads table
2. Verify remaining columns (Driver, Rate, Status, Billing, Attachments) appear narrower
3. Verify notes filter chip no longer appears in active filters
4. Verify notes data is still accessible in the load detail/edit modal
