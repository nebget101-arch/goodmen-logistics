# FN-683: Update Inventory & Work Order UIs to Use New Bin System

## Goal

Replace free-text `bin_location` inputs across Inventory, Receiving, and Work Order UIs with a reusable bin picker backed by `location_bins`.

## Agent

frontend

## Branch (merge target)

`frontend/FN-683/bin-picker-integration`

## Subtasks

| Key | Summary | Agent | Branch | Status |
|-----|---------|-------|--------|--------|
| FN-704 | Reusable `<bin-picker>` component with inline create + text fallback | frontend | `frontend/FN-704/bin-picker-component` | Done |
| FN-705 | Inventory edit UI — integrate bin picker, persist bin_id | frontend | `frontend/FN-705/inventory-bin-picker` | Done |
| FN-706 | Receiving Tickets UI — bin override picker | frontend | | Selected for Dev |
| FN-707 | Work Order parts issuance — read-only bin display | frontend | | Selected for Dev |
| FN-708 | Parts search/lookup — per-location bin breakdown | frontend | `frontend/FN-708/parts-search-bin-breakdown` | Done |
| FN-709 | QA — E2E Cypress tests for bin picker integration | qa | | Backlog |

## Acceptance Criteria

- [ ] Bin picker component works with any location
- [ ] Inventory edit uses picker and persists `bin_id`
- [ ] Receiving ticket bin override uses picker
- [ ] Work order parts issuance shows bin read-only
- [ ] Parts search lookup shows per-location bin breakdown
- [ ] Inline "Create new bin" creates a bin and selects it
- [ ] Fallback text input shown when location has zero bins

## Implementation Summary

_In progress — FN-704, FN-705, FN-708 done; FN-706, FN-707, FN-709 pending._

## Files Changed

_To be updated as subtasks complete._

## Key Decisions

- Expand-row pattern used in parts catalog instead of a separate "parts-search" component (component doesn't exist; parts-catalog is the search surface).
- Stock loaded lazily on first expand per part; cached in a `Map<string, any[]>` for the session.
- `GET /inventory?partId=...` added as `getInventoryByPart()` in `api.service.ts`.

## Deployment Handoff

- **Services to restart/redeploy**: None — frontend-only changes
- **DB migrations**: None
- **Environment/config changes**: None
- **Post-deploy verification**:
  - Navigate to Parts Catalog → click the `▶` chevron on any part row
  - Verify per-location stock breakdown loads and shows location name, bin code badge, and qty
  - Verify "View →" link navigates to `/inventory-reports`
  - Verify compact summary line displays: `SKU — Location (BIN): N units, ...`

---

## Implementation Notes

### FN-708 (subtask)
**Parts search/lookup — show per-location bin breakdown for each part**

**Branch:** `frontend/FN-708/parts-search-bin-breakdown`

1. **`frontend/src/app/services/api.service.ts`** (modified):
   - Added `getInventoryByPart(partId: string)` → `GET /inventory?partId=...`
   - Returns inventory lines for the given part across all locations, each with `location_id`, `location_name`, `on_hand_qty`, `bin_id?`, `bin_code?`, `bin_name?`, `bin_location?`

2. **`frontend/src/app/components/parts-catalog/parts-catalog.component.ts`** (modified):
   - Added `expandedStockPartId: string | null` — tracks which part's row is expanded
   - Added `stockCache: Map<string, any[]>` — lazy cache; populated on first expand per part
   - Added `stockLoadingForId: string | null` and `stockLoadError: string | null` — loading state
   - Added `toggleStockBreakdown(part)` — collapses if already open; loads if uncached
   - Added `getStockLines(partId)` — returns cached lines or `[]`
   - Added `getStockSummary(partId)` — returns compact comma-separated string: `"SHOP-A (A-3): 5 units, WAREHOUSE-1 (W-27): 20 units"`
   - Added `formatStockLine(item)` — formats one line as `"Location Name (BIN-CODE): N units"`

3. **`frontend/src/app/components/parts-catalog/parts-catalog.component.html`** (modified):
   - Added `▶` expand chevron column (first column)
   - Changed `<tr *ngFor>` to `<ng-container *ngFor>` wrapping main row + expand row
   - Main row gets `[class.row-expanded]` when expanded
   - Expand row (`*ngIf="expandedStockPartId === part.id"`): loading/error/empty/cards states
   - Cards grid: one card per inventory line — shows location name, bin badge, on-hand qty, "View →" `[routerLink]` to `/inventory-reports`
   - Color-coded cards: default / yellow `.sb-card-low` (qty ≤ reorder_level) / red `.sb-card-zero` (out of stock)
   - Summary heading: `"Stock for SKU — SHOP-A (A-3): 5, WAREHOUSE-1 (W-27): 20"`

4. **`frontend/src/app/components/parts-catalog/parts-catalog.component.css`** (modified):
   - Added `.btn-expand-stock`, `.expand-chevron` (rotates 90° when open)
   - Added `.stock-breakdown-row`, `.stock-breakdown-cell`, `.sb-*` card/badge/spinner styles
   - Cards use dark fleet theme; amber highlight for low-stock, red tint for zero
