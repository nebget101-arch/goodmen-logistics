# FN-1416: Migrate consumers to fmcsa.* reference data + retire SAFER scraper

## Requirement

Final cutover: switch all live consumers off the FMCSA SAFER/SMS/L&I page scraper onto the new `fmcsa.*` reference dataset, validate parity against current scraped data, then delete the scraper code.

Consumers to migrate:
1. `routes/fmcsa-safety.js` — carrier dashboard, snapshots, L&I, SMS, inspections, crashes
2. `routes/shop-clients.js` — `/fmcsainfo/{dot}` lookup (latency drops from ~5-15s to <50ms)
3. `routes/brokers.js` — populate brokers table from `fmcsa.authorities`, preserving tenant-curated fields

New shared service `fmcsa-reference.js` is the only public read API. After parity QA passes, delete the scraper and drop legacy tables.

## Acceptance Criteria

- [ ] All 3 routes use `fmcsa-reference` service exclusively
- [ ] No live HTTP calls to `safer.fmcsa.dot.gov`, `li-public.fmcsa.dot.gov`, or `ai.fmcsa.dot.gov` remain (verified by grep)
- [ ] `/api/shop-clients/fmcsainfo/{dot}` p95 latency < 200ms
- [ ] Brokers table populated; tenant-curated fields (`notes`, `payment_rating`, `broker_notes`) preserved on import
- [ ] Public phone/email exposed via `getCarrierContacts(dot)` and visible in shop-clients FMCSA lookup
- [ ] Parity report saved to `docs/stories/evidence/FN-1416/parity-report.md` for 5 representative DOTs
- [ ] `fmcsa-safer-scraper.js` and `fmcsa-scrape-queue.js` deleted (or scrape-queue repurposed as import queue)
- [ ] Legacy tables dropped via revertible migration: `fmcsa_safety_snapshots`, `fmcsa_monitored_carriers`, `fmcsa_scrape_jobs`, `fmcsa_basic_details`, `fmcsa_basic_measures_history`, `fmcsa_violations`, `fmcsa_inspection_history`
- [ ] All Cypress + unit tests pass; tests referencing scraper internals updated to use reference service mock

## Dependencies

- **Blocked by FN-1413** — needs Census + Authority data populated → resolved on `dev` (FN-1420 / FN-1422 importers merged)
- **Blocked by FN-1414** — needs Inspections + Crashes + SMS data populated → resolved on `dev`

## Agent

backend

## Integration Branch

`integration/FN-1416` (created by first subtask agent from `origin/dev`; subtasks branch off this; story PR merges to `dev`)

## Two-pass plan (FN-1427 + follow-up)

This story is split across two backend subtasks to resolve a chicken-and-egg in
the original scope: FN-1427 was supposed to (a) migrate consumers and (b)
delete the scraper, but FN-1428 (QA parity) needs the scraper alive to compare
against. Two-pass plan agreed with the product owner before starting:

- **Pass 1 (FN-1427, this subtask)** — build `fmcsa-reference.js`, migrate the
  three route surfaces to use it, add the broker-sync mechanism, add
  `brokers.source` / `fmcsa_synced_at` / pg_trgm GIN indexes. **The scraper
  and legacy tables stay in place** so FN-1428 can run side-by-side parity.
- **Pass 2 (new subtask, see Subtasks table)** — after FN-1428 confirms parity:
  delete `fmcsa-safer-scraper.js`, `fmcsa-scrape-queue.js`, `utils/fmcsa.js`,
  the scrape POST endpoints in `routes/fmcsa-safety.js`, and the legacy reads
  on the BASIC-detail endpoints. Add the revertible drop-legacy-tables
  migration. Verify the `safer.fmcsa.dot.gov` / `li-public.fmcsa.dot.gov` /
  `ai.fmcsa.dot.gov` grep returns clean.

## Subtasks

| Key | Summary | Agent | Branch | Files Touched | Status |
|-----|---------|-------|--------|---------------|--------|
| FN-1427 | [BE] fmcsa-reference service + migrate consumer reads (pass 1) | backend | `backend/FN-1427/fmcsa-consumer-migration` | `services/fmcsa-reference.js` (new), `services/fmcsa-reference.test.js` (new), `routes/{fmcsa-safety,shop-clients,brokers}.js` (modified), `migrations/20260506140000_add_brokers_source_and_synced_at.js`, `migrations/20260506140100_add_brokers_trgm_index.js` | Done |
| FN-1428 | [QA] Parity validation + latency benchmarks + scraper retirement check | qa | _manual_ | — | Backlog |
| FN-1451 | [BE] Retire SAFER scraper (pass 2) | backend | `backend/FN-1451/retire-safer-scraper` | DELETE `services/fmcsa-safer-scraper.js`, `services/fmcsa-scrape-queue.js`, `utils/fmcsa.js`; removed `POST /scrape*` endpoints + `initQueue` + scrape queue wiring in `integrations-service/server.js`. Read endpoints + legacy tables intentionally kept until the frontend cleanup ticket lands. | Done |

## Implementation Summary

### FN-1427 (subtask, pass 1)

`backend/packages/goodmen-shared/services/fmcsa-reference.js` is the new single
read API over the `fmcsa.*` schema. It exposes:

- `getCarrier(dot)`, `getCarrierContacts(dot)`, `getCarrierAuthorities(dot)`,
  `searchCarriers({ q, limit, offset })`
- `getBroker(mc)`, `searchBrokers({ q, limit, offset })`
- `getInspections(dot, { since })`, `getInspectionStats(dot, { since })`
- `getCrashes(dot)`
- `getBasicScores(dot, { latest = true })`

All reads go through `getFmcsaKnex()` (Phase-1 returns the main DB Knex; Phase-2
will swap to a separate connection without touching consumers).

**Consumer migrations:**

- `routes/shop-clients.js` `/fmcsainfo/{dot}` — was making a live HTTP call to
  `mobile.fmcsa.dot.gov`; now reads from fmcsa-reference. Response now also
  surfaces phone/fax/email + the latest BASIC scores. `axios` import removed.
- `routes/brokers.js` — added `POST /api/brokers/sync-from-fmcsa` (admin-only)
  and `syncBrokersFromFmcsa()` exported function. Two SQL passes: INSERT
  active broker authorities not yet in the table (NOT EXISTS guard on
  `mc_number`), then UPDATE rows where `source='fmcsa'` to refresh data.
  Manual rows (`source='manual'`) are NEVER touched. Tenant overrides in
  `tenant_broker_overrides` are structurally untouched.
- `routes/fmcsa-safety.js` — tenant-facing reads (`/my-scores`,
  `/my-scores/:dotNumber/history`) switched to fmcsa-reference. A
  `buildSnapshotShape(dot)` helper assembles a snapshot-shaped row from
  `fmcsa.basic_scores` + `fmcsa.carriers` + `fmcsa.authorities` so the client
  contract stays stable. `safety_rating`, `safety_rating_date`, and
  `out_of_service_date` return null pending an importer enhancement.

  The internal admin endpoints (`/dashboard`, `/carriers` CRUD,
  `/carriers/:id/basic-details*`, `/inspection-details*`, `/jobs`, `/scrape*`
  POSTs) **intentionally still read from / write to legacy tables** in pass 1
  so the scraper can run in parallel for QA parity. Pass 2 retires them.

**New migrations** (`backend/packages/goodmen-database/migrations/`):

- `20260506140000_add_brokers_source_and_synced_at.js` — adds `source` (CHECK
  IN ('manual','fmcsa'), default 'manual') + `fmcsa_synced_at` timestamptz,
  backfills existing rows to 'manual', adds `idx_broker_source` btree index.
- `20260506140100_add_brokers_trgm_index.js` — `CREATE EXTENSION IF NOT EXISTS
  pg_trgm` + GIN trigram indexes on `legal_name`, `dba_name`, `mc_number`.

**Tests:** `services/fmcsa-reference.test.js` is a node:test e2e suite that
seeds fixture rows in `fmcsa.*`, exercises every public API, and tears down.
Skips cleanly when the FMCSA-schema database is unreachable (matches the
existing `fmcsa-knex.test.js` / fmcsa-importer test pattern).

### FN-1451 (subtask, pass 2)

Pass 2 was scoped down at implementation time after auditing consumers:

- **QA parity (FN-1428) was skipped** by product-owner choice. Trail recorded
  in commits and Jira so the decision is auditable.
- **Original AC was wrong** about dropping `fmcsa_inspection_history` and
  `fmcsa_inspection_details`. Both are consumed by unrelated production
  features (FN-474 inspection ingest/list/match, FN-476 match details, FN-478
  PSP report processing in `routes/dqf-documents.js`). Dropping them would
  break those features. They are kept.
- **Drop-legacy-tables migration deferred to a future ticket.** Four frontend
  components (`fmcsa-dashboard`, `fmcsa-carriers`, `fmcsa-carrier-detail`,
  `fmcsa-inspections`) still call the legacy GET endpoints (`/dashboard`,
  `/carriers*`, `/jobs`, `/carriers/:id/basic-details*`, `/inspection-details*`,
  `/my-scores/:dot/basic-details`). Dropping the backing tables today would
  black-hole that admin UI. Frontend retirement is its own story.

What FN-1451 actually delivers:

- Deletes the SAFER scraper modules:
  - `backend/packages/goodmen-shared/services/fmcsa-safer-scraper.js`
  - `backend/packages/goodmen-shared/services/fmcsa-scrape-queue.js`
  - `backend/packages/goodmen-shared/utils/fmcsa.js`
- Removes `POST /scrape`, `POST /scrape/basic-details`,
  `POST /scrape/:carrierId`, `POST /scrape/:carrierId/basic-details` from
  `routes/fmcsa-safety.js`. Also removes the `initQueue`/`scrapeQueue` ref,
  the `canScrape` permission gate, and the `fmcsa_safety.scrape` slot in the
  router-level `requireAnyPermission`.
- Strips scraper-queue wiring from `backend/microservices/integrations-service/server.js`:
  `initScrapeQueue()`, `createScrapeQueue` import, `scrapeQueueInstance`,
  the SIGTERM scrape-queue shutdown hook, and the scrape queue field on
  `/health`.
- Light docstring updates in `services/fmcsa-importer/index.js` and
  `services/fmcsa-import-queue.js` so they no longer reference the deleted
  `fmcsa-scrape-queue.js`.

Verification grep:
```
grep -rn 'safer.fmcsa.dot.gov\|li-public.fmcsa.dot.gov\|ai.fmcsa.dot.gov' \
  backend/packages/goodmen-shared/ backend/microservices/
# (zero matches)
```

## Files Changed

### FN-1427 (pass 1)

- `backend/packages/goodmen-shared/services/fmcsa-reference.js` (new)
- `backend/packages/goodmen-shared/services/fmcsa-reference.test.js` (new)
- `backend/packages/goodmen-shared/routes/shop-clients.js` (modified — `/fmcsainfo/{dot}`)
- `backend/packages/goodmen-shared/routes/brokers.js` (modified — added `/sync-from-fmcsa`)
- `backend/packages/goodmen-shared/routes/fmcsa-safety.js` (modified — `/my-scores`, `/my-scores/:dotNumber/history`, snapshot-shape helper)
- `backend/packages/goodmen-database/migrations/20260506140000_add_brokers_source_and_synced_at.js` (new)
- `backend/packages/goodmen-database/migrations/20260506140100_add_brokers_trgm_index.js` (new)

### FN-1451 (pass 2)

- `backend/packages/goodmen-shared/services/fmcsa-safer-scraper.js` (DELETED)
- `backend/packages/goodmen-shared/services/fmcsa-scrape-queue.js` (DELETED)
- `backend/packages/goodmen-shared/utils/fmcsa.js` (DELETED)
- `backend/packages/goodmen-shared/routes/fmcsa-safety.js` (modified — removed POST `/scrape*` endpoints, `initQueue`, `scrapeQueue`, `canScrape`)
- `backend/microservices/integrations-service/server.js` (modified — removed scrape-queue init + shutdown + health field)
- `backend/packages/goodmen-shared/services/fmcsa-importer/index.js` (docstring trim)
- `backend/packages/goodmen-shared/services/fmcsa-import-queue.js` (docstring trim)

## Key Decisions

- **Two-pass split** instead of one heavy "build + delete" subtask. The QA
  subtask (FN-1428) needs the scraper alive to validate parity, which is
  incompatible with the scraper being deleted in pass 1. Confirmed with
  product owner before starting.
- **Tenant-curated broker fields are structurally protected.** `notes`,
  `payment_rating`, `broker_notes`, `is_blocked`, `is_preferred` already live
  in `tenant_broker_overrides` (keyed by `tenant_id, broker_id`), not on the
  `brokers` table. The `source` column adds a second guard: even
  `brokers.broker_notes` (the legacy unscoped column from
  `20260308000000_add_brokers_extended.js`) is never overwritten because the
  sync only touches rows where `source='fmcsa'`.
- **`mobile.fmcsa.dot.gov` (referenced from `routes/fmcsa.js`) is NOT in the AC's
  forbidden-URL list** (`safer.fmcsa.dot.gov`, `li-public.fmcsa.dot.gov`,
  `ai.fmcsa.dot.gov`). It stays in pass 1; pass 2 will assess.
- **`utils/fmcsa.js` is dead code** (no callers) and contains a
  `safer.fmcsa.dot.gov` URL. Pass 2 deletes it.
- **`fmcsa-scrape-queue.js` will NOT be repurposed** — `fmcsa-import-queue.js`
  is already the importer's queue (FN-1422). Pass 2 deletes scrape-queue.
- **Snapshot-shaped response helper** preserves the existing API contract for
  `/my-scores` so the Angular frontend doesn't need a coordinated change.
  `safety_rating` / `safety_rating_date` / `out_of_service_date` are returned
  as null until the importer adds them.
- **(FN-1451) Pass-2 scope deviated from the literal AC** in three explicit ways:
  1. **QA parity skipped** by product-owner choice. FN-1428 had been auto-marked
     Done by Jira automation without parity validation; PO chose to proceed
     anyway (option (b) in the FN-1427 conversation, and option (X) for the
     scoped-down pass-2 plan).
  2. **`fmcsa_inspection_history` + `fmcsa_inspection_details` NOT dropped** —
     audit found they're consumed by FN-474 / FN-476 / FN-478 (inspection
     ingest, match details, PSP report processing). Original AC was wrong.
  3. **Drop-legacy-tables migration deferred** — four Angular admin components
     still hit `/dashboard`, `/carriers*`, `/jobs`, `/carriers/:id/basic-details*`,
     `/inspection-details*`, `/my-scores/:dot/basic-details`. Backend agent
     scope doesn't include retiring frontend components; that goes in a
     follow-up ticket along with the table drops.
- **(FN-1451) The `fmcsa_safety.scrape` permission constant** is now unused in
  `routes/fmcsa-safety.js` but still exists in the seeded permissions table
  (DB row from `20260326000300_add_fmcsa_safety_permissions.js`). Cleaning the
  permission row out of the DB belongs in the same follow-up that drops the
  legacy tables.

## Deployment Handoff

### FN-1427 (pass 1)

**Render services to redeploy** (`.agent/docs/render_services.md`):
- `fleetneuron-integrations` (mounts `routes/fmcsa-safety.js`, `routes/shop-clients.js`, `routes/brokers.js`)

**DB migrations to run** (in order):
1. `20260506140000_add_brokers_source_and_synced_at.js`
2. `20260506140100_add_brokers_trgm_index.js`

Both are revertible (`down` defined). The trgm index migration runs `CREATE
EXTENSION IF NOT EXISTS pg_trgm` (already created by the fmcsa schema
migration; no-op).

**Post-deploy steps:**

1. Run migrations.
2. Hit `POST /api/brokers/sync-from-fmcsa` once (admin token) to seed the
   broker table from active broker authorities. Subsequent runs are
   idempotent and refresh FMCSA-sourced rows.
3. Verify `GET /api/shop-clients/fmcsainfo/{dot}` returns DB-backed data
   (response now includes `phone`, `fax`, `email`, `mc_number`, `basic_scores`)
   and p95 latency drops to <200ms.
4. Spot-check `/api/fmcsa/safety/my-scores` for at least one tenant: the
   carrier list should still render with BASIC percentiles populated where
   `fmcsa.basic_scores` has data.

**No env vars / feature flags required.**

**No cache invalidation needed.**

**Scraper still runs.** Pass 2 will retire it after FN-1428 (QA) confirms
parity — reference service vs. scraper output for 5 representative DOTs.

### FN-1451 (pass 2)

**Render services to redeploy** (`.agent/docs/render_services.md`):
- `fleetneuron-integrations` (no longer wires up `createScrapeQueue`; safe to
  unset `REDIS_URL` if it was only set for the scrape queue, but other Bull
  consumers in the same service may still need it — check before removing.)

**No DB migrations.** Legacy tables (`fmcsa_monitored_carriers`,
`fmcsa_safety_snapshots`, `fmcsa_basic_details`, `fmcsa_basic_measures_history`,
`fmcsa_violations`, `fmcsa_scrape_jobs`) are intentionally left in place.
A follow-up ticket pairs the frontend admin retirement with the drop migration.

**Behavior changes after deploy:**
- `POST /api/fmcsa/safety/scrape*` (4 endpoints) → 404. The frontend has
  buttons that call these (`triggerScrape`, etc. in
  `frontend/src/app/safety/fmcsa-safety.service.ts`); they will surface as
  HTTP errors. Frontend cleanup is a separate ticket.
- The legacy `fmcsa_*` tables stop receiving fresh data. Reads against them
  return whatever last-scraped data exists (until the follow-up ticket drops
  them).
- `/health` no longer reports a `scrapeQueue` field.

**No env vars / feature flags / cache invalidation.**

**Verification post-deploy:**
1. `curl /api/fmcsa/safety/dashboard` still returns; data is stale but the
   endpoint shape is unchanged.
2. `curl -X POST /api/fmcsa/safety/scrape` returns 404 (route gone).
3. Service starts without `[integrations] FMCSA scrape queue initialized`
   log line; no Redis-dependent failure modes on boot.

## Review Findings
_To be filled by Code Reviewer_

## QA Findings
_To be filled by QA — see FN-1428_

## Open Items
- ~~Run parity QA against 5 representative DOTs before deleting the scraper~~ — **skipped per PO decision** (FN-1428 not actually validated).
- **Open: file the legacy admin retirement ticket** that retires the four Angular components (`fmcsa-dashboard`, `fmcsa-carriers`, `fmcsa-carrier-detail`, the legacy half of `fmcsa-inspections`), removes the now-dead `triggerScrape*` / `getCarriers` / `getDashboard` / `getJobs` / `getCarrierBasicDetails` / `getCarrierInspectionDetails` methods from `frontend/src/app/safety/fmcsa-safety.service.ts`, and pairs the frontend cleanup with the drop migration for `fmcsa_monitored_carriers`, `fmcsa_safety_snapshots`, `fmcsa_basic_details`, `fmcsa_basic_measures_history`, `fmcsa_violations`, `fmcsa_scrape_jobs`. Also clean up the `fmcsa_safety.scrape` permission row from `20260326000300_add_fmcsa_safety_permissions.js`.
- `safety_rating`, `safety_rating_date`, `out_of_service_date` are returned as null in the new `/my-scores` response — open whether the FMCSA Census importer should pull these (they exist in the L&I dataset).
- `mobile.fmcsa.dot.gov` is still referenced in `backend/packages/goodmen-shared/routes/fmcsa.js`. Not in the AC's forbidden-host list, but worth a follow-up assessment.
