# FN-1124: S1 — Daily AI Briefing on Control Center

## Requirement
On `/dashboard` load, surface a Daily AI Briefing card summarizing fleet state for the current day: load throughput, exception count, top driver risk, top vehicle risk, and one recommended next action.

## Acceptance Criteria
- [ ] Briefing renders within 2s on dashboard load (cached) or 6s (cold)
- [ ] Includes 5 sections: throughput, exceptions, driver risk, vehicle risk, recommended action
- [ ] AI service caches briefing per tenant per day; manual refresh button invalidates cache
- [ ] Backend endpoint `GET /api/ai/briefing` aggregates upstream data and forwards to ai-service
- [ ] Frontend card has loading skeleton, error fallback, and a11y labels

## Dependencies
- None (foundational; blocks FN-1125 + FN-1130 + FN-1133)

## Agent
ai (story owner; subtasks span ai, backend, frontend, qa)

## Integration Branch
`integration/FN-1124` (created by first subtask agent from `origin/dev`; subtasks branch off this; story PR merges to `dev`)

## Subtasks
| Key     | Summary                                                | Agent    | Branch                                    | Files Touched                                                      | Status                   |
|---------|--------------------------------------------------------|----------|-------------------------------------------|--------------------------------------------------------------------|--------------------------|
| FN-1139 | Build briefing generator in ai-service                 | ai       | `ai/FN-1139/briefing-generator`           | `services/ai-service/routes/briefing.js`, `services/ai-service/services/briefing-generator.js`, `services/ai-service/prompts/briefing.md`, `services/ai-service/cache/briefing-cache.js`, `services/ai-service/__tests__/briefing.test.js` | Selected for Development |
| FN-1141 | Backend `GET /api/ai/briefing` aggregator + gateway    | backend  | `backend/FN-1141/briefing-aggregator`     | `backend/gateway/routes/ai.js`, `backend/gateway/services/briefing-aggregator.js`, `backend/gateway/__tests__/briefing.test.js`, `backend/gateway/index.js` | Done                     |
| FN-1143 | Daily Briefing card component on Control Center        | frontend | `frontend/FN-1143/daily-briefing-card`    | `frontend/src/app/components/control-center/daily-briefing/**`, `frontend/src/app/services/briefing.service.ts`, `frontend/src/app/components/control-center/control-center.component.html` | Selected for Development |
| FN-1145 | Validate Daily Briefing end-to-end                     | qa       | _manual_                                  | `docs/stories/evidence/FN-1124/`                                   | Backlog (blocked)        |

## Implementation Summary
_To be filled by implementing agent_

## Files Changed
_To be filled by implementing agent_

## Key Decisions
_To be filled by implementing agent_

## Deployment Handoff

### FN-1141 (backend gateway)
- **Service to redeploy**: `fleetneuron-logistics-gateway` (dev) / `fleetneuron-logistics-gateway` (prod) — see `.agent/docs/render_services.md`.
- **No DB migrations**.
- **No new env vars** — reuses existing `LOGISTICS_SERVICE_URL`, `DRIVERS_COMPLIANCE_SERVICE_URL`, `VEHICLES_MAINTENANCE_SERVICE_URL`, `AI_SERVICE_URL`, `JWT_SECRET`.
- **No feature flags**.
- **Cache**: in-memory per-process gateway cache (12h TTL, keyed `tenantId:YYYY-MM-DD`); flushed by restart or `?refresh=true`.
- **Deploy order**: gateway can deploy independently. The route returns 502 with descriptive errors until FN-1139 ships the `POST /api/ai/briefing/generate` endpoint in ai-service. The gateway response will still include `upstreamErrors[]` so the frontend (FN-1143) can render an error state.
- **Post-deploy verification**:
  1. `curl -H "Authorization: Bearer $TOKEN" https://<gateway>/api/ai/briefing` returns 401 without token, 200 with valid JWT once ai-service is live.
  2. `?refresh=true` triggers a fresh fan-out (check gateway logs).

## Subtask Implementation Notes

### FN-1141 (subtask)
- New module `backend/gateway/services/briefing-aggregator.js` does the parallel fan-out via `Promise.allSettled` (so a single upstream failure does not break the briefing) and forwards aggregated `fleetState` to `${AI_SERVICE_URL}/api/ai/briefing/generate`.
- New router `backend/gateway/routes/ai.js` exposes `GET /briefing`, mounted at `/api/ai` **before** the catch-all `/api/ai` proxy in `index.js`. JWT is verified locally on the gateway (using `JWT_SECRET`) so the briefing route can extract `tenant_id` and apply per-tenant caching without a downstream round-trip.
- Caching is gateway-side, in-memory, 12h TTL, keyed `tenantId:YYYY-MM-DD`. `?refresh=true` invalidates the gateway cache **and** sets `forceRefresh: true` in the body sent to ai-service so its own per-day cache is bypassed too.
- Tests run with plain Node (`node backend/gateway/__tests__/briefing.test.js`) — same convention as `ai-service/src/handlers/__tests__/loads-nlq-handler.test.js`. Cases: unauthorized (no/invalid/missing-tenant token), happy path, refresh invalidation, partial upstream failure (sources rejected with `Promise.allSettled` are tracked in `upstreamErrors[]` and forwarded as `null` in `fleetState`).

## Review Findings
_To be filled by Code Reviewer_

## QA Findings
_To be filled by QA_

## Open Items
- None
