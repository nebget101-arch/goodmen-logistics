# FN-422: Toll-to-Settlement Integration

## Overview
Integrate toll transactions into driver settlements as expense deductions.

## Subtasks

| Key | Summary | Agent | Branch | Status |
|-----|---------|-------|--------|--------|
| FN-448 | DB: Create migration for settlement_id FK constraint | database | `agent/database/FN-448/settlement-fk` | Done |
| FN-449 | BE: Auto-consume tolls during settlement processing | backend | — | Selected for Dev |
| FN-450 | FE: Show toll deductions in settlement detail view | frontend | — | Selected for Dev |
| FN-451 | QA: Validate toll-settlement integration | qa | — | Backlog |

### FN-448 (subtask) — DB: settlement_id FK constraint

**Changes:**
- Added partial index `idx_toll_txn_settlement` on `toll_transactions(settlement_id)` where not null
- Added FK constraint `fk_toll_txn_settlement`: `toll_transactions.settlement_id` → `settlements.id` ON DELETE SET NULL
- Reversible: down migration drops constraint and index

**Files Changed:**
- `backend/packages/goodmen-database/migrations/20260328160000_add_toll_settlement_fk.js`

## Deployment Handoff

### Services to redeploy
- None (migration only)

### DB migrations
- `20260328160000_add_toll_settlement_fk.js` — adds FK + index

### Environment/config changes
- None

### Post-deploy verification
1. Run migration: `npx knex migrate:latest`
2. Verify constraint exists: `\d toll_transactions` should show FK to settlements
3. Verify existing toll_transactions with null settlement_id are unaffected
