# FN-1195: Roadside v2 Story 3.1 — Image Upload + Tenant-Scoped Storage

## Requirement

As a driver, I want to upload damage photos that are stored securely and associated with my incident.

## Acceptance Criteria

- [ ] `POST /api/incidents/:id/images` accepts multipart upload with size (≤10 MB) + format (jpg/png/heic) validation
- [ ] Images stored in R2 under tenant-scoped prefix `tenants/{tenantId}/incidents/{incidentId}/`
- [ ] Pre-signed `GET` URLs returned for retrieval
- [ ] Metadata row: `id`, `incident_id`, `tenant_id`, `s3_key`, `mime_type`, `size_bytes`, `uploaded_by`, `uploaded_at`
- [ ] `GET /api/incidents/:id/images` returns all images with signed URLs
- [ ] `GET /api/incidents/:id/images/:imageId` returns single image with signed URL
- [ ] Telemetry: upload count, average size, rejection reasons

## Dependencies

None — independent.

## Integration Branch

`integration/FN-1195` (Shape C — 3 non-QA subtasks: backend, database, devops)

## Subtasks

| Key | Summary | Agent | Branch | Files Touched | Status |
|-----|---------|-------|--------|---------------|--------|
| FN-1231 | [backend] Image upload endpoint + storage service + signed URL retrieval | backend | `backend/FN-1231/incident-images` | See FN-1231 section | Done |
| FN-1232 | [database] incident_images table | database | `database/FN-1232/incident-images-table` | `backend/packages/goodmen-database/migrations/20260611120000_create_incident_images.js` | Done |
| FN-1233 | [devops] S3 bucket provisioning + IAM + Render env wiring | devops | `devops/FN-1233/incident-image-bucket` | `infra/render/drivers-compliance.yaml`, `infra/buckets/incident-images-policy.json`, `docs/runbooks/incident-image-storage.md`, `.env.example` | Done |
| FN-1234 | [qa] Validate image upload: size/format reject, tenant isolation, signed URL TTL | qa | — | — | Canceled |

## Deployment Handoff

### Services to restart
- `goodmen-drivers-compliance-service` (port 5004) — new `/api/incidents/:id/images` endpoints
- No other services affected

### DB migrations
- `backend/packages/goodmen-database/migrations/20260611120000_create_incident_images.js` — creates `incident_images` table (FN-1232, Done)
- FN-1231 backend code assumes this table exists; deploy FN-1232 migration before routing traffic

### Environment / config changes (added by FN-1233)
- **Required** on `fleetneuron-drivers-compliance-service` (the service throws on first upload if any is missing): `R2_ACCOUNT_ID`, `R2_BUCKET`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`. Set as secrets in the Render dashboard (`sync: false`).
- Optional (documented defaults fine): `R2_REGION=auto`, `INCIDENT_IMAGE_SIGNED_URL_TTL=900`, `R2_SIGNED_URL_EXPIRES_SECONDS=900`.
- Per-environment buckets: `fleetneuron-incident-images-{dev,staging,prod}`; each with a **bucket-scoped R2 API token** and CORS allowing the app origin to `GET/PUT/HEAD`.
- Canonical declarations added this subtask: `infra/render/drivers-compliance.yaml` (env contract checklist), `infra/buckets/incident-images-policy.json` (tenant-prefix S3 IAM policy of record), `.env.example` (dev template). Full procedure in `docs/runbooks/incident-image-storage.md`.
- **Note:** the original ticket named AWS-S3 vars (`S3_BUCKET`/`AWS_*`); the backend shipped on Cloudflare R2, so the wiring uses the `R2_*` vars the merged code reads. Wiring the AWS names would be dead config.

### Deploy order
1. FN-1233 (devops) — provision R2 bucket + IAM + set Render env vars
2. FN-1232 (database) — run migration to create `incident_images`
3. FN-1231 (backend) — deploy drivers-compliance-service with new route

### Post-deploy verification
- `POST /api/incidents/{id}/images` with a JPEG file returns 201 with `signed_url`
- `GET /api/incidents/{id}/images` returns array of images
- Uploading a 11 MB file returns 400 with `file_too_large` reason
- Uploading a GIF returns 400 with `unsupported_format` reason

## Implementation Summary

### FN-1231 (subtask — backend)

**Service** (`backend/packages/goodmen-shared/services/incident-images.service.js`):
- `uploadImage(incidentId, file, uploadedBy, context)` — validates size/MIME, writes to R2 under `tenants/{tenantId}/incidents/{incidentId}/`, inserts metadata row into `incident_images`, emits telemetry.
- `listImages(incidentId, context)` — fetches all `incident_images` rows for the incident (tenant-scoped) and decorates each with a short-lived signed GET URL.
- `getImage(incidentId, imageId, context)` — single image retrieval with signed URL.
- Uses existing `backend/packages/goodmen-shared/storage/r2-storage.js` (`uploadBuffer`, `getSignedDownloadUrl`).
- R2 key format: `tenants/{tenantId}/incidents/{incidentId}/{ISO-timestamp}-{originalName}`

**Route** (`backend/packages/goodmen-shared/routes/incident-images.js`):
- `POST /:id/images` — multer `memoryStorage`, 10 MB limit, MIME filter (jpg/png/heic), delegates to `incidentImagesService.uploadImage`.
- `GET /:id/images` — delegates to `incidentImagesService.listImages`.
- `GET /:id/images/:imageId` — delegates to `incidentImagesService.getImage`.
- Mounted at `/api/incidents` in `drivers-compliance-service/server.js` alongside `incidentsTriageRouter`.

**Tests** (`backend/packages/goodmen-shared/services/incident-images.service.test.js`):
- 10 hermetic tests covering: size rejection, MIME rejection, missing incident 404, successful jpg/png/heic upload, list, get, missing image 404.
- All pass: `node --test services/incident-images.service.test.js` → 10/10.

### FN-1232 (subtask — database)

**Migration** (`backend/packages/goodmen-database/migrations/20260611120000_create_incident_images.js`):
- Creates `incident_images` table with UUID PK (uuid_generate_v4), logical cross-service FKs for `incident_id` and `tenant_id`, R2 metadata columns (`s3_key`, `mime_type`, `size_bytes`, `original_file_name`), nullable `uploaded_by` UUID, and `uploaded_at` timestamptz defaulting to now().
- Indexes: `idx_incident_images_tenant_incident (tenant_id, incident_id)` — primary list/scope query; `idx_incident_images_incident_id (incident_id)` — admin/global fallback.
- Idempotent: guarded with `hasTable` check.
- Integrated into `integration/FN-1195` via ff-merge.

### FN-1233 (subtask — devops / infra)

Provisioning-as-docs + env wiring for the incident-image R2 storage the backend
(FN-1231) shipped on. No application code changes.

**Key discovery:** FN-1231 stores images in **Cloudflare R2** (S3-compatible) via
`backend/packages/goodmen-shared/storage/r2-storage.js`, which reads `R2_ACCOUNT_ID`
(required — derives the endpoint `https://<id>.r2.cloudflarestorage.com`),
`R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET`, `R2_REGION` (default
`auto`), and signed-URL TTLs. The ticket's AWS-S3 var names would be dead config,
so this subtask wires the actual `R2_*` contract and documents the divergence.

**Files:**
- `.env.example` — added an "Incident image storage (R2)" block; the pre-existing
  storage section was missing the **required** `R2_ACCOUNT_ID` plus `R2_REGION` and
  the TTL vars. Now matches what the merged code reads.
- `infra/render/drivers-compliance.yaml` — Render Blueprint fragment declaring the
  env contract for `fleetneuron-drivers-compliance-service` (secrets `sync: false`;
  non-secret `R2_REGION`/TTL defaults inline). Source-of-truth checklist for the
  dashboard-managed service.
- `infra/buckets/incident-images-policy.json` — tenant-prefix-scoped S3 IAM policy
  of record (Put/Get/Delete on `tenants/*/incidents/*`, List restricted to
  `tenants/*`, explicit Deny outside the prefix). Applies directly on AWS S3; on R2
  it is the reference intent (R2 enforces via a bucket-scoped token + the app's
  server-side prefix enforcement).
- `docs/runbooks/incident-image-storage.md` — env table, per-environment bucket
  provisioning (create/CORS/lifecycle/token), tenant-isolation defense-in-depth,
  verification curls, troubleshooting, key rotation.

**Tenant isolation** is layered: (1) app always writes under
`tenants/{tenantId}/...` and checks incident ownership; (2) bucket-scoped R2 token;
(3) the IAM policy document as the canonical intent.

Validated: policy JSON parses; render YAML is well-formed Render-blueprint syntax.
Integrated into `integration/FN-1195` via ff-merge.
